---
phase: 08-pfw-skill-alignment
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - examples/fuse-app/Sources/FuseApp/ContactsFeature.swift
  - examples/fuse-app/Sources/FuseApp/TodosFeature.swift
  - examples/fuse-app/Sources/FuseApp/AppFeature.swift
  - examples/fuse-app/Sources/FuseApp/SettingsFeature.swift
  - examples/fuse-app/Sources/FuseApp/DatabaseFeature.swift
  - examples/fuse-app/Sources/FuseApp/CounterFeature.swift
  - examples/fuse-app/Sources/FuseApp/SharedModels.swift
  - examples/fuse-library/Tests/NavigationTests/NavigationTests.swift
  - examples/fuse-library/Tests/NavigationTests/NavigationStackTests.swift
  - examples/fuse-library/Tests/TCATests/StoreReducerTests.swift
  - examples/fuse-library/Tests/TCATests/ObservableStateTests.swift
  - examples/fuse-library/Tests/NavigationTests/UIPatternTests.swift
  - examples/fuse-app/Tests/FuseAppIntegrationTests/FuseAppIntegrationTests.swift
autonomous: true
requirements: []

must_haves:
  truths:
    - "@CasePathable added to all top-level Action enums in fuse-app @Reducer struct types (NOT @Reducer enum types)"
    - "All if case let pattern matches replaced with .is() / [case:] subscript per pfw-case-paths"
    - "@Reducer enum Path types un-nested from parent features to file-scope with parent-prefixed names"
    - "CombineReducers removed where no modifier is applied to the combined group"
    - "[Todo] and [Note] replaced with IdentifiedArrayOf<Todo> and IdentifiedArrayOf<Note>"
    - "state.path.popLast() replaced with state.path.remove(id:) using stack element ID"
    - "Boolean sheet state replaced with @Presents optional state"
    - "Manual destination = nil replaced with @Dependency(\\.dismiss) pattern"
    - "Default UUID()/Date() removed from model inits; callers inject via @Dependency"
    - "Action names follow PFW conventions (viewAppeared, addButtonTapped)"
    - "121+ tests still pass after all changes"
  artifacts:
    - path: "examples/fuse-app/Sources/FuseApp/ContactsFeature.swift"
      provides: "Un-nested Path, dismiss pattern, remove(id:), @CasePathable"
    - path: "examples/fuse-app/Sources/FuseApp/SharedModels.swift"
      provides: "IdentifiedArrayOf<Todo>, no default UUID()/Date()"
    - path: "examples/fuse-app/Sources/FuseApp/DatabaseFeature.swift"
      provides: "IdentifiedArrayOf<Note>, @CasePathable, viewAppeared naming"
  key_links:
    - from: "examples/fuse-app/Sources/FuseApp/SharedModels.swift"
      to: "IdentifiedCollections"
      via: "import IdentifiedCollections"
      pattern: "IdentifiedArrayOf<Todo>"
    - from: "examples/fuse-app/Sources/FuseApp/ContactsFeature.swift"
      to: "SwiftNavigation"
      via: "@Dependency(\\.dismiss)"
      pattern: "await dismiss\\(\\)"
---

<objective>
Apply structural PFW alignment changes that have cascading effects. These MUST be applied in the listed order because later changes depend on earlier ones (e.g., @CasePathable must precede .is() usage, Path un-nesting must precede test reference updates).

Purpose: Bring all fuse-app source code and affected tests into full PFW canonical compliance for TCA patterns, CasePaths, navigation, and data modelling.
Output: 12 files modified with structural alignment; test count maintained at >= 121.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-pfw-skill-alignment/08-RESEARCH.md
@.planning/phases/08-pfw-skill-alignment/08-CONTEXT.md
@examples/fuse-app/Sources/FuseApp/ContactsFeature.swift
@examples/fuse-app/Sources/FuseApp/TodosFeature.swift
@examples/fuse-app/Sources/FuseApp/AppFeature.swift
@examples/fuse-app/Sources/FuseApp/SettingsFeature.swift
@examples/fuse-app/Sources/FuseApp/DatabaseFeature.swift
@examples/fuse-app/Sources/FuseApp/CounterFeature.swift
@examples/fuse-app/Sources/FuseApp/SharedModels.swift
@examples/fuse-library/Tests/NavigationTests/NavigationTests.swift
@examples/fuse-library/Tests/NavigationTests/NavigationStackTests.swift
@examples/fuse-library/Tests/TCATests/StoreReducerTests.swift
@examples/fuse-library/Tests/NavigationTests/UIPatternTests.swift
@examples/fuse-app/Tests/FuseAppIntegrationTests/FuseAppIntegrationTests.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add @CasePathable to all top-level Action enums (H11)</name>
  <files>
    examples/fuse-app/Sources/FuseApp/ContactsFeature.swift
    examples/fuse-app/Sources/FuseApp/TodosFeature.swift
    examples/fuse-app/Sources/FuseApp/AppFeature.swift
    examples/fuse-app/Sources/FuseApp/SettingsFeature.swift
    examples/fuse-app/Sources/FuseApp/DatabaseFeature.swift
    examples/fuse-app/Sources/FuseApp/CounterFeature.swift
  </files>
  <action>
Add `@CasePathable` annotation to ALL top-level `enum Action` declarations inside `@Reducer struct` types across all 6 fuse-app feature files (approximately 9 enums across 5-6 files, including nested Delegate/View sub-enums).

**Rules from research (Conflict 1 resolution):**
- `@Reducer struct` types: ADD `@CasePathable` explicitly on `enum Action` — safe and additive
- `@Reducer enum` types (Destination, Path): DO NOT add `@CasePathable` — it is auto-synthesised and would cause a duplicate conformance error
- Nested sub-enums (`Delegate`, `Alert`, `ConfirmationDialog`, `View`): ALWAYS add explicit `@CasePathable`

Additionally, grep for `@CasePathable private enum` or any `private enum` used with CasePaths (e.g., `CancelID`) and change access from `private` to `fileprivate`. Per P3 in the research: `@CasePathable` generates a nested `AllCasePaths` struct, and `private` prevents access from the generated code. Pattern: `@CasePathable fileprivate enum CancelID { case timer }`.

Search: `grep -rn "private enum.*CancelID\|@CasePathable private" examples/fuse-app/`

Consult `/pfw-case-paths` and `/pfw-composable-architecture` skills for canonical patterns.

Build after each file to catch any duplicate-conformance errors immediately.
  </action>
  <verify>
`cd examples/fuse-app && swift build` — compiles cleanly with zero errors.
  </verify>
  <done>All Action enums in @Reducer struct types have explicit @CasePathable. No duplicates on @Reducer enum types.</done>
</task>

<task type="auto">
  <name>Task 2: Replace if case let with .is() / [case:] subscript (M3)</name>
  <files>
    examples/fuse-library/Tests/TCATests/StoreReducerTests.swift
    examples/fuse-library/Tests/TCATests/ObservableStateTests.swift
    examples/fuse-library/Tests/NavigationTests/NavigationTests.swift
    examples/fuse-library/Tests/NavigationTests/NavigationStackTests.swift
    examples/fuse-app/Tests/FuseAppIntegrationTests/FuseAppIntegrationTests.swift
  </files>
  <action>
Replace ALL `if case let` / `if case` pattern matching with CasePaths idioms per pfw-case-paths canonical pattern:

- **Case checking:** `if case .detail = state.destination` -> `#expect(state.destination.is(\.detail))`
- **Value extraction:** `if case let .detail(value) = state.destination` -> `let value = state.destination[case: \.detail]`
- **In-place mutation:** `state.modify(\.detail) { $0.title = "Updated" }`

Search across approximately 10 callsites in 4-5 test files. The `@CasePathable` annotations from Task 1 must be in place first for key-path syntax to work.

**DO NOT change:** Pattern matching in `switch` statements within reducer bodies — those are standard Swift and not covered by this finding.
  </action>
  <verify>
`cd examples/fuse-library && swift test --filter StoreReducerTests` — passes.
`cd examples/fuse-library && swift test --filter ObservableStateTests` — passes.
`cd examples/fuse-library && swift test --filter NavigationTests` — passes.
  </verify>
  <done>All if case let patterns in test files replaced with .is() / [case:] CasePaths idioms.</done>
</task>

<task type="auto">
  <name>Task 3: Un-nest @Reducer enum Path types (H1)</name>
  <files>
    examples/fuse-app/Sources/FuseApp/ContactsFeature.swift
    examples/fuse-library/Tests/NavigationTests/NavigationTests.swift
    examples/fuse-library/Tests/NavigationTests/NavigationStackTests.swift
  </files>
  <action>
Un-nest all `@Reducer enum Path` types from parent features to file-scope, prefixed with the parent name, per pfw-composable-architecture canonical pattern:

1. **ContactsFeature.swift:**
   - Move `@Reducer enum Path` OUT of `ContactsFeature` struct
   - Rename to `@Reducer enum ContactsFeaturePath` at file scope
   - Update all references: `Path.State` -> `ContactsFeaturePath.State`, `Path.Action` -> `ContactsFeaturePath.Action`
   - Update `StackState<Path.State>` -> `StackState<ContactsFeaturePath.State>`
   - Update `.forEach(\.path, action: \.path)` body reference
   - Update Equatable extension: `extension ContactsFeaturePath.State: Equatable {}`

2. **NavigationTests.swift:**
   - If `StackFeature` has nested `@Reducer enum Path`, move to `@Reducer enum StackFeaturePath` at file scope
   - Update all test references

3. **NavigationStackTests.swift:**
   - If nested Path exists, move to file-scope with appropriate prefix
   - Update all test references

**CRITICAL:** Each file must be updated atomically — all references within the file must be changed in the same commit. Build-verify after each file.
  </action>
  <verify>
`cd examples/fuse-app && swift build` — compiles cleanly.
`cd examples/fuse-library && swift test --filter NavigationTests` — passes.
`cd examples/fuse-library && swift test --filter NavigationStackTests` — passes.
  </verify>
  <done>All @Reducer enum Path types are at file scope with parent-prefixed names.</done>
</task>

<task type="auto">
  <name>Task 4: Remove unnecessary CombineReducers (H2)</name>
  <files>
    examples/fuse-library/Tests/TCATests/StoreReducerTests.swift
  </files>
  <action>
Remove `CombineReducers` wrapper where no modifier (`.ifLet`, `.forEach`, etc.) is applied to the combined group, per pfw-composable-architecture canonical pattern.

In `StoreReducerTests.swift`, find any `CombineReducers { Reduce { ... } Reduce { ... } }` patterns where the CombineReducers has no trailing modifier. Merge the `Reduce` closures into a single `Reduce` or inline them directly.

**DO NOT remove** `CombineReducers` where a modifier IS applied (e.g., `.ifLet(\.$child, ...)` on the combined group).
  </action>
  <verify>
`cd examples/fuse-library && swift test --filter StoreReducerTests` — all tests pass.
  </verify>
  <done>CombineReducers only used where a modifier is applied to the group.</done>
</task>

<task type="auto">
  <name>Task 5: Switch [Todo] and [Note] to IdentifiedArrayOf (H12 + P17)</name>
  <files>
    examples/fuse-app/Sources/FuseApp/SharedModels.swift
    examples/fuse-app/Sources/FuseApp/SettingsFeature.swift
    examples/fuse-app/Sources/FuseApp/DatabaseFeature.swift
    examples/fuse-app/Sources/FuseApp/TodosFeature.swift
  </files>
  <action>
**H12 — SharedModels.swift + SettingsFeature.swift:**
- Change `[Todo]` to `IdentifiedArrayOf<Todo>` in `@Shared(.fileStorage)` declarations and any State properties
- Add `import IdentifiedCollections` where needed
- `IdentifiedArrayOf` has the same JSON wire format as `[Todo]` so no Codable migration is needed
- Update SettingsFeature.swift if it references the same `[Todo]` type

**P17 — DatabaseFeature.swift:**
- Change `[Note]` to `IdentifiedArrayOf<Note>` in `DatabaseFeature.State`
- Update action payloads that pass `[Note]` to use `IdentifiedArrayOf<Note>`
- Add `import IdentifiedCollections` if needed

**TodosFeature.swift:**
- Update any local state or action payloads referencing `[Todo]`

Update ALL call sites that construct, append to, or iterate over these collections. `IdentifiedArrayOf` supports array literal initialization and `forEach`, so most code remains unchanged.
  </action>
  <verify>
`cd examples/fuse-app && swift build` — compiles cleanly.
`cd examples/fuse-app && swift test` — all tests pass.
  </verify>
  <done>[Todo] and [Note] arrays replaced with IdentifiedArrayOf for O(1) ID access and PFW compliance.</done>
</task>

<task type="auto">
  <name>Task 6: Replace popLast with remove(id:) (H13)</name>
  <files>
    examples/fuse-app/Sources/FuseApp/ContactsFeature.swift
  </files>
  <action>
Replace `state.path.popLast()` with `state.path.remove(id: stackID)` in ContactsFeature.swift, per pfw-composable-architecture canonical dismiss pattern.

The parent reducer must capture the stack element ID from the `.path(.element(let stackID, ...))` pattern and use it for targeted removal instead of blind last-element pop.

**Before:**
```swift
case .path(.element(_, .detail(.delegate(.deleteContact(let id))))):
  state.contacts.remove(id: id)
  _ = state.path.popLast()
  return .none
```

**After:**
```swift
case .path(.element(let stackID, .detail(.delegate(.deleteContact(let contactID))))):
  state.contacts.remove(id: contactID)
  state.path.remove(id: stackID)
  return .none
```
  </action>
  <verify>
`cd examples/fuse-app && swift build` — compiles cleanly.
  </verify>
  <done>Navigation pop uses targeted remove(id:) instead of blind popLast().</done>
</task>

<task type="auto">
  <name>Task 7: Replace boolean sheet state with @Presents optional (M11)</name>
  <files>
    examples/fuse-library/Tests/NavigationTests/UIPatternTests.swift
  </files>
  <action>
In `UIPatternTests.swift`, find the `SheetToggleFeature` (or similar) that uses a boolean `showSheet` flag to drive `.sheet(isPresented:)`. Replace with the PFW canonical `@Presents var sheet: SheetContent.State?` pattern per pfw-swift-navigation.

- Replace `var showSheet = false` with `@Presents var sheet: SheetContent.State?`
- Update the reducer logic accordingly
- Update tests that toggle the boolean to instead set/nil the optional state

If `SheetToggleFeature` is a test-only feature, the change stays within UIPatternTests.swift.
  </action>
  <verify>
`cd examples/fuse-library && swift test --filter UIPatternTests` — all tests pass.
  </verify>
  <done>Boolean sheet state replaced with @Presents optional per PFW canonical pattern.</done>
</task>

<task type="auto">
  <name>Task 8: Replace manual destination = nil with dismiss pattern (M12)</name>
  <files>
    examples/fuse-app/Sources/FuseApp/ContactsFeature.swift
  </files>
  <action>
Replace any `state.destination = nil` assignments in `ContactsFeature.swift` with the PFW canonical dismiss pattern, per pfw-composable-architecture and pfw-swift-navigation.

**For child self-dismissal:**
- Add `@Dependency(\.dismiss) var dismiss` to the child feature
- Replace direct nil assignment with `return .run { _ in await dismiss() }`

**For parent-driven dismissal:**
- Use `.send(.destination(.dismiss))` instead of `state.destination = nil`
- This ensures PresentationReducer's effect cancellation pipeline runs

**NEVER set `destination = nil` directly** — it bypasses PresentationReducer effect cancellation.

Consult `/pfw-composable-architecture` and `/pfw-swift-navigation` skills for the exact pattern.
  </action>
  <verify>
`cd examples/fuse-app && swift build` — compiles cleanly.
`cd examples/fuse-app && swift test` — all tests pass.
  </verify>
  <done>Manual destination = nil replaced with @Dependency(\.dismiss) or .destination(.dismiss) pattern.</done>
</task>

<task type="auto">
  <name>Task 9: Remove default UUID()/Date() from models (M6)</name>
  <files>
    examples/fuse-app/Sources/FuseApp/SharedModels.swift
    examples/fuse-app/Sources/FuseApp/ContactsFeature.swift
    examples/fuse-app/Sources/FuseApp/TodosFeature.swift
    examples/fuse-app/Sources/FuseApp/DatabaseFeature.swift
    examples/fuse-app/Tests/FuseAppIntegrationTests/FuseAppIntegrationTests.swift
  </files>
  <action>
Remove default `UUID()` and `Date()` / `.now` parameter values from `Todo` and `Contact` model initializers, per pfw-dependencies canonical pattern.

**Before:**
```swift
struct Todo {
  var id: UUID = UUID()
  var createdAt: Date = .now
}
```

**After:**
```swift
struct Todo {
  var id: UUID
  var createdAt: Date
}
```

Then update ALL call sites:
- In reducers: Use `@Dependency(\.uuid)` and `@Dependency(\.date)` to inject values
  ```swift
  @Dependency(\.uuid) var uuid
  @Dependency(\.date) var date
  // ...
  let newTodo = Todo(id: uuid(), createdAt: date.now)
  ```
- In tests: Override via `withDependencies { $0.uuid = .incrementing; $0.date = .constant(.distantPast) }`
- In integration tests: Update all `Todo(...)` and `Contact(...)` constructors to supply explicit values

This is a cascading change — every call site must be updated in the same commit.
  </action>
  <verify>
`cd examples/fuse-app && swift build` — compiles cleanly.
`cd examples/fuse-app && swift test` — all tests pass.
  </verify>
  <done>Models require explicit UUID/Date injection; no uncontrolled randomness in model defaults.</done>
</task>

<task type="auto">
  <name>Task 10: Rename action cases to PFW naming conventions (LOW)</name>
  <files>
    examples/fuse-app/Sources/FuseApp/DatabaseFeature.swift
    examples/fuse-app/Sources/FuseApp/ContactsFeature.swift
    examples/fuse-app/Sources/FuseApp/TodosFeature.swift
    examples/fuse-app/Tests/FuseAppIntegrationTests/FuseAppIntegrationTests.swift
  </files>
  <action>
Rename action cases to follow PFW naming conventions per pfw-composable-architecture:

1. `onAppear` -> `viewAppeared` (lifecycle actions named after what happened, not the modifier)
2. `addNoteTapped` -> `addButtonTapped` (always `<noun>ButtonTapped`, not `<contextNoun>Tapped`)
3. Any other non-canonical action names found during inspection

Update ALL references in:
- Reducer switch cases
- View body `store.send(...)` calls
- Test assertions (`store.send(...)`, `store.receive(...)`)

Build and test after each rename to catch any missed references.
  </action>
  <verify>
`cd examples/fuse-app && swift build` — compiles cleanly.
`cd examples/fuse-app && swift test` — all tests pass.
`make test` — full suite passes.
  </verify>
  <done>All action cases follow PFW naming: viewAppeared for lifecycle, <noun>ButtonTapped for user actions.</done>
</task>

<task type="auto">
  <name>Task 11: Final verification — full test suite</name>
  <files></files>
  <action>
Run the full test suite to confirm zero regressions after all structural changes.
  </action>
  <verify>
`make test` — all tests pass. Count >= 121.
`make test EXAMPLE=fuse-app` — all fuse-app tests pass.
  </verify>
  <done>Wave 2 complete. All structural alignment applied with zero regressions.</done>
</task>

</tasks>

<verification>
1. `make test` — full suite passes with count >= 121
2. `make test EXAMPLE=fuse-app` — fuse-app tests pass
3. All Action enums in @Reducer struct types have @CasePathable
4. No `if case let` remains in test assertions (replaced with .is() / [case:])
5. No nested `@Reducer enum Path` inside parent features
6. No `CombineReducers` without a trailing modifier
7. No `[Todo]` or `[Note]` arrays (all IdentifiedArrayOf)
8. No `popLast()` — only `remove(id:)`
9. No boolean sheet state — only `@Presents` optional
10. No `destination = nil` — only dismiss patterns
11. No default `UUID()` or `Date()` in model inits
12. Action names follow PFW conventions
</verification>

<success_criteria>
- H11: @CasePathable on all Action enums
- M3: CasePaths idioms replace if case let
- H1: Path types un-nested to file scope
- H2: CombineReducers removed where unnecessary
- H12 + P17: IdentifiedArrayOf for all Identifiable collections
- H13: remove(id:) replaces popLast()
- M11: @Presents replaces boolean sheet state
- M12: dismiss pattern replaces destination = nil
- M6: No default UUID()/Date() in models
- LOW: PFW naming conventions applied
- Test count >= 121 maintained
</success_criteria>

<output>
After completion, create `.planning/phases/08-pfw-skill-alignment/08-02-SUMMARY.md`
</output>
