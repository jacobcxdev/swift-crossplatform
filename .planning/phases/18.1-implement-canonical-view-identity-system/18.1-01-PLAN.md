---
phase: 18.1-implement-canonical-view-identity-system
plan: 01
type: execute
wave: 1a
depends_on: []
files_modified:
  - examples/fuse-app/Sources/FuseApp/IdentityFeature.swift
autonomous: true
requirements:
  - VIEWID-03
  - VIEWID-04
  - VIEWID-05
  - VIEWID-06
  - VIEWID-07
  - VIEWID-08
  - VIEWID-09
  - VIEWID-10
  - VIEWID-11

must_haves:
  truths:
    - "IdentityFeature reducer has state, actions, and reducer body for all 8 identity tab sections"
    - "All /pfw skill alignment is completed — TCA patterns, navigation, @Shared usage audited"
    - "The IdentityFeature reducer compiles on iOS and Android"
    - "Section structure is defined but UI views are NOT implemented yet (that's Plans 02+03)"
  artifacts:
    - path: "examples/fuse-app/Sources/FuseApp/IdentityFeature.swift"
      provides: "IdentityFeature reducer with state/actions/body for all 8 sections; UI stubs only"
      contains: "IdentityFeature"
  key_links:
    - from: "IdentityFeature.State"
      to: "IdentityFeature.Action"
      via: "Reducer body maps actions to state mutations for all 8 sections"
      pattern: "IdentityFeature"
---

<objective>
RED PHASE (Wave 1a): Create the IdentityFeature TCA reducer scaffolding — state, actions, and reducer body for all 8 identity tab sections. Perform ALL /pfw skill alignment work here.

This plan establishes the architectural foundation. It defines the complete reducer but does NOT implement the full UI views — those come in Plans 02 and 03. The reducer body is the single source of truth for what state exists and how actions mutate it.

Purpose: Concentrate all TCA architectural decisions and /pfw alignment in one small plan. Subsequent plans (02, 03) implement UI sections against this stable reducer without needing to re-audit TCA patterns.

Output: IdentityFeature.swift with complete reducer (state, actions, body). Minimal placeholder view body (just enough to compile). No tests yet (Plan 02/03).
</objective>

<execution_context>
@/Users/jacob/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jacob/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/18.1-implement-canonical-view-identity-system/18.1-CONTEXT.md

Source files:
@examples/fuse-app/Sources/FuseApp/IdentityFeature.swift
@examples/fuse-app/Tests/ (read test directory structure)
@docs/identity-proposals/00-final-recommendation.md

NOTE: This is Wave 1a (RED phase). No implementation summaries exist yet — Plans 04-07 have not been executed.
</context>

<interfaces>
<!-- Existing Identity tab structure (to be extended) -->

From IdentityFeature.swift (current):
- `IdentityFeature` — TCA Reducer with @ObservableState
  - State: `cards: IdentifiedArrayOf<CardItem>`, `nextCardLetter: Character`
  - Actions: `addCardButtonTapped`, `deleteCardButtonTapped(CardItem.ID)`
- `CardItem` — struct with `id: UUID`, `title: String`
- `CounterCard` — View with `title: String`, local `Store(initialState:)`, `instanceID = UUID()`
  - This is a mixed view: @State-like (Store) + let-with-default (instanceID) — exercises Plan 05's transpiler fix
- `IdentityView` — ScrollView with VStack of ForEach cards + add/delete buttons
  - Currently exercises: ForEach identity preservation on deletion (eager VStack keying)
  - Currently missing: all other facets

From TCA testing patterns (consult /pfw-composable-architecture):
- `TestStore(initialState:reducer:)` for state mutation verification
- `store.send(.view(.action))` for @ViewAction reducers
- `store.assert` for state assertions
- `@Dependency(\.uuid)` for deterministic UUID generation in tests
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Define complete IdentityFeature reducer with state, actions, and body for all 8 sections</name>
  <files>examples/fuse-app/Sources/FuseApp/IdentityFeature.swift</files>
  <action>
**IMPORTANT: Consult `/pfw-composable-architecture` before writing ANY TCA code.** Verify patterns for: TestStore usage, @ViewAction, IdentifiedArray mutations, Binding in TCA, @Shared reactivity.

Refactor `IdentityFeature.swift` to define the COMPLETE reducer for all 8 identity tab sections. This plan focuses on the reducer architecture — NOT the full UI implementation.

**State additions to IdentityFeature.State:**
```swift
// Section 1: Eager Container Keying (existing cards + reorder)
var cards: IdentifiedArrayOf<CardItem> = [...]
var nextCardLetter: Character = "C"

// Section 4: Picker Selection
var selectedStyle: String = "bold"

// Section 5: TabView Selection
var selectedTab: Int = 0

// Section 8: .id() State Reset
var resetToken: UUID = UUID()
```

**Action additions to IdentityFeature.Action.View:**
```swift
case reorderCardButtonTapped
case toggleAnimatedDeletion
case styleSelected(String)
case tabSelected(Int)
case resetTokenButtonTapped
```

**Reducer body:** Implement all action handlers with clear, minimal state mutations:
- `addCardButtonTapped` — append new card, increment nextCardLetter
- `deleteCardButtonTapped(id)` — remove card by ID
- `reorderCardButtonTapped` — move last card to first position
- `toggleAnimatedDeletion` — (if needed, add `isAnimatedDeletion: Bool` to state)
- `styleSelected(String)` — update selectedStyle
- `tabSelected(Int)` — update selectedTab
- `resetTokenButtonTapped` — generate new UUID for resetToken

Use `@Dependency(\.uuid)` for ALL UUID generation (enables deterministic testing in Plan 02/03).

**Supporting types:**
- Keep existing `CardItem` struct
- Keep existing `CounterCard` view (it exercises transpiler peer remembering)
- Add `PeerRememberTestView` — a purpose-built view for Section 7:
  - `@State var tapCount: Int = 0`
  - `let color: Color = .blue` (let-with-default)
  - NO constructor params
  - Body: colored button that increments tapCount, displaying current count

**View body:** Create a MINIMAL placeholder view that compiles:
```swift
var body: some View {
    ScrollView {
        VStack(spacing: 24) {
            Text("Identity Tab — Sections implemented in subsequent plans")
                .font(.headline)
            // Placeholder: full UI comes in Plans 02 + 03
        }
    }
}
```

The placeholder ensures the app compiles and the Identity tab is navigable, even though it doesn't show the full UI yet.

**What NOT to do in this plan:**
- Do NOT implement the full sectioned UI (that's Plans 02 + 03)
- Do NOT write tests (that's Plans 02 + 03)
- Do NOT implement the non-TCA Section 2 (duplicate keys with raw array) — that's Plan 02
  </action>
  <verify>
    <automated>cd /Users/jacob/Developer/src/github/jacobcxdev/swift-crossplatform && just ios-build fuse-app 2>&1 | tail -10</automated>
    <automated>cd /Users/jacob/Developer/src/github/jacobcxdev/swift-crossplatform && just android-build fuse-app 2>&1 | tail -10</automated>
  </verify>
  <done>
IdentityFeature.swift has complete reducer scaffolding: State with all 8 sections' properties, Action with all view actions, reducer body with all action handlers. PeerRememberTestView defined. Placeholder view body compiles. /pfw alignment completed. iOS and Android builds succeed.
  </done>
</task>

</tasks>

<verification>
**Build verification:**
1. `just ios-build fuse-app` — succeeds
2. `just android-build fuse-app` — succeeds

**Structural verification:**
3. IdentityFeature.State contains: cards, nextCardLetter, selectedStyle, selectedTab, resetToken
4. IdentityFeature.Action.View contains: addCardButtonTapped, deleteCardButtonTapped, reorderCardButtonTapped, toggleAnimatedDeletion, styleSelected, tabSelected, resetTokenButtonTapped
5. Reducer body handles all actions with correct state mutations
6. PeerRememberTestView exists with @State + let-with-default pattern
7. @Dependency(\.uuid) used for all UUID generation

**/pfw alignment verification:**
8. No use of deprecated ViewStore or WithViewStore
9. No Store created inside view body
10. @ViewAction pattern used correctly
11. IdentifiedArray used (not raw Array) for cards
</verification>

<success_criteria>
- IdentityFeature reducer has complete state, actions, and body for all 8 sections
- PeerRememberTestView defined with @State + let-with-default (no constructor params)
- @Dependency(\.uuid) used for all UUID generation
- /pfw skill alignment completed — all TCA patterns audited
- iOS and Android builds succeed
- Placeholder view body compiles and is navigable
</success_criteria>

<pfw_alignment>
**Point-Free skill alignment (MANDATORY — this plan defines ALL TCA architecture):** Before writing ANY code, the executor MUST consult:
- `/pfw-composable-architecture` — for ALL patterns: @ViewAction, Reducer, IdentifiedArray, @Dependency, Binding in TCA, Store scoping
- `/pfw-sharing` — if any scenario uses @Shared
- `/pfw-swift-navigation` — if any scenario involves NavigationStack
- `/pfw-case-paths` — for @CasePathable enum patterns in Action

**Specific anti-patterns to watch for (from /pfw skills):**
- Do NOT use `ViewStore` — deprecated in favor of direct `store` observation
- Do NOT use `WithViewStore` — same
- Do NOT create `Store` inside a view's `body` — use `let store` property or `@Bindable var store`
- Use `store.send(.view(.action))` pattern with @ViewAction
</pfw_alignment>

<output>
After completion, create `.planning/phases/18.1-implement-canonical-view-identity-system/18.1-01-SUMMARY.md`

<checkpoint>
**HUMAN INTERVENTION REQUIRED — Wave 1a (Plan 01 — TCA scaffolding) complete.**

Before proceeding to Wave 1b (Plan 02 — Sections 1-4 UI + tests):
1. **Run `/clear`** to compact context.
2. **Confirm iOS build succeeds** — `just ios-build fuse-app`
3. **Review IdentityFeature.swift** — verify reducer state/actions/body look correct.

Once confirmed, proceed to Wave 1b: Plan 02 (Identity tab sections 1-4 + tests).
</checkpoint>
</output>
</content>
</invoke></output>
