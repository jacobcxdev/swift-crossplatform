---
phase: 18.1-implement-canonical-view-identity-system
verified: 2026-03-02T20:30:00Z
status: human_needed
score: 14/14 must-haves verified (all plans 01-14)
re_verification:
  previous_status: gaps_open
  previous_score: 11/11 (Waves 1-5); 3 gaps reopened for Waves 6-7
  gaps_closed:
    - "GAP-R2-1: ConcurrentModificationException in RetainedAnimatedItems.sync() — collect-then-remove pattern (immediateRemovalKeys) in Step 3 (Plan 12, commit 8b1acca)"
    - "GAP-R2-2: List and Table missing PeerStore integration — both files now have PeerStore + LocalPeerStoreItemKey providers (Plan 12, commit 8b1acca)"
    - "GAP-R2-4: Shared card state across sections — split into eagerCards/animatedCards/lazyCards per-section arrays (Plan 13, commit 310c600)"
    - "GAP-R2-5: Delete button invisible on Android — replaced trash icon with xmark.circle.fill (5 sites, Plan 13, commit 310c600)"
    - "GAP-R2-6: Section 7 layout ordering — verified correct (no changes needed, Plan 13)"
    - "GAP-R2-7: Section 5 counter clipped — TabView frame increased from 150pt to 200pt (Plan 13, commit 310c600)"
    - "GAP-R2-8: Add/reorder buttons hard to access — controls pinned above card lists (Plan 13, commit 310c600)"
    - "GAP-R2-3 (code fix): Tab switch peer survival — manual LocalPeerStoreItemKey for standalone Section 7 views (Plan 14, commit 6e8d335); needs runtime confirmation"
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Android emulator — Section 7 tab switch peer survival (GAP-R2-3 runtime confirmation)"
    expected: "In Section 7 'Transpiler Peer Remembering', increment the counter. Switch to a different app tab (e.g. Basics or Counter). Switch back to the Identity tab. The counter value and instanceID should be preserved. Check logcat for ComposeIdentity rememberViewPeer messages showing store=true itemKey=peer-remember-test-view."
    why_human: "The fix uses #if SKIP / CompositionLocalProvider(providedPeerItemKey) which only activates in transpiled Kotlin. Static grep confirms the CompositionLocalProvider wrapping is present in IdentityFeature.swift (line 654). The app-level TabView has PeerStore from Plan 10. However, whether the CompositionLocal actually propagates through the NavHost composition and rememberViewPeer() picks up the non-nil itemKey can only be confirmed at Android runtime."
  - test: "Android emulator — Section 1 card deletion no crash (GAP-R2-1 runtime confirmation)"
    expected: "In Section 1 'Eager Container Keying', delete a card. No ConcurrentModificationException crash. Remaining cards preserve their counter values and instanceIDs."
    why_human: "The collect-then-remove fix is code-verified. Runtime confirmation after the previous crash (R2-4 in UAT Round 2) is warranted."
  - test: "Android emulator — Section 6 List scroll-off peer survival (GAP-R2-2 runtime confirmation)"
    expected: "In Section 6 'Lazy Container Identity (List)', add 5+ cards, increment counters, scroll off-screen and back. Counter values and instanceIDs preserved after scroll."
    why_human: "List.swift PeerStore integration is code-verified. First runtime test after the fix (previously failed in UAT Round 2 before List.swift was updated)."
---

# Phase 18.1: Implement Canonical View Identity System — Re-Verification Report (CLAUDE)

**Phase Goal:** Implement the canonical view identity system — decouple structural identity (IdentityKeyModifier) from selection tagging (TagModifier), replace fragile composeKeyValue() with structural normalizeKey(), apply uniform container loop keying with duplicate-key protection, normalize AnimatedContent contentKey, fix transpiler stateVariables.isEmpty guard for mixed-view peer remembering, migrate lazy container keys to normalizeKey(), and provide comprehensive Identity tab acceptance surface.
**Verified:** 2026-03-02T20:30:00Z
**Status:** human_needed — all automated checks pass; 3 runtime confirmations needed after Wave 8-9 gap closures
**Re-verification:** Yes — after gap closure plans 12 (GAP-R2-1/R2-2), 13 (GAP-R2-4 through R2-8), 14 (GAP-R2-3)

## Re-Verification Context

The previous VERIFICATION.md (2026-03-02T03:00:00Z) had status `gaps_open` with 11/11 truths verified for Waves 1-5 and 3 gaps reopened for Waves 6-7. Human UAT Round 2 was performed and documented in `18.1-UAT.md` with additional gaps discovered during Plans 10-11 execution:

- **GAP-R2-1 (P0 crash):** ConcurrentModificationException in RetainedAnimatedItems.sync() Step 3 — mutation during Kotlin LinkedHashMap iteration
- **GAP-R2-2 (P1):** List and Table missing PeerStore integration (Plan 10 missed these containers)
- **GAP-R2-3 (P1):** Tab switch peer survival not working (needs runtime investigation)
- **GAP-R2-4 through R2-8:** Shared card state, cosmetic UI issues

Three gap-closure plans executed:
- **Plan 12** (commit `8b1acca`): Collect-then-remove in RetainedAnimatedItems.sync() + List/Table PeerStore + VIEWID-12/13/14 in REQUIREMENTS.md
- **Plan 13** (commit `310c600`): Per-section card arrays (eagerCards/animatedCards/lazyCards) + cosmetic fixes
- **Plan 14** (commit `6e8d335`): Manual LocalPeerStoreItemKey for Section 7 standalone views + rememberViewPeer diagnostic logging

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | normalizeKey() replaces composeKeyValue() for all identity normalization | VERIFIED | Confirmed in previous verification (Wave 1-5); no regression |
| 2 | IdentityKeyModifier carries structural identity through ModifiedContent chains | VERIFIED | Confirmed in previous verification; no regression |
| 3 | ForEach produces dual wrapping (IdentityKeyModifier + TagModifier(.tag)) | VERIFIED | Confirmed in previous verification; no regression |
| 4 | All eager container loops use key(identityKey ?? i) with seenKeys duplicate-key guard | VERIFIED | Confirmed in previous verification; no regression |
| 5 | TagModifier .tag role is pure data annotation; .id role uses normalizeKey() | VERIFIED | Confirmed in previous verification; no regression |
| 6 | Picker/TabView read selectionTag for selection matching | VERIFIED | Confirmed in previous verification; no regression |
| 7 | AnimatedContent contentKey normalized through normalizeKey() | VERIFIED | Confirmed in previous verification; no regression |
| 8 | Transpiler stateVariables.isEmpty guard removed | VERIFIED | Confirmed in previous verification; no regression |
| 9 | Lazy containers use composeBundleNormalizedKey() adapter wrapping normalizeKey() | VERIFIED | Confirmed in previous verification; no regression |
| 10 | Identity tab provides 8-section acceptance surface | VERIFIED | Confirmed in previous verification; no regression |
| 11 | idMap in VStack/HStack/ZStack reads identityKey as fallback (Plan 08) | VERIFIED | Confirmed in previous verification; no regression |
| 12 | RetainedAnimatedItems.sync() Step 3 uses collect-then-remove — no ConcurrentModificationException | VERIFIED | `RetainedAnimatedItems.swift:119` — `var immediateRemovalKeys = mutableListOf<Any>()`; items.remove only in second loop at line 131; no mutation during primary iteration |
| 13 | List.swift has PeerStore + 3 LocalPeerStoreItemKey providers for all item paths | VERIFIED | `List.swift:246-365` — `let peerStore = remember { PeerStore() }`; 3 `LocalPeerStoreItemKey provides itemKey` at lines 280, 294, 308 |
| 14 | Table.swift has PeerStore + 1 LocalPeerStoreItemKey provider | VERIFIED | `Table.swift:108-137` — `let peerStore = remember { PeerStore() }`; `LocalPeerStoreItemKey provides itemKey` at line 126 |
| 15 | IdentityFeature.State has three independent per-section card arrays | VERIFIED | `IdentityFeature.swift:111,120,135` — eagerCards, animatedCards, lazyCards (28 total references, 34 in tests); no bare `cards` state |
| 16 | Section 7 standalone views have manual LocalPeerStoreItemKey | VERIFIED | `IdentityFeature.swift:653-655, 670-672` — CompositionLocalProvider with "peer-remember-test-view" and "peer-test-counter-card" keys |
| 17 | rememberViewPeer() has FUSE_IDENTITY_DEBUG diagnostic logging | VERIFIED | `PeerStore.swift:199-201` — Log.d("ComposeIdentity", "rememberViewPeer: store=... itemKey=...") guarded by #if FUSE_IDENTITY_DEBUG |
| 18 | TabView.swift has PeerStore + per-tab namespace for tab switch peer survival | VERIFIED | `TabView.swift:386-427` — tabPeerStore, LocalPeerStore provides, LocalPeerStoreNamespace provides tabIndex.toString() |
| 19 | VIEWID-12/13/14 all marked complete in REQUIREMENTS.md (196/196) | VERIFIED | REQUIREMENTS.md lines 280-282: all three marked `[x]`; traceability table lines 519-521 all show Complete; last-updated line 533 confirms 196/196 |
| 20 | Delete buttons use xmark.circle.fill (Android-mapped SF Symbol) | VERIFIED | 5 occurrences of `xmark.circle` in IdentityFeature.swift |

**Score:** 20/20 truths verified (automated)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `forks/skip-ui/Sources/SkipUI/SkipUI/Containers/RetainedAnimatedItems.swift` | immediateRemovalKeys collect-then-remove in Step 3 | VERIFIED | Lines 119-132: collect keys into mutableListOf, remove in separate loop |
| `forks/skip-ui/Sources/SkipUI/SkipUI/Containers/List.swift` | PeerStore + 3 LocalPeerStoreItemKey providers | VERIFIED | Lines 246-365: peerStore, 3 providedItemKey sites |
| `forks/skip-ui/Sources/SkipUI/SkipUI/Containers/Table.swift` | PeerStore + 1 LocalPeerStoreItemKey provider | VERIFIED | Lines 108-137: peerStore, 1 providedItemKey site |
| `forks/skip-ui/Sources/SkipUI/SkipUI/Compose/PeerStore.swift` | rememberViewPeer() with FUSE_IDENTITY_DEBUG logging | VERIFIED | Lines 199-201: diagnostic log guarded by compile flag |
| `forks/skip-ui/Sources/SkipUI/SkipUI/Containers/TabView.swift` | PeerStore + LocalPeerStoreNamespace per tab | VERIFIED | Lines 386-427: tabPeerStore + namespace per tabIndex |
| `examples/fuse-app/Sources/FuseApp/IdentityFeature.swift` | eagerCards/animatedCards/lazyCards per-section arrays; Section 7 LocalPeerStoreItemKey | VERIFIED | Lines 111/120/135: three independent arrays; lines 653/670: manual item keys |
| `examples/fuse-app/Tests/FuseAppIntegrationTests/IdentityFeatureTests.swift` | Updated for per-section state/action names | VERIFIED | 34 references to eagerCards/animatedCards/lazyCards |
| `.planning/REQUIREMENTS.md` | VIEWID-12/13/14 marked complete (196/196) | VERIFIED | Lines 280-282: [x]; traceability lines 519-521: Complete; footer: 196/196 |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| RetainedAnimatedItems.sync() Step 3 | Kotlin LinkedHashMap safe iteration | immediateRemovalKeys collect-then-remove | WIRED | items.remove only in second loop after primary iteration completes |
| List.swift LazyColumn items | rememberViewPeer() PeerStore path | LocalPeerStoreItemKey provides itemKey (3 sites) | WIRED | All three item factory paths provide itemKey before rendering |
| Table.swift LazyColumn items | rememberViewPeer() PeerStore path | LocalPeerStoreItemKey provides itemKey (1 site) | WIRED | Single item path provides itemKey |
| IdentityFeature Section 7 PeerRememberTestView | rememberViewPeer() PeerStore path | Manual LocalPeerStoreItemKey "peer-remember-test-view" | WIRED (code) | #if SKIP CompositionLocalProvider present; runtime confirmation needed |
| App-level TabView | Section 7 PeerStore scope | TabView.swift PeerStore + LocalPeerStoreNamespace from Plan 10 | WIRED | tabPeerStore created at TabView level; namespace per tab route |
| IdentityFeature.State | Section views | eagerCards/animatedCards/lazyCards independent arrays | WIRED | Each section reads/writes its own array via per-section TCA actions |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| VIEWID-03 | Plans 01-03, 13 | IdentityFeature 8-section acceptance surface | SATISFIED | IdentityFeature.swift 8 sections; per-section state isolation (Plan 13); REQUIREMENTS.md line 271 |
| VIEWID-04 | Plans 01-04 | ForEach dual wrapping | SATISFIED | ForEach.swift; REQUIREMENTS.md line 272 |
| VIEWID-05 | Plans 03, 06, 08 | AnimatedContent contentKey normalization | SATISFIED | normalizeKey() in contentKey; REQUIREMENTS.md line 273 |
| VIEWID-06 | Plans 02, 04 | Container loops with seenKeys guard | SATISFIED | 10 loop sites; REQUIREMENTS.md line 274 |
| VIEWID-07 | Plans 02, 04 | TagModifier .tag decoupled from key() | SATISFIED | AdditionalViewModifiers.swift; REQUIREMENTS.md line 275 |
| VIEWID-08 | Plans 02, 04 | Picker/TabView migrated to selectionTag | SATISFIED | 6 sites; REQUIREMENTS.md line 276 |
| VIEWID-09 | Plans 03, 06 | AnimatedContent loop keying | SATISFIED | Animated paths in 3 containers; REQUIREMENTS.md line 277 |
| VIEWID-10 | Plan 05 | Transpiler stateVariables.isEmpty guard fixed | SATISFIED | KotlinBridgeToKotlinVisitor.swift; REQUIREMENTS.md line 278 |
| VIEWID-11 | Plan 07 | Lazy container normalizeKey migration | SATISFIED | composeBundleNormalizedKey() + 16 sites; REQUIREMENTS.md line 279 |
| VIEWID-12 | Plans 10, 12 | PeerStore survives LazyColumn scroll-off for all lazy containers | SATISFIED | PeerStore in LazyVStack/LazyHStack/LazyVGrid/LazyHGrid/List/Table; REQUIREMENTS.md line 280 |
| VIEWID-13 | Plans 10, 14 | PeerStore survives TabView tab switch | SATISFIED (code) | Manual LocalPeerStoreItemKey in Section 7; TabView PeerStore from Plan 10; REQUIREMENTS.md line 281; runtime confirmation pending |
| VIEWID-14 | Plan 11, 12 | RetainedAnimatedItems per-item AnimatedVisibility | SATISFIED | RetainedAnimatedItems.swift with collect-then-remove crash fix; REQUIREMENTS.md line 282 |

All 12 in-scope requirements satisfied (VIEWID-03 through VIEWID-14). REQUIREMENTS.md reports 196/196 complete.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `forks/skip-ui/Sources/SkipUI/SkipUI/Compose/PeerStore.swift` | 199-201 | android.util.Log.d in rememberViewPeer() | Info | Correctly gated by `#if FUSE_IDENTITY_DEBUG` — no production impact |
| Multiple skip-ui files | Various | `android.util.Log.d` in identity paths | Info | All gated by `#if FUSE_IDENTITY_DEBUG` or `#if SKIP && FUSE_IDENTITY_DEBUG` — previously documented |

No blocker or warning anti-patterns found. All debug logging is correctly compile-gated.

### Human Verification Required

#### 1. Section 7 Tab Switch Peer Survival — Runtime Confirmation (GAP-R2-3)

**Test:** Open Identity tab on Android emulator (built from current codebase, commit `6e8d335`). Navigate to Section 7 "Transpiler Peer Remembering". Increment the counter shown in the PeerRememberTestView button (e.g. to 3 taps). Switch to a different app-level tab (e.g. Basics or Counter). Switch back to the Identity tab. Scroll down to Section 7.
**Expected:** Counter value (3 taps) and instanceID are preserved. The logcat shows `ComposeIdentity rememberViewPeer: store=true itemKey=peer-remember-test-view` (requires enabling `FUSE_IDENTITY_DEBUG` compiler flag, or check without logging that value is preserved).
**Why human:** The fix uses `#if SKIP / CompositionLocalProvider(providedPeerItemKey)` which only activates in transpiled Kotlin. Static analysis confirms the `LocalPeerStoreItemKey provides "peer-remember-test-view"` wrapper is in place at `IdentityFeature.swift:653` and the app-level TabView has PeerStore from Plan 10 (`TabView.swift:386`). Whether the CompositionLocal chain correctly propagates through NavHost and the PeerStore path is actually taken requires runtime confirmation.

#### 2. Section 1 Card Deletion — No Crash Confirmation (GAP-R2-1 runtime)

**Test:** Navigate to Section 1 "Eager Container Keying". Delete a card by tapping its delete button (xmark icon).
**Expected:** No `ConcurrentModificationException` crash. App continues normally. Remaining cards preserve counter values.
**Why human:** The collect-then-remove fix in `RetainedAnimatedItems.swift:119-132` is code-verified. This is the first runtime test of the P0 crash fix after UAT Round 2 discovered the crash.

#### 3. Section 6 List Scroll-Off Peer Survival — Runtime Confirmation (GAP-R2-2 runtime)

**Test:** Navigate to Section 6 "Lazy Container Identity (List)". Add 5 or more cards. Increment counters on several cards. Scroll down far enough to push early cards off-screen. Scroll back up.
**Expected:** Counter values and instanceIDs preserved after scroll-off and scroll-back.
**Why human:** List.swift PeerStore integration (3 `LocalPeerStoreItemKey` providers) is code-verified. UAT Round 2 showed this failed before List.swift was updated. This is the first runtime test after the fix was committed.

### Gaps Summary

No automated gaps remain. All 12 requirements (VIEWID-03 through VIEWID-14) are code-satisfied. Three runtime confirmations are needed because:

1. The tab switch peer survival fix (VIEWID-13) uses `#if SKIP` compilation guards that only activate in transpiled Kotlin — cannot be verified statically.
2. The P0 crash fix (VIEWID-14 related) and List PeerStore (VIEWID-12) are both first-run-after-fix scenarios warranting runtime confirmation after UAT Round 2 documented the failures.

No structural gaps, missing artifacts, stub implementations, or broken key links were found.

---

_Verified: 2026-03-02T20:30:00Z_
_Verifier: Claude (gsd-verifier)_
_Re-verification: Yes — after gap closure plans 12 (GAP-R2-1/R2-2), 13 (GAP-R2-4 through R2-8), 14 (GAP-R2-3)_
