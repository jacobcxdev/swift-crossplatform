---
phase: 18.1-implement-canonical-view-identity-system
plan: 03
type: execute
wave: 1c
depends_on:
  - 18.1-01
  - 18.1-02
files_modified:
  - examples/fuse-app/Sources/FuseApp/IdentityFeature.swift
  - examples/fuse-app/Tests/FuseAppTests/IdentityFeatureTests.swift
autonomous: true
requirements:
  - VIEWID-05
  - VIEWID-07
  - VIEWID-08
  - VIEWID-09
  - VIEWID-10
  - VIEWID-11

must_haves:
  truths:
    - "Identity tab sections 5-8 are fully implemented with interactive UI elements and visible state labels"
    - "Section 5 (TabView Selection) covers tab selection binding with 3 tabs"
    - "Section 6 (Lazy Container Identity) covers LazyVStack/List with scroll + state retention"
    - "Section 7 (Transpiler Peer Remembering) has PeerRememberTestView exercising @State + let-with-default"
    - "Section 8 (.id() State Reset) demonstrates state destruction via .id() value change"
    - "TCA TestStore tests for sections 5, 6, 8 compile and pass (reducer state mutations)"
    - "Section 7 has NO TCA TestStore test — documented as Android integration/manual test only"
    - "The Identity tab is the single definitive acceptance surface for the entire canonical view identity system"
  artifacts:
    - path: "examples/fuse-app/Sources/FuseApp/IdentityFeature.swift"
      provides: "Complete Identity tab with all 8 sections"
      contains: "Section 8"
    - path: "examples/fuse-app/Tests/FuseAppTests/IdentityFeatureTests.swift"
      provides: "Complete TCA TestStore tests for sections 1, 3, 4, 5, 6, 8"
      contains: "IdentityFeatureTests"
  key_links:
    - from: "IdentityFeature.swift sections 5-8 UI"
      to: "IdentityFeatureTests.swift tests"
      via: "Sections 5, 6, 8 have TestStore assertions; section 7 is Android manual test only"
      pattern: "IdentityFeature"
---

<objective>
RED PHASE (Wave 1c): Implement Identity tab UI sections 5-8 and write their TCA TestStore tests. Complete the RED phase.

This plan finishes the Identity tab acceptance surface. After this plan, all 8 sections are implemented, all tests are written, and the Identity tab is the definitive acceptance surface for the view identity system.

Output: All 8 sections fully rendered. Complete test file with tests for sections 1, 3, 4, 5, 6, 8. Sections 2 and 7 documented as Android-only. RED phase complete.
</objective>

<execution_context>
@/Users/jacob/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jacob/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/18.1-implement-canonical-view-identity-system/18.1-CONTEXT.md
@.planning/phases/18.1-implement-canonical-view-identity-system/18.1-01-SUMMARY.md
@.planning/phases/18.1-implement-canonical-view-identity-system/18.1-02-SUMMARY.md

Source files:
@examples/fuse-app/Sources/FuseApp/IdentityFeature.swift
@examples/fuse-app/Tests/FuseAppTests/IdentityFeatureTests.swift

NOTE: Plans 01 (reducer scaffolding) and 02 (sections 1-4 + tests) have been completed. This plan adds sections 5-8 and their tests.
</context>

<interfaces>
From IdentityFeature.swift (after Plans 01 + 02):
- Complete reducer with state/actions/body for all 8 sections
- Sections 1-4 fully implemented with interactive UI
- PeerRememberTestView defined (from Plan 01)
- Placeholder comments for sections 5-8

From IdentityFeatureTests.swift (after Plan 02):
- Tests for sections 1, 3, 4 (card deletion, reorder, addition, animated deletion, picker selection)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Implement Identity tab UI sections 5-8</name>
  <files>examples/fuse-app/Sources/FuseApp/IdentityFeature.swift</files>
  <action>
Replace the placeholder comments for sections 5-8 with full UI implementations.

**Section 5: TabView Selection Correctness — Plan 04**

Add a mini TabView subsection (nested TabView with 3 tabs):
- Each tab shows its name and a counter
- Tab selection binding displayed as text
- **HUMAN-ONLY: Do not orchestrate the emulator via adb. Manual UI verification is performed by the human between waves. Do not waste tokens on adb commands, screenshot capture, or emulator automation.**
- **Manual test:** Tap each tab → selection binding updates correctly → tab content matches
- This verifies TabView selection works after tag decoupling

**Section 6: Lazy Container Identity — Plan 07**

Add a LazyVStack section:
- `List` or `LazyVStack` with ForEach over a large-ish dataset (20+ items)
- Each item has a counter (local @State or local Store)
- Scroll down, increment some counters, scroll back up → counters retained
- Add/delete items → scroll position stable, counters on remaining items retained
- **HUMAN-ONLY: Do not orchestrate the emulator via adb. Manual UI verification is performed by the human between waves. Do not waste tokens on adb commands, screenshot capture, or emulator automation.**
- **Manual test:** Scroll to item 15, increment its counter, scroll to top, scroll back to item 15 → counter value preserved

**Section 7: Transpiler Peer Remembering — Plan 05**

Use the `PeerRememberTestView` defined in Plan 01:
- Add a label explicitly showing "Test: @State + let-with-default (no constructor params)"
- The existing `CounterCard` is retained as a SEPARATE subsection showing the "mixed view with Store" pattern
- **HUMAN-ONLY: Do not orchestrate the emulator via adb. Manual UI verification is performed by the human between waves. Do not waste tokens on adb commands, screenshot capture, or emulator automation.**
- **Manual test (Android):** Tap the `PeerRememberTestView` button 3 times. Then trigger parent recomposition (e.g., add a new card above). Verify `tapCount` is still 3 — without peer remembering, `@State` resets to 0 every recomposition on Android.
- **NOTE:** This section is verified via Android integration test ONLY — TCA TestStore cannot test transpiler/Compose peer remembering behavior. No TestStore test is created for this section.

**Section 8: .id() State Reset — Plans 04, 06**

Add an `.id()` reset demonstration:
- A counter view with a "Reset Token" button that changes the view's `.id()` value
- When the reset token changes, the counter should reset to 0 (state destruction via `.id()`)
- Display the current reset token value
- **HUMAN-ONLY: Do not orchestrate the emulator via adb. Manual UI verification is performed by the human between waves. Do not waste tokens on adb commands, screenshot capture, or emulator automation.**
- **Manual test:** Increment counter to 5, tap "Reset Token" → counter resets to 0

**Layout consistency:** Follow the same section header/description/UI pattern established in Plan 02. Each section has header, description, interactive elements, and visible state labels.
  </action>
  <verify>
    <automated>cd /Users/jacob/Developer/src/github/jacobcxdev/swift-crossplatform && just ios-build fuse-app 2>&1 | tail -10</automated>
    <automated>cd /Users/jacob/Developer/src/github/jacobcxdev/swift-crossplatform && just android-build fuse-app 2>&1 | tail -10</automated>
  </verify>
  <done>
Sections 5-8 fully implemented. All 8 sections now render with interactive UI elements and visible state labels. iOS and Android builds succeed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add TCA TestStore tests for sections 5, 6, 8</name>
  <files>examples/fuse-app/Tests/FuseAppTests/IdentityFeatureTests.swift</files>
  <action>
Add tests for sections 5, 6, and 8 to the existing IdentityFeatureTests.swift. Section 7 has no TestStore test (Android-only).

**Test: TabView selection (Section 5)**
- Send tabSelected action, verify selectedTab state updates

**Test: Lazy container item management (Section 6)**
- Test add/delete state mutations for the lazy section's TCA-managed items (the lazy section MUST have TCA-managed items to enable TestStore testing)
- Scroll position retention and lazy counter preservation are additionally verified on Android

**Test: .id() reset token (Section 8)**
- Send resetTokenButtonTapped, verify resetToken changes (new UUID generated)

**Notes:**
- Section 7 (Peer Remembering) has NO TestStore test — `@State` retention across recomposition is transpiler/Compose behavior
- Each test should be independent (own TestStore instance)
- Follow the test patterns established in Plan 02
  </action>
  <verify>
    <automated>cd /Users/jacob/Developer/src/github/jacobcxdev/swift-crossplatform && just ios-test fuse-app 2>&1 | tail -20</automated>
  </verify>
  <done>
Tests for sections 5, 6, 8 added. Complete test file now covers sections 1, 3, 4, 5, 6, 8. Section 2 (duplicate keys) and Section 7 (peer remembering) documented as Android-only. All tests compile and pass. RED phase complete.
  </done>
</task>

</tasks>

<verification>
**Build verification:**
1. `just ios-build fuse-app` — succeeds
2. `just android-build fuse-app` — succeeds

**Test verification:**
3. `just ios-test fuse-app` — all IdentityFeatureTests compile and pass
4. `just android-test fuse-app` — no regressions

**Coverage verification (complete RED phase):**
5. Sections with TCA TestStore tests (reducer state mutations — fully automatable):
   - Section 1 (Eager Container Keying): card add, delete, reorder state mutations
   - Section 4 (Picker Selection): selectedStyle state update
   - Section 5 (TabView Selection): selectedTab state update
   - Section 8 (.id() State Reset): resetToken state change
6. Sections with TCA TestStore tests (state) + Android integration/manual test (rendering behavior):
   - Section 3 (AnimatedContent): TestStore verifies animated deletion state mutation; animation exit behavior verified on Android only
   - Section 6 (Lazy Container): TestStore verifies item add/delete state; scroll position retention and lazy counter preservation verified on Android only
7. Sections with Android integration/manual test ONLY (not TCA-testable):
   - Section 2 (Duplicate Key Guard): uses non-TCA raw array with ForEach(\.self) — duplicate-key `_dup` suffix is a Compose-level behavior
   - Section 7 (Peer Remembering): `@State` retention across recomposition is a transpiler/Compose behavior — no TCA reducer involvement

**HUMAN-ONLY: Do not orchestrate the emulator via adb. Manual UI verification is performed by the human between waves. Do not waste tokens on adb commands, screenshot capture, or emulator automation.**

**RED phase acceptance criteria (user manually confirms BEFORE proceeding to Wave 2):**
- Identity tab renders on Android emulator with ALL 8 sections
- Section 1: Delete a card → remaining cards LOSE their counter state (EXPECTED BROKEN)
- Section 2: Duplicate keys may crash or show shared state (EXPECTED BROKEN)
- Section 7: Trigger recomposition → `@State` resets (EXPECTED BROKEN)
- Section 8: `.id()` reset may not work correctly with raw SwiftHashable keys (EXPECTED BROKEN)
- Sections 3, 4, 5, 6: May or may not show issues depending on current baseline
</verification>

<success_criteria>
**RED phase complete = all tests WRITTEN + Identity tab RENDERS + behaviour visually BROKEN:**
- IdentityFeature.swift contains all 8 clearly labelled sections covering all identity system facets
- Each section has interactive UI elements AND visible state labels for manual verification
- IdentityFeatureTests.swift contains TCA TestStore tests for sections 1, 3, 4, 5, 6, 8
- Sections 2 (duplicate key) and 7 (peer remembering) explicitly marked as Android integration/manual test only
- All TCA TestStore tests compile and pass (reducer logic independent of identity infrastructure)
- iOS and Android builds succeed
- The Identity tab is the single definitive acceptance surface for the entire canonical view identity system
</success_criteria>

<pfw_alignment>
**Point-Free skill alignment:** Plans 01 and 02 completed the full /pfw audit. This plan uses the established patterns. If TabView binding or lazy container patterns require new TCA patterns, consult `/pfw-composable-architecture`.
</pfw_alignment>

<output>
After completion, create `.planning/phases/18.1-implement-canonical-view-identity-system/18.1-03-SUMMARY.md`

<checkpoint>
**HUMAN INTERVENTION REQUIRED — Wave 1c (Plan 03 — Sections 5-8) complete. RED PHASE DONE.**

Before proceeding to Wave 2 (Plans 04 + 05 — GREEN phase):
1. **Run `/clear`** to compact context — minimises token usage for Wave 2 execution.
2. **Verify Identity tab on Android emulator** — confirm that the Identity tab renders ALL 8 sections but shows INCORRECT identity behaviour:
   - Section 1: Card deletion does NOT preserve counter state on remaining cards
   - Section 7: Parent recomposition resets `@State` values (no peer remembering)
   - Section 8: `.id()` reset may not work correctly
3. **Confirm all TCA TestStore tests compile and pass** — `just ios-test fuse-app`
4. **Confirm all 8 sections are rendered** — review IdentityFeature.swift for completeness

Once confirmed, proceed to Wave 2: Plans 04 (core identity infra) + 05 (transpiler fix) in parallel.
</checkpoint>
</output>
</content>
</invoke></output>
