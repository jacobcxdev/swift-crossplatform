---
phase: 18.1-implement-canonical-view-identity-system
plan: 13
type: execute
wave: 8
depends_on:
  - 18.1-11
files_modified:
  - examples/fuse-app/Sources/FuseApp/IdentityFeature.swift
  - examples/fuse-app/Tests/FuseAppIntegrationTests/IdentityFeatureTests.swift
autonomous: true
gap_closure: true
requirements:
  - VIEWID-03

must_haves:
  truths:
    - "Each section (1/2, 3, 6) has its own independent card array in TCA state"
    - "Adding/deleting cards in one section does not affect other sections"
    - "Delete button uses a visible icon on Android (xmark.circle.fill or text fallback)"
    - "Section 7 PeerRememberTestView renders below the section header and description"
    - "Section 5 tab content has sufficient vertical space for counter display"
  artifacts:
    - path: "examples/fuse-app/Sources/FuseApp/IdentityFeature.swift"
      provides: "Per-section card arrays, cosmetic UI fixes"
      contains: "eagerCards"
  key_links:
    - from: "IdentityFeature.State per-section arrays"
      to: "Section views"
      via: "store.eagerCards / store.animatedCards / store.lazyCards"
      pattern: "eagerCards"
---

<objective>
Fix GAP-R2-4 (shared card state across sections) and GAP-R2-5/R2-6/R2-7/R2-8 (cosmetic UI issues) in the Identity tab acceptance surface.

**GAP-R2-4:** All sections share a single `cards` array in TCA state. Adding/deleting in one section affects all others, causing scroll jumps and cross-contamination. Fix: split into per-section arrays (`eagerCards` for Sections 1/2, `animatedCards` for Section 3, `lazyCards` for Section 6).

**GAP-R2-5:** Delete button trash icon invisible on Android — likely unmapped SF Symbol. Fix: use `xmark.circle.fill` (mapped to Material `Cancel` icon) or use `Label("Delete", systemImage: "xmark.circle.fill")`.

**GAP-R2-6:** Section 7 button renders above header. Fix: verify VStack ordering in IdentitySection7View.

**GAP-R2-7:** Section 5 counter clipped by mini tab bar. Fix: add padding or frame to tab content.

**GAP-R2-8:** Add/reorder buttons hard to access. Fix: move to `.toolbar {}` or pin at section top.

Output: Clean per-section isolation, visible delete buttons, correct layout ordering.
</objective>

<execution_context>
@/Users/jacob/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jacob/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/18.1-implement-canonical-view-identity-system/18.1-CONTEXT.md
@.planning/phases/18.1-implement-canonical-view-identity-system/18.1-UAT.md

Source file:
@examples/fuse-app/Sources/FuseApp/IdentityFeature.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split shared cards into per-section arrays</name>
  <files>
    examples/fuse-app/Sources/FuseApp/IdentityFeature.swift
  </files>
  <action>
**State refactoring:**

Replace the single `cards` array in `IdentityFeature.State` with three independent arrays:

```swift
var eagerCards: IdentifiedArrayOf<CardItem> = [
    CardItem(title: "Card A"),
    CardItem(title: "Card B"),
    CardItem(title: "Card C"),
]
var animatedCards: IdentifiedArrayOf<CardItem> = [
    CardItem(title: "Card A"),
    CardItem(title: "Card B"),
    CardItem(title: "Card C"),
]
var lazyCards: IdentifiedArrayOf<CardItem> = [
    CardItem(title: "Card A"),
    CardItem(title: "Card B"),
    CardItem(title: "Card C"),
]
```

**Action refactoring:**

Replace generic `addCard`, `deleteCard(id:)`, `moveCard(from:to:)` actions with per-section variants:

```swift
case addEagerCard
case deleteEagerCard(id: UUID)
case moveEagerCard(from: Int, to: Int)
case addAnimatedCard
case deleteAnimatedCard(id: UUID)
case addLazyCard
case deleteLazyCard(id: UUID)
```

**Reducer body:** Duplicate the existing add/delete/move logic for each action variant, operating on the corresponding array (`eagerCards`, `animatedCards`, `lazyCards`).

**View updates:**
- `IdentitySection1View` and `IdentitySection2View`: read `store.eagerCards`, send `addEagerCard`/`deleteEagerCard`/`moveEagerCard`
- `IdentitySection3View`: read `store.animatedCards`, send `addAnimatedCard`/`deleteAnimatedCard`
- `IdentitySection6View`: read `store.lazyCards`, send `addLazyCard`/`deleteLazyCard`

**Test updates:** Update `IdentityFeatureTests.swift` to use the new per-section action/state names. Tests that reference `cards` should be updated to reference the appropriate section array.
  </action>
  <verify>
    <automated>cd /Users/jacob/Developer/src/github/jacobcxdev/swift-crossplatform && grep -c "eagerCards\|animatedCards\|lazyCards" examples/fuse-app/Sources/FuseApp/IdentityFeature.swift</automated>
  </verify>
  <done>
- Three independent card arrays in TCA state
- Each section reads/writes its own array
- No cross-section state contamination
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix cosmetic UI issues (delete button, layout, spacing)</name>
  <files>
    examples/fuse-app/Sources/FuseApp/IdentityFeature.swift
  </files>
  <action>
**GAP-R2-5 — Delete button visibility:**

Find all delete button instances using `Image(systemName: "trash")` or similar. Replace with `Image(systemName: "xmark.circle.fill")` which maps to Material Design `Cancel` icon on Android. Alternatively use `Label("Delete", systemImage: "xmark.circle.fill")` for text+icon fallback. Apply `.foregroundStyle(.red)` for visibility.

Check the SF Symbol mapping table reference in CLAUDE.md (~line 431 of `forks/skip-ui/Sources/SkipUI/SkipUI/Components/Image.swift`) to verify the chosen icon name is mapped.

**GAP-R2-6 — Section 7 layout ordering:**

In `IdentitySection7View.body`, ensure the VStack contains views in this order:
1. Section header text (title + description)
2. PeerRememberTestView button/counter content

If the button is being rendered via an overlay or ZStack, restructure to a plain VStack ordering.

**GAP-R2-7 — Section 5 counter clipping:**

In `IdentitySection5View` or `IdentitySection5TabContent`, add `.frame(minHeight: 120)` or `.padding(.bottom, 48)` to the tab content to prevent the counter from being clipped by the tab bar.

**GAP-R2-8 — Add/reorder button placement:**

For each section that has add/reorder buttons embedded in scrollable content, move them to a fixed position. Options (use your judgment):
- Move to `.toolbar { ToolbarItem(placement: .primaryAction) { ... } }` on the section view
- Or place them in an HStack pinned at the top of each section (above the ForEach)
- Ensure they remain accessible regardless of scroll position

Keep the approach consistent across all sections.
  </action>
  <verify>
    <automated>cd /Users/jacob/Developer/src/github/jacobcxdev/swift-crossplatform && grep -c "xmark.circle" examples/fuse-app/Sources/FuseApp/IdentityFeature.swift</automated>
  </verify>
  <done>
- Delete buttons use visible icon on Android (xmark.circle.fill or equivalent mapped icon)
- Section 7 content renders below header
- Section 5 counter not clipped by tab bar
- Add/reorder buttons accessible without scrolling
  </done>
</task>

</tasks>

<verification>
1. `just ios-build fuse-app` — succeeds
2. `just android-build fuse-app` — succeeds (cosmetic fixes are Android-facing)
3. `just ios-test fuse-app` — all TCA TestStore tests pass (updated for per-section state)
3. Grep: `grep -c "eagerCards\|animatedCards\|lazyCards" examples/fuse-app/Sources/FuseApp/IdentityFeature.swift` — multiple hits
4. Grep: No remaining references to bare `cards` in section views (only per-section variants)

**HUMAN-ONLY: Do not orchestrate the emulator via adb. Manual UI verification is performed by the human after execution.**

After this plan executes, the human should verify:
- Adding a card in Section 6 does not affect Sections 1/3
- Delete buttons are visible on Android
- Section 7 layout is correct (header above content)
- Section 5 counters not clipped
</verification>

<success_criteria>
- Per-section card arrays eliminate cross-section interference
- Delete buttons visible on Android
- Section 7 layout corrected
- Section 5 counter visible
- All builds and tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/18.1-implement-canonical-view-identity-system/18.1-13-SUMMARY.md`
</output>
