# Phase 18.1 Gap-Closure Plan Verification (Plans 10-11)

**Phase:** 18.1-implement-canonical-view-identity-system
**Plans verified:** 2 (18.1-10-PLAN.md, 18.1-11-PLAN.md)
**Verifier:** Claude (gsd-plan-checker)
**Date:** 2026-03-02
**Status:** ISSUES FOUND — 1 blocker, 3 warnings

---

## Dimension 1: Requirement Coverage

| Requirement | Gap | Plans | Tasks | Status |
|-------------|-----|-------|-------|--------|
| VIEWID-12 (lazy container peer disposal) | GAP-2 | 10 | 1,2,3,4,5 | COVERED |
| VIEWID-13 (tab switch composition scope) | GAP-3 | 10 | 1,2,6 | COVERED |
| VIEWID-14 (AnimatedContent architecturally wrong) | GAP-1 | 11 | 1,2,3,4,5 | COVERED |

All three gap requirements are covered by tasks in the plans.

**Note:** VIEWID-12, VIEWID-13, VIEWID-14 are referenced in VERIFICATION.md and CONTEXT.md but are NOT present in `ROADMAP.md` Requirements line (which lists only VIEWID-03..VIEWID-11) and are NOT present in `REQUIREMENTS.md`. This is a tracking issue — see Blocker 1 below.

## Dimension 2: Task Completeness

### Plan 10 (8 tasks)

| Task | Name | Files | Action | Verify | Done | Notes |
|------|------|-------|--------|--------|------|-------|
| 1 | Create PeerStore.swift | Yes | Yes (specific) | Yes (automated) | Yes | Thorough — all types listed |
| 2 | IdentityKeyModifier LocalPeerStoreItemKey | Yes | Yes (exact code) | Yes (automated) | Yes | Clear before/after |
| 3 | ForEach namespace and eviction | Yes | Yes (references plan) | Yes (automated) | Yes | OK |
| 4 | LazyVStack/LazyHStack providers | Yes | Yes (references plan) | Yes (automated) | Yes | OK |
| 5 | LazyVGrid/LazyHGrid mirror | Yes | Yes (brief) | Yes (automated) | Yes | Acceptable — mirrors Task 4 |
| 6 | TabView PeerStore provider | Yes | Yes (references plan) | Yes (automated) | Yes | OK |
| 7 | Transpiler rememberViewPeer | Yes | Yes (4 codegen sites) | Yes (automated) | Yes | Detailed |
| 8 | Transpiler tests | Yes | Yes (3 test changes) | Yes (automated — runs tests) | Yes | OK |

### Plan 11 (5 tasks)

| Task | Name | Files | Action | Verify | Done | Notes |
|------|------|-------|--------|--------|------|-------|
| 1 | Create RetainedAnimatedItems.swift | Yes | Yes (references plan) | Yes (automated) | Yes | OK |
| 2 | Replace VStack.swift Render | Yes | Yes (delete/replace regions) | Yes (automated) | Yes | Specific line ranges |
| 3 | Replace HStack.swift Render | Yes | Yes (references plan) | Yes (automated) | Yes | OK |
| 4 | Replace ZStack.swift Render | Yes | Yes (references plan) | Yes (automated) | Yes | OK |
| 5 | Delete AnimatedContentArguments.swift | Yes | Yes (specific) | Yes (2 automated checks) | Yes | Thorough |

All tasks have required fields. Actions reference self-contained implementation plans with exact line numbers and code — sufficient for autonomous execution.

## Dimension 3: Dependency Correctness

```
Plan 10: depends_on: [18.1-08, 18.1-09] → Wave 6
Plan 11: depends_on: [18.1-10]           → Wave 7
```

- Plans 08 and 09 are completed (marked with checkmarks in ROADMAP.md) — valid dependency.
- Plan 11 depends on Plan 10 — correct, because Plan 11's RetainedAnimatedItems replaces the AnimatedContent dual-path which Plan 10's transpiler changes might interact with (shared skip-ui container files VStack/HStack/ZStack).
- No cycles detected.
- Wave assignment is consistent: Wave 6 (Plan 10) then Wave 7 (Plan 11).

**Status:** PASS

## Dimension 4: Key Links Planned

### Plan 10

| From | To | Via | Status |
|------|----|-----|--------|
| LazyVStack/LazyHStack PeerStore provider | rememberViewPeer() in transpiler _ComposeContent | LocalPeerStore CompositionLocal | WIRED — Task 4 creates provider, Task 7 generates consumer |
| TabView PeerStore provider | rememberViewPeer() in transpiler _ComposeContent | LocalPeerStore CompositionLocal | WIRED — Task 6 creates provider, Task 7 generates consumer |
| ForEach eviction SideEffect | PeerStore.cleanup(namespace:activeKeys:) | SideEffect on data change | WIRED — Task 3 creates eviction, Task 1 creates cleanup method |
| IdentityKeyModifier | LocalPeerStoreItemKey | CompositionLocalProvider | WIRED — Task 2 bridges identity to peer store item key |

### Plan 11

| From | To | Via | Status |
|------|----|-----|--------|
| VStack/HStack/ZStack Render | RetainedAnimatedItemsState.sync() | rememberRetainedAnimatedItemsState() | WIRED — Tasks 2-4 consume, Task 1 creates |
| effectiveAnimatedKey() | Renderable.identityKey | identityKey fallback | WIRED — Plan 04 established identityKey; Plan 11 Task 1 reads it |
| Plan 08 idMap fix | SUPERSEDED | AnimatedContent dual-path removed | WIRED — Plan 11 removes entire infrastructure Plan 08 patched |

**Status:** PASS — all critical wiring accounted for.

## Dimension 5: Scope Sanity

| Plan | Tasks | Files Created | Files Modified | Total Files | Status |
|------|-------|---------------|----------------|-------------|--------|
| 10 | 8 | 1 | 9 | 10 | WARNING |
| 11 | 5 | 1 | 3 (+1 deleted) | 5 | OK |

**Plan 10 has 8 tasks and 10 files** — this exceeds the 2-3 task target significantly. However, mitigating factors:
- Tasks 4-5 are mechanical mirrors (LazyVStack/LazyHStack pattern applied to LazyVGrid/LazyHGrid) — low cognitive load.
- Task 2 is a 4-line change (trivial).
- Task 5 is explicitly described as "same pattern as Task 4" — copy-paste.
- A self-contained implementation plan (`docs/identity-implementation-plan-peerstore.md`, 64KB) provides exact code at every insertion point.
- The plan is `autonomous: true` with gap closure scope — splitting would force multiple context loads of the same 64KB spec.

**Plan 11 has 5 tasks and 5 files** — borderline but acceptable:
- Tasks 2-4 are the same pattern applied to 3 files (VStack, HStack, ZStack).
- Task 5 is a file deletion (trivial).
- Self-contained implementation plan provides complete replacement code.

**Verdict:** Warning for Plan 10 scope but not a blocker given the mechanical nature and self-contained spec.

## Dimension 6: Verification Derivation (must_haves)

### Plan 10 must_haves.truths

| Truth | User-Observable? | Testable? |
|-------|-------------------|-----------|
| "PeerStore caches Swift peers across Compose composition disposal" | Yes — user sees counters preserved after scroll-off/tab-switch | Yes — UAT Tests 6, 7 |
| "rememberViewPeer() replaces raw remember { SwiftPeerHandle }" | Implementation-focused | Indirectly — transpiler test |
| "Transpiler generates Swift_refreshPeer cdecl for mixed views" | Implementation-focused | Yes — transpiler test |
| "Fallback path preserves existing behaviour when no PeerStore" | Yes — existing functionality unchanged | Yes — existing tests pass |
| "ForEach eviction cleans up stale peers" | Yes — memory not leaked | Indirectly — no direct test |

Truths 2-3 are implementation-focused rather than user-observable. However, they are necessary intermediate conditions for Truth 1 to hold, and they have concrete automated verification (transpiler tests). Acceptable for gap-closure plans.

### Plan 11 must_haves.truths

| Truth | User-Observable? | Testable? |
|-------|-------------------|-----------|
| "RetainedAnimatedItems provides per-item AnimatedVisibility" | Partially — user sees animations | Yes — grep + build |
| "VStack/HStack/ZStack use single unified rendering path" | Architectural — user sees same output | Yes — grep for no AnimatedContent |
| "ForEach items with withAnimation get visible enter/exit animations" | Yes — user sees animations | Yes — UAT Test 3 |
| "Items without transition modifier get axis-aware defaults" | Yes — user sees appropriate defaults | Partially — runtime only |
| "AnimatedContentArguments.swift is deleted" | Implementation cleanup | Yes — file absence check |

Mix of user-observable and implementation truths — acceptable for this scope.

**Status:** PASS with info-level note on implementation-focused truths.

## Dimension 7: Context Compliance

### Locked Decisions Check

| Decision | Plan Compliance | Status |
|----------|-----------------|--------|
| PeerStore for GAP-2/GAP-3 (CONTEXT.md Wave 6) | Plan 10 implements exactly | COMPLIANT |
| RetainedAnimatedItems for GAP-1 (CONTEXT.md Wave 7) | Plan 11 implements exactly | COMPLIANT |
| Self-contained specs as PRIMARY (CONTEXT.md) | Both plans reference their spec docs | COMPLIANT |
| HUMAN-ONLY manual UI testing | Both plans include the note | COMPLIANT |
| `#if SKIP` guard for new files | Plan 10 Task 1 specifies it; Plan 11 Task 1 specifies it | COMPLIANT |
| FUSE_IDENTITY_DEBUG for debug logging | Both plans specify it in diagnostic_logging | COMPLIANT |

### Deferred Ideas Check

| Deferred Idea | Present in Plans? | Status |
|---------------|-------------------|--------|
| explicitResetKey promotion | No | OK |
| Transpiler structural ID injection | No | OK |
| Conditional branch identity | No | OK |
| @Stable/skippability | No | OK |

**Status:** PASS — no deferred ideas included, all locked decisions honored.

## Dimension 8: Nyquist Compliance

SKIPPED (nyquist_validation not applicable — no "Validation Architecture" section in RESEARCH.md for these gap-closure plans).

---

## Issues

### Blockers (must fix)

**1. [requirement_coverage] VIEWID-12, VIEWID-13, VIEWID-14 missing from ROADMAP.md and REQUIREMENTS.md**

```yaml
issue:
  plan: null
  dimension: requirement_coverage
  severity: blocker
  description: >
    VIEWID-12, VIEWID-13, and VIEWID-14 are referenced in VERIFICATION.md gaps_remaining,
    CONTEXT.md Wave 6/7 descriptions, and Plans 10/11 requirements frontmatter, but they
    do NOT appear in ROADMAP.md (Requirements line lists only VIEWID-03..VIEWID-11) and
    do NOT appear in REQUIREMENTS.md. Without formal requirement entries, these gaps
    have no traceability anchor. Plan 09 added VIEWID-03..VIEWID-11 to REQUIREMENTS.md
    but did not add the three new gap-closure requirements.
  fix_hint: >
    Add VIEWID-12 (lazy container peer disposal), VIEWID-13 (tab switch composition scope),
    and VIEWID-14 (AnimatedContent replacement) to both REQUIREMENTS.md and ROADMAP.md
    Phase 18.1 Requirements line. This can be done as part of Plan 10's execution or as
    a pre-execution housekeeping step.
```

### Warnings (should fix)

**2. [scope_sanity] Plan 10 has 8 tasks — exceeds recommended 2-3 target**

```yaml
issue:
  plan: "18.1-10"
  dimension: scope_sanity
  severity: warning
  description: >
    Plan 10 has 8 tasks modifying 10 files. This exceeds the 2-3 task target.
    Mitigated by: mechanical mirror tasks (4-5), trivial tasks (2), self-contained
    64KB implementation spec, and autonomous gap-closure scope.
  metrics:
    tasks: 8
    files: 10
  fix_hint: >
    Acceptable as-is given mitigating factors. If context budget is a concern,
    consider splitting into 10a (runtime: Tasks 1-6) and 10b (transpiler: Tasks 7-8),
    but this adds overhead of loading the spec twice.
```

**3. [task_completeness] Plan 10 Task 5 action is vague**

```yaml
issue:
  plan: "18.1-10"
  dimension: task_completeness
  severity: warning
  task: 5
  description: >
    Task 5 (LazyVGrid/LazyHGrid) action says "Apply same PeerStore provider pattern
    as LazyVStack/LazyHStack" without referencing specific implementation plan step
    numbers or line numbers, unlike other tasks which say "Follow Step N of the
    implementation plan."
  fix_hint: >
    Add reference to specific implementation plan step(s) or note that LazyVGrid/LazyHGrid
    follow identical structure to Steps 4-5. The executor should be able to infer this,
    but explicit is better.
```

**4. [verification_derivation] Plan 10 verification lacks direct peer survival assertion**

```yaml
issue:
  plan: "18.1-10"
  dimension: verification_derivation
  severity: warning
  description: >
    Plan 10's automated verification checks (grep for patterns, build, test) confirm
    code exists and compiles but do not directly verify peer survival across
    composition disposal. The only direct verification is human UAT (Tests 6, 7).
    There is no automated test that exercises PeerStore's retain-across-disposal
    behavior — e.g., a unit test that simulates remember/forget/re-remember cycle.
  fix_hint: >
    This is acceptable for gap-closure scope since the behavior is inherently a
    Compose runtime concern (composition disposal cannot be simulated in unit tests
    without Robolectric). The human UAT step adequately covers this. No action needed
    unless a Robolectric integration test is feasible.
```

---

## Coverage Summary

| Requirement | Plans | Status |
|-------------|-------|--------|
| VIEWID-12 (lazy peer disposal) | 10 | COVERED (tasks 1-5) |
| VIEWID-13 (tab switch peer) | 10 | COVERED (tasks 1,2,6) |
| VIEWID-14 (AnimatedContent replacement) | 11 | COVERED (tasks 1-5) |

## Plan Summary

| Plan | Tasks | Files | Wave | Depends On | Structural Validity | Issues |
|------|-------|-------|------|------------|---------------------|--------|
| 10 | 8 | 10 | 6 | 08, 09 | Valid (all fields present) | Scope warning, vague Task 5 |
| 11 | 5 | 5 | 7 | 10 | Valid (all fields present) | None |

## Recommendation

**1 blocker requires action before or during execution:**

The VIEWID-12/13/14 requirements must be added to ROADMAP.md and REQUIREMENTS.md for traceability. This is a documentation/tracking gap, not a plan design issue — the plans themselves correctly address all three gaps with specific, actionable tasks backed by self-contained implementation specs.

**3 warnings are acceptable as-is** given the gap-closure context, mechanical task nature, and comprehensive self-contained implementation plans. No plan revision needed for the warnings.

**Overall assessment:** Plans 10 and 11 WILL achieve the gap-closure goals. The technical design is sound — PeerStore addresses the root cause of peer disposal (not symptoms), and RetainedAnimatedItems is a clean architectural replacement (not a patch). Both plans have detailed implementation specs that enable autonomous execution. The dependency chain (10 before 11) is correct since both touch skip-ui container files.

After fixing the blocker (requirement tracking), proceed with `/gsd:execute-phase 18.1` for Wave 6 (Plan 10), then Wave 7 (Plan 11).
