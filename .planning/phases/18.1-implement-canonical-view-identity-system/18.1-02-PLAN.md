---
phase: 18.1-implement-canonical-view-identity-system
plan: 02
type: execute
wave: 1b
depends_on:
  - 18.1-01
files_modified:
  - examples/fuse-app/Sources/FuseApp/IdentityFeature.swift
  - examples/fuse-app/Tests/FuseAppTests/IdentityFeatureTests.swift
autonomous: true
requirements:
  - VIEWID-03
  - VIEWID-04
  - VIEWID-06

must_haves:
  truths:
    - "Identity tab sections 1-4 are fully implemented with interactive UI elements and visible state labels"
    - "Section 1 (Eager Container Keying) covers VStack, HStack, ZStack with ForEach card deletion and reorder"
    - "Section 2 (Duplicate Key Guard) uses a non-TCA raw [String] array with ForEach(\\.self) to produce actual duplicate keys"
    - "Section 3 (AnimatedContent) has animated deletion toggle with counter state preservation verification"
    - "Section 4 (Picker Selection) covers segmented and menu picker styles with selectionTag verification"
    - "TCA TestStore tests for sections 1, 3, 4 compile and pass (reducer state mutations)"
    - "Section 2 has NO TCA TestStore test — documented as Android integration/manual test only"
  artifacts:
    - path: "examples/fuse-app/Sources/FuseApp/IdentityFeature.swift"
      provides: "Identity tab sections 1-4 with full UI implementation"
      contains: "Section 1"
    - path: "examples/fuse-app/Tests/FuseAppTests/IdentityFeatureTests.swift"
      provides: "TCA TestStore tests for sections 1, 3, 4"
      contains: "IdentityFeatureTests"
  key_links:
    - from: "IdentityFeature.swift sections 1-4 UI"
      to: "IdentityFeatureTests.swift tests"
      via: "Sections 1, 3, 4 have TestStore assertions; section 2 is Android manual test only"
      pattern: "IdentityFeature"
---

<objective>
RED PHASE (Wave 1b): Implement Identity tab UI sections 1-4 and write their TCA TestStore tests.

This plan builds on Plan 01's reducer scaffolding. It implements the interactive UI views for sections 1-4 and writes corresponding tests. The reducer state/actions/body already exist from Plan 01 — this plan adds the VIEW layer and TESTS.

Output: Sections 1-4 fully rendered with interactive elements. Tests for sections 1, 3, 4 compile and pass. Section 2 documented as Android-only verification.
</objective>

<execution_context>
@/Users/jacob/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jacob/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/18.1-implement-canonical-view-identity-system/18.1-CONTEXT.md
@.planning/phases/18.1-implement-canonical-view-identity-system/18.1-01-SUMMARY.md

Source files:
@examples/fuse-app/Sources/FuseApp/IdentityFeature.swift
@examples/fuse-app/Tests/ (read test directory structure)

NOTE: Plan 01 (reducer scaffolding) has been completed. The reducer state, actions, and body are defined. This plan adds the UI views and tests for sections 1-4.
</context>

<interfaces>
From IdentityFeature.swift (after Plan 01):
- `IdentityFeature` reducer with complete state/actions/body
- `CardItem` struct with id/title
- `CounterCard` view with local Store + instanceID
- `PeerRememberTestView` with @State + let-with-default

From TCA testing patterns (already audited in Plan 01 /pfw alignment):
- `TestStore(initialState:reducer:)` for state mutation verification
- `store.send(.view(.action))` for @ViewAction reducers
- `@Dependency(\.uuid)` with `.incrementing` for deterministic UUIDs
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Implement Identity tab UI sections 1-4</name>
  <files>examples/fuse-app/Sources/FuseApp/IdentityFeature.swift</files>
  <action>
Replace the placeholder view body from Plan 01 with a sectioned ScrollView. Implement sections 1-4 only (sections 5-8 come in Plan 03).

**Section 1: Eager Container Keying (VStack/HStack/ZStack) — Plans 04, 06**

Retain and enhance the existing card deletion scenario:
- `ForEach(store.cards)` in a `VStack` — already exists
- Add: "Reorder" functionality (move last card to first position) to test identity stability during reorder
- Add: Inline display of each card's `instanceID` prefix AND `store.count` — makes state preservation visually obvious
- **HUMAN-ONLY: Do not orchestrate the emulator via adb. Manual UI verification is performed by the human between waves. Do not waste tokens on adb commands, screenshot capture, or emulator automation.**
- **Manual test:** Increment counters on cards, delete middle card → remaining cards keep their counter values and instanceIDs
- **Manual test:** Increment counters, reorder → cards keep state after position change

Add a parallel HStack section (horizontal scroll) with a smaller set of items:
- Same ForEach-over-IdentifiedArray pattern but in HStack layout
- Demonstrates identity preservation works in horizontal containers too

Add a ZStack section:
- ForEach items rendered as overlapping cards (offset by index)
- Delete an item → remaining items retain state (ZStack previously had NO key() wrapping — Plan 04 Phase 0 fix)

**Section 2: Duplicate Key Guard — Plan 04**

`IdentifiedArrayOf<CardItem>` enforces unique IDs, so duplicate keys CANNOT be tested at the TCA state level. Instead, test the `seenKeys` duplicate guard at the rendering level:
- Add a NON-TCA subsection that renders a raw `[String]` array with intentional duplicates (e.g. `["Alpha", "Beta", "Alpha", "Gamma"]`) through `ForEach` using `\.self` as the id keypath — this produces duplicate keys ("Alpha" appears twice)
- Each item renders as a small counter card (local `@State var count = 0`) so state independence is observable
- Display a label showing "Duplicate keys present" to confirm the scenario is active
- **HUMAN-ONLY: Do not orchestrate the emulator via adb. Manual UI verification is performed by the human between waves. Do not waste tokens on adb commands, screenshot capture, or emulator automation.**
- **Manual test (Android):** Increment the counter on the first "Alpha" card. Verify the second "Alpha" card's counter is independent (not shared). Verify no crash occurs.
- **NOTE:** This section has NO TCA TestStore test — `IdentifiedArray` prevents the scenario. The duplicate-key `_dup` suffix guard is a Compose-level behavior verified via Android integration test or manual emulator testing only.

**Section 3: Animated Content — Plan 06**

Add an animated deletion section:
- Toggle `withAnimation` for deletions (button or switch)
- When animated: deleted card should visually animate out (fade/slide)
- Remaining cards should retain their counter state through the animation
- **HUMAN-ONLY: Do not orchestrate the emulator via adb. Manual UI verification is performed by the human between waves. Do not waste tokens on adb commands, screenshot capture, or emulator automation.**
- **Manual test:** Enable animation → increment counters → delete middle card → watch exit animation → verify remaining counters unchanged

**Section 4: Picker Selection Correctness — Plan 04**

Add a Picker subsection with multiple styles:
- **Segmented Picker:** `Picker("Style", selection: $store.selectedStyle) { ForEach(styles) { Text($0.name).tag($0) } }` with `.pickerStyle(.segmented)`
- **Menu Picker:** Same data with `.pickerStyle(.menu)`
- Display the currently selected value below each picker
- **HUMAN-ONLY: Do not orchestrate the emulator via adb. Manual UI verification is performed by the human between waves. Do not waste tokens on adb commands, screenshot capture, or emulator automation.**
- **Manual test:** Tap each segment/menu item → displayed selection matches tapped item
- This verifies that `selectionTag` works correctly after ForEach identity refactoring

**Layout:** Use a `ScrollView` with `VStack(spacing: 24)`. Each section has:
- A header (`Text("Section N: Title").font(.headline)`)
- A description (`Text("...").font(.footnote).foregroundStyle(.secondary)`)
- The interactive UI elements
- Clear labels showing observable state values (counters, IDs, selection values)

Leave a placeholder comment for sections 5-8: `// Sections 5-8 implemented in Plan 03`
  </action>
  <verify>
    <automated>cd /Users/jacob/Developer/src/github/jacobcxdev/swift-crossplatform && just ios-build fuse-app 2>&1 | tail -10</automated>
    <automated>cd /Users/jacob/Developer/src/github/jacobcxdev/swift-crossplatform && just android-build fuse-app 2>&1 | tail -10</automated>
  </verify>
  <done>
Sections 1-4 fully implemented with interactive UI, visible state labels, and clear section headers. iOS and Android builds succeed. Sections 5-8 have placeholder comments.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write TCA TestStore tests for sections 1, 3, 4</name>
  <files>examples/fuse-app/Tests/FuseAppTests/IdentityFeatureTests.swift</files>
  <action>
Create `IdentityFeatureTests.swift` with TCA TestStore tests covering reducer logic for sections 1, 3, and 4. Section 2 (duplicate keys) has no TestStore test — documented as Android-only.

**Test 1: Card deletion preserves remaining cards (Section 1)**
```swift
@Test func deleteCardPreservesRemaining() async {
    let store = TestStore(initialState: IdentityFeature.State(...)) {
        IdentityFeature()
    } withDependencies: {
        $0.uuid = .incrementing
    }
    // Add cards, delete middle one, verify remaining cards unchanged
    await store.send(.view(.deleteCardButtonTapped(cardBId))) {
        $0.cards.remove(id: cardBId)
    }
}
```

**Test 2: Card reorder (Section 1)**
- Send reorder action, verify cards array is reordered but all cards retain their IDs and titles

**Test 3: Card addition (Section 1)**
- Send addCardButtonTapped, verify new card appended with correct title and nextCardLetter incremented

**Test 4: Multiple deletions (Section 1)**
- Add 4 cards, delete first and third, verify second and fourth remain with correct IDs

**Test 5: Animated deletion state (Section 3)**
- If isAnimatedDeletion state exists: toggle it, verify state change
- Then delete a card, verify remaining cards unchanged (same as test 1 — animation is visual only)

**Test 6: Picker selection (Section 4)**
- Send styleSelected action, verify selectedStyle state updates

**Notes:**
- Use `@Dependency(\.uuid) var uuid` with `.incrementing` for deterministic UUIDs
- Tests verify reducer logic only — Compose identity rendering is verified by Android tests and manual UI testing
- Each test should be independent (own TestStore instance)
- Follow existing test file conventions in fuse-app/Tests/
- Section 2 (Duplicate Key Guard) has NO TestStore test — `IdentifiedArray` prevents duplicate IDs at the state level
  </action>
  <verify>
    <automated>cd /Users/jacob/Developer/src/github/jacobcxdev/swift-crossplatform && just ios-test fuse-app 2>&1 | tail -20</automated>
  </verify>
  <done>
IdentityFeatureTests.swift created with TCA TestStore tests for sections 1, 3, 4. All tests compile and pass (reducer state mutations work regardless of identity infrastructure). Section 2 documented as Android-only verification.
  </done>
</task>

</tasks>

<verification>
**Build verification:**
1. `just ios-build fuse-app` — succeeds
2. `just android-build fuse-app` — succeeds

**Test verification:**
3. `just ios-test fuse-app` — IdentityFeatureTests compile and pass
4. `just android-test fuse-app` — no regressions

**Coverage verification:**
5. Section 1: card add, delete, reorder tests pass
6. Section 2: NO TestStore test (documented as Android-only)
7. Section 3: animated deletion state test passes
8. Section 4: picker selection test passes

**HUMAN-ONLY: Do not orchestrate the emulator via adb. Manual UI verification is performed by the human between waves. Do not waste tokens on adb commands, screenshot capture, or emulator automation.**

**Manual acceptance test protocol (for human between waves):**
- Section 1: Increment Card A counter to 3, Card B counter to 5. Delete Card A. Verify Card B still shows count=5 and same instanceID. (EXPECTED: BROKEN — counter resets before identity infra)
- Section 2: Verify both duplicate items render without crash. (EXPECTED: MAY CRASH before _dup guard)
- Section 3: Enable animation. Increment counters. Delete middle card. Watch exit animation. Verify remaining counters. (EXPECTED: MAY BE BROKEN)
- Section 4: Tap each Picker segment. Verify displayed selection matches. (EXPECTED: MAY WORK on baseline)
</verification>

<success_criteria>
- Sections 1-4 fully rendered with interactive UI elements and visible state labels
- Section 1: VStack, HStack, ZStack variants with card deletion and reorder
- Section 2: Non-TCA raw array with duplicate keys — Android-only verification
- Section 3: Animated deletion toggle with counter preservation
- Section 4: Segmented and menu picker styles
- TCA TestStore tests for sections 1, 3, 4 compile and pass
- Section 2 explicitly documented as Android integration/manual test only
- iOS and Android builds succeed
</success_criteria>

<pfw_alignment>
**Point-Free skill alignment:** Plan 01 completed the full /pfw audit. This plan uses the patterns established there. If any new TCA pattern emerges during UI implementation (e.g., Binding for Picker), consult `/pfw-composable-architecture` for the correct binding pattern.
</pfw_alignment>

<output>
After completion, create `.planning/phases/18.1-implement-canonical-view-identity-system/18.1-02-SUMMARY.md`

<checkpoint>
**HUMAN INTERVENTION REQUIRED — Wave 1b (Plan 02 — Sections 1-4) complete.**

Before proceeding to Wave 1c (Plan 03 — Sections 5-8):
1. **Run `/clear`** to compact context.
2. **Confirm iOS build and tests pass** — `just ios-build fuse-app && just ios-test fuse-app`
3. **Review sections 1-4** in IdentityFeature.swift — verify interactive elements are present.

Once confirmed, proceed to Wave 1c: Plan 03 (Identity tab sections 5-8 + tests).
</checkpoint>
</output>
</content>
</invoke></output>
