---
phase: 18.1-implement-canonical-view-identity-system
plan: 02
type: execute
wave: 2
depends_on:
  - 18.1-05
files_modified:
  - forks/skipstone/Sources/SkipSyntax/Kotlin/KotlinBridgeToKotlinVisitor.swift
  - forks/skipstone/Tests/SkipSyntaxTests/BridgeToKotlinTests.swift
autonomous: true
requirements:
  - VIEWID-10

must_haves:
  truths:
    - "Views with both @State properties and let-with-default properties get peer remembering codegen"
    - "Views with only @State (no let-with-default) are unaffected by the guard removal"
    - "Views with only let-with-default (no @State) continue to get peer remembering as before"
  artifacts:
    - path: "forks/skipstone/Sources/SkipSyntax/Kotlin/KotlinBridgeToKotlinVisitor.swift"
      provides: "Removed stateVariables.isEmpty guard enabling mixed-view peer remembering"
      contains: "canRememberPeer"
    - path: "forks/skipstone/Tests/SkipSyntaxTests/BridgeToKotlinTests.swift"
      provides: "Test for @State + let-with-default combined codegen"
      contains: "stateAndLetWithDefault"
  key_links:
    - from: "KotlinBridgeToKotlinVisitor.swift guard condition"
      to: "Evaluate/_ComposeContent override generation"
      via: "canRememberPeer || canRememberPeerWithInputCheck condition"
      pattern: "canRememberPeer"
---

<objective>
Fix the transpiler stateVariables.isEmpty guard that prevents peer remembering for views with both @State and let-with-default properties. Add a transpiler test validating the combined codegen.

Purpose: Views like `struct Counter: View { @State var count = 0; let instanceID = UUID() }` currently don't get peer remembering because the guard at KotlinBridgeToKotlinVisitor.swift:1734 skips Evaluate/_ComposeContent override generation when stateVariables is non-empty. Removing this guard allows peer remembering to coexist with state variables.

Output: Transpiler generates correct codegen for mixed views. Test validates the fix.
</objective>

<execution_context>
@/Users/jacob/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jacob/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/18.1-implement-canonical-view-identity-system/18.1-CONTEXT.md
@.planning/phases/18.1-implement-canonical-view-identity-system/18.1-RESEARCH.md

Source files:
@forks/skipstone/Sources/SkipSyntax/Kotlin/KotlinBridgeToKotlinVisitor.swift
@forks/skipstone/Tests/SkipSyntaxTests/BridgeToKotlinTests.swift
</context>

<interfaces>
<!-- Key transpiler types the executor needs -->

From KotlinBridgeToKotlinVisitor.swift (~line 1734):
- Guard condition: `if (canRememberPeer || canRememberPeerWithInputCheck) && stateVariables.isEmpty`
- `canRememberPeer` = has let-with-default properties AND no constructor params AND no non-bridgable constructor params
- `canRememberPeerWithInputCheck` = has let-with-default AND has constructor params AND no non-bridgable constructor params
- `stateVariables` = list of @State properties on the view struct
- The guard controls whether Evaluate/_ComposeContent override is generated for peer remembering

From BridgeToKotlinTests.swift:
- Existing tests use `assertTranspiled(source:kotlin:)` pattern
- Test views are defined as Swift source strings, expected Kotlin output is verified
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Remove stateVariables.isEmpty guard and add transpiler test</name>
  <files>
    forks/skipstone/Sources/SkipSyntax/Kotlin/KotlinBridgeToKotlinVisitor.swift,
    forks/skipstone/Tests/SkipSyntaxTests/BridgeToKotlinTests.swift
  </files>
  <action>
1. **KotlinBridgeToKotlinVisitor.swift (~line 1734):**
   Change the guard condition from:
   ```swift
   if (canRememberPeer || canRememberPeerWithInputCheck) && stateVariables.isEmpty {
   ```
   to:
   ```swift
   if canRememberPeer || canRememberPeerWithInputCheck {
   ```
   This allows peer remembering codegen to be emitted for views that also have @State properties.

   **Verify the surrounding code:** Read the full block inside this guard to ensure removing `stateVariables.isEmpty` doesn't interact with state variable handling elsewhere in the same block. The Evaluate/_ComposeContent override should coexist with state variable compilation — @State uses Compose's `remember { mutableStateOf() }` pattern independently of SwiftPeerHandle remembering.

2. **BridgeToKotlinTests.swift — Add test for mixed view:**
   Add a new test method (following existing test naming conventions) that validates a view struct with both `@State var count: Int = 0` and `let instanceID = UUID()`:
   - The source should define a simple `View` struct with both property types
   - The expected Kotlin output should include:
     - `SwiftPeerHandle` remembering (from let-with-default)
     - `mutableStateOf` for the @State property
     - The Evaluate/_ComposeContent override IS generated (not skipped)
   - Name the test something like `testStateAndLetWithDefaultCombinedCodegen` or follow existing naming conventions in the file

   Read existing tests in BridgeToKotlinTests.swift to match the assertion style exactly. The test should assert that the transpiled Kotlin contains the peer remembering infrastructure (SwiftPeerHandle or similar) AND the @State infrastructure simultaneously.
  </action>
  <verify>
    <automated>cd /Users/jacob/Developer/src/github/jacobcxdev/swift-crossplatform/forks/skipstone && swift test --filter BridgeToKotlinTests 2>&1 | tail -20</automated>
  </verify>
  <done>
The stateVariables.isEmpty guard is removed. The new transpiler test passes, confirming that views with both @State and let-with-default properties generate peer remembering codegen alongside state variable codegen.
  </done>
</task>

</tasks>

<verification>
1. `cd forks/skipstone && swift test --filter BridgeToKotlinTests` — all tests pass including the new mixed-view test
2. `cd forks/skipstone && swift test` — full skipstone test suite passes (no regressions)
3. Verify the guard line no longer contains `stateVariables.isEmpty`

**GREEN phase verification (Plan 05 tests that should now PASS):**
4. Plan 05's **Section 7** (Transpiler Peer Remembering) — Android integration test: views with both `@State` and `let`-with-default` now get peer remembering codegen. On Android emulator, `PeerRememberTestView` should retain `@State` values across parent recomposition (was FAILING in Wave 1 RED phase).
</verification>

<success_criteria>
- stateVariables.isEmpty condition removed from the guard at KotlinBridgeToKotlinVisitor.swift
- New transpiler test validates @State + let-with-default combined codegen
- All existing transpiler tests pass (no regressions)
- Mixed views will now get both peer remembering and state variable codegen
</success_criteria>

<pfw_alignment>
**Point-Free skill alignment (MANDATORY):** This plan modifies the skipstone transpiler which generates Kotlin bridge code for views that may use TCA patterns. While the transpiler changes are mechanical (guard removal), the transpiler test should verify codegen for a view pattern that is valid under TCA conventions. Before writing the test view struct, consult `/pfw-composable-architecture` to ensure the test view's `@State` + `let`-with-default pattern is representative of real TCA usage (e.g., a view with a `Store` as a `let`-with-default and local `@State`).
</pfw_alignment>

<output>
After completion, create `.planning/phases/18.1-implement-canonical-view-identity-system/18.1-02-SUMMARY.md`

**Traceability note:** VIEWID-10 is referenced in this plan's frontmatter but is not yet defined in REQUIREMENTS.md. During phase closure, add this requirement ID to `.planning/REQUIREMENTS.md` with a description matching the must_haves truth it traces to.

**Wave 2 checkpoint:** This plan runs in parallel with Plan 01. The Wave 2 checkpoint is in Plan 01's output section — both plans must complete before the human intervention break.
</output>
