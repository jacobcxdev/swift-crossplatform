---
phase: 18.1-implement-canonical-view-identity-system
plan: 06
type: execute
wave: 3
depends_on:
  - 18.1-04
  - 18.1-03
files_modified:
  - forks/skip-ui/Sources/SkipUI/SkipUI/Containers/VStack.swift
  - forks/skip-ui/Sources/SkipUI/SkipUI/Containers/HStack.swift
  - forks/skip-ui/Sources/SkipUI/SkipUI/Containers/ZStack.swift
  - examples/fuse-app/Tests/FuseAppTests/IdentityFeatureTests.swift
autonomous: true
requirements:
  - VIEWID-05
  - VIEWID-09

must_haves:
  truths:
    - "AnimatedContent contentKey uses normalizeKey() instead of raw SwiftHashable values from idMap"
    - "AnimatedContent in VStack/HStack with ForEach deletion preserves remaining items' state AND deleted item gets exit animation"
    - "key() inside AnimatedContent content lambda does not break animateEnterExit scope chain"
    - "VStack/HStack AnimatedContent render loops use key(identityKey ?? i) with seenKeys guard"
  artifacts:
    - path: "forks/skip-ui/Sources/SkipUI/SkipUI/Containers/VStack.swift"
      provides: "AnimatedContent contentKey normalization + key() in animated loops"
      contains: "normalizeKey"
    - path: "forks/skip-ui/Sources/SkipUI/SkipUI/Containers/HStack.swift"
      provides: "Same as VStack"
      contains: "normalizeKey"
    - path: "forks/skip-ui/Sources/SkipUI/SkipUI/Containers/ZStack.swift"
      provides: "AnimatedContent contentKey normalization + key() in animated Box loop"
      contains: "normalizeKey"
  key_links:
    - from: "VStack/HStack/ZStack AnimatedContent contentKey"
      to: "Renderable.swift normalizeKey()"
      via: "idMap output normalization through normalizeKey()"
      pattern: "normalizeKey"
    - from: "VStack/HStack AnimatedContent render loops"
      to: "Renderable.swift identityKey"
      via: "key(identityKey ?? i) pattern inside AnimatedContent content lambda"
      pattern: "identityKey"
---

<objective>
Apply identity normalization and key() wrapping to AnimatedContent paths in VStack, HStack, and ZStack. This is Phase 2 from the recommendation document — empirically gated on testing that key() inside AnimatedContent does not break animateEnterExit.

Purpose: The animated paths in VStack.swift, HStack.swift, and ZStack.swift derive AnimatedContent's contentKey from raw .id() values via idMap. These values are SwiftHashable — subject to the JNI equality problem. This plan normalizes them through normalizeKey() and adds key(identityKey ?? i) to animated render loops.

Output: All animated container paths use normalized keys and have identity-based key() wrapping.
</objective>

<execution_context>
@/Users/jacob/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jacob/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/18.1-implement-canonical-view-identity-system/18.1-CONTEXT.md
@.planning/phases/18.1-implement-canonical-view-identity-system/18.1-RESEARCH.md
@.planning/phases/18.1-implement-canonical-view-identity-system/18.1-04-SUMMARY.md

Source files:
@forks/skip-ui/Sources/SkipUI/SkipUI/Containers/VStack.swift
@forks/skip-ui/Sources/SkipUI/SkipUI/Containers/HStack.swift
@forks/skip-ui/Sources/SkipUI/SkipUI/Containers/ZStack.swift
@forks/skip-ui/Sources/SkipUI/SkipUI/Compose/Renderable.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize AnimatedContent contentKey and add key() to animated render loops</name>
  <files>
    forks/skip-ui/Sources/SkipUI/SkipUI/Containers/VStack.swift,
    forks/skip-ui/Sources/SkipUI/SkipUI/Containers/HStack.swift,
    forks/skip-ui/Sources/SkipUI/SkipUI/Containers/ZStack.swift
  </files>
  <action>
**IMPORTANT: Test-first approach per CONTEXT.md "Phase 2 is gated on passing tests."**

Before applying changes, understand the AnimatedContent pattern by reading the animated paths in VStack.swift, HStack.swift, and ZStack.swift. The key concern (from Paper 07 section 1) is that key() inside AnimatedContent's content lambda might break the AnimatedVisibilityScope chain required by animateEnterExit.

**Step 0: Write behavioral tests BEFORE making any code changes**

Write Android/Robolectric tests (or extend existing fuse-app/fuse-library test suite) that exercise animated deletion and state retention. These tests MUST pass after changes are applied:

1. **Animated deletion preserves remaining state:** Create a VStack/HStack with ForEach over a mutable list of items, each containing @State counter. Increment counters, delete the middle item. Assert remaining items' counter values are preserved (not reset to 0).
2. **Deleted item gets exit animation:** After deleting an item from an animated VStack/HStack ForEach, assert that AnimatedContent receives the removed item for exit animation (not silently dropped). Verify via the AnimatedContent contentKey diff — the removed key should appear in the "exiting" set.
3. **key() inside AnimatedContent does not break AnimatedVisibilityScope:** Render an animated VStack with `withAnimation` deletion. Assert that `animateEnterExit` modifiers on child items still function — specifically, the `AnimatedVisibilityScope` receiver is available inside the key() block.
4. **ZStack animated deletion preserves remaining state:** Create a ZStack with ForEach over a mutable list of items, each containing @State counter. Increment counters, delete an item. Assert remaining items' counter values are preserved (not reset to 0). This covers ZStack's `RenderAnimatedContent` Box loop path which differs from VStack/HStack's Column/Row layout.

If any test CANNOT be written as a unit test (e.g., AnimatedVisibilityScope requires a live Compose runtime), document what manual emulator verification is needed instead and note the limitation in the SUMMARY.

Run these tests BEFORE proceeding to Step 1. If tests fail on the CURRENT code (pre-change baseline), document that as expected behavior and note the test as a regression gate for the changes.

**Step 1: Normalize contentKey through normalizeKey()**

In all three files, the AnimatedContent receives a `contentKey` parameter derived from `idMap`:
```swift
contentKey: { $0.map(arguments.idMap) }
```
where `idMap` reads raw `.id()` values via `TagModifier.on(content:role:.id)?.value`. These are SwiftHashable — subject to JNI equality issues.

Normalize the idMap output through normalizeKey():
```swift
contentKey: { $0.map { normalizeKey(arguments.idMap($0)) } }
```
Or equivalently, modify idMap to normalize:
```swift
contentKey: { $0.map { renderable in
    let raw = arguments.idMap(renderable)
    return normalizeKey(raw)
} }
```

Apply to:
- VStack.swift AnimatedContent contentKey (~line 201-202)
- HStack.swift AnimatedContent contentKey (~line 166-167)
- ZStack.swift AnimatedContent contentKey (~line 85-86)

**Step 2: Add key(identityKey ?? i) to animated render loops**

Inside the AnimatedContent content lambda, there are render loops that iterate renderables. Apply the same key(identityKey ?? i) + seenKeys guard pattern from Plan 04, but inside the AnimatedContent content lambda.

Apply to:
- VStack.swift animated loop sites (~lines 224-235, 250-261 — check exact locations)
- HStack.swift animated loop sites (~lines 188-199, 214-225 — check exact locations)
- ZStack.swift animated Box loop (~lines 92-103)

The pattern is the same as non-animated loops:
```swift
var seenKeys = mutableSetOf<Any>()
for i in 0..<renderables.size {
    let renderable = renderables[i]
    var composeKey: Any = renderable.identityKey ?? i
    if !seenKeys.add(composeKey) {
        composeKey = "\(composeKey)_dup\(i)"
    }
    androidx.compose.runtime.key(composeKey) {
        renderable.Render(context: contentContext)
    }
}
```

**Step 2.5: Verify key() inside AnimatedContent does not break animateEnterExit exit animations**

After applying Step 2, run the behavioral tests from Step 0. Specifically verify:
- Animated deletion of a ForEach item produces a visible exit animation (fade/slide out)
- Remaining items retain their @State values across the deletion
- The `AnimatedVisibilityScope` receiver is available inside the `key()` block (i.e., `animateEnterExit` modifiers still function)

**If Step 2's `key()` wrapping inside AnimatedContent breaks `animateEnterExit` exit animations:**
1. **Revert** all inner `key()` changes from Step 2 (the `key(identityKey ?? i)` additions inside AnimatedContent content lambdas)
2. **Keep** the `contentKey` normalization from Step 1 (this is safe — it only changes how contentKey values are compared)
3. Document in the SUMMARY: "Inner `key()` wrapping inside AnimatedContent breaks `animateEnterExit` scope chain. Reverted to contentKey-only normalization. AnimatedContent paths rely on `contentKey` for identity and positional index for inner rendering. Future work may investigate `contentKey` enrichment with `identityKey` as an alternative."
4. Update the success criteria to reflect the contentKey-only outcome
5. Proceed to Step 4 (explicitResetKey checkpoint) with the contentKey-only approach

**If Step 2 passes (exit animations work correctly with inner key()):** proceed to Step 3.

**Step 3: Verify**

After applying changes:
1. Build: `just ios-build fuse-app` — must succeed
2. Build: `just android-build fuse-app` — must succeed (AnimatedContent is #if SKIP)

**Step 4: Evaluate explicitResetKey checkpoint — implement if necessary**

Per CONTEXT.md: "Phase 2 is also the checkpoint for evaluating whether .id() reset scoping needs to become a first-class field (explicitResetKey)."

After applying changes, assess:
- Does the nested key-group approach (outer identityKey for sibling matching, inner TagModifier(.id) key for state reset) work correctly for animated containers?
- Specifically: when a ForEach item has `.id(resetToken)`, does AnimatedContent's diff logic correctly distinguish the outer sibling identity (from `identityKey`) from the inner reset key (from `TagModifier(.id)`)?
- Test case: ForEach items with `.id()` modifier inside an animated VStack — change the `.id()` value for one item while keeping the ForEach identity stable. The item should reset its @State (inner key change) WITHOUT triggering an exit+enter animation (outer key unchanged).

**If the nested key-group approach is insufficient (AnimatedContent cannot distinguish sibling matching from state reset):**
1. **Implement `explicitResetKey`** as a third field on `IdentityKeyModifier`:
   - Add `let explicitResetKey: Any?` to `IdentityKeyModifier` (default `nil`)
   - Modify container loops to use `identityKey` for `key()` wrapping (sibling matching) and pass `explicitResetKey` to a nested `key()` inside the AnimatedContent content lambda (state reset)
   - Update `ForEach.identifiedRenderable` to read `.id()` from `TagModifier(.id)` and pass it as the `explicitResetKey` parameter
   - Update AnimatedContent `contentKey` to use `identityKey` (not `.id()` values) for diff logic
2. Add tests verifying: `.id()` value change resets state WITHOUT triggering exit animation; ForEach identity change triggers exit animation
3. Document the implementation rationale in SUMMARY

**If no issues are evident:** document that `explicitResetKey` is NOT needed at this time. Add assessment as a code comment near the AnimatedContent contentKey normalization, and document in the SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/jacob/Developer/src/github/jacobcxdev/swift-crossplatform && just ios-build fuse-app 2>&1 | tail -10 && just android-build fuse-app 2>&1 | tail -10</automated>
    <automated>cd /Users/jacob/Developer/src/github/jacobcxdev/swift-crossplatform && just android-test fuse-app 2>&1 | tail -20</automated>
  </verify>
  <done>
All 3 files updated (one of two outcomes):

**Outcome A (key() wrapping works):**
- Behavioral tests written and passing: animated deletion preserves remaining state, deleted items get exit animation, key() does not break AnimatedVisibilityScope (or manual verification documented if unit test not feasible)
- AnimatedContent contentKey normalized through normalizeKey() in VStack, HStack, ZStack
- Animated render loops use key(identityKey ?? i) with seenKeys guard in all 3 containers
- explicitResetKey checkpoint evaluated: either implemented (if needed) or deferred with rationale
- iOS and Android builds succeed
- Android tests pass (no regressions)

**Outcome B (key() breaks animateEnterExit — fallback):**
- Inner key() changes reverted; contentKey normalization from Step 1 retained
- AnimatedContent contentKey normalized through normalizeKey() in VStack, HStack, ZStack
- Animated render loops use positional identity only (no inner key() wrapping)
- Limitation documented in SUMMARY with rationale
- explicitResetKey checkpoint evaluated with contentKey-only approach
- iOS and Android builds succeed
- Android tests pass (no regressions)
  </done>
</task>

</tasks>

<verification>
1. `just ios-build fuse-app` — succeeds
2. `just android-build fuse-app` — succeeds
3. `just ios-build fuse-library` — succeeds
4. `just android-test fuse-app` — behavioral tests pass: animated deletion preserves remaining item state, deleted items receive exit animations, key() inside AnimatedContent does not break AnimatedVisibilityScope
5. Grep: `grep -rn "normalizeKey" forks/skip-ui/Sources/SkipUI/SkipUI/Containers/ --include="*.swift"` — shows normalization in VStack, HStack, ZStack animated paths
6. Grep: `grep -rn "composeKeyValue" forks/skip-ui/Sources/SkipUI/SkipUI/ --include="*.swift"` — no new usage of deprecated function (only deprecated declarations and lazy-path legacy usage)
7. If any behavioral test could not be written as a unit test, manual emulator verification documented in SUMMARY with steps to reproduce

**GREEN phase verification (Plan 01-03 tests that should now PASS):**
8. Run `just ios-test fuse-app` — TCA TestStore tests for the following section should now pass:
   - **Section 3** (AnimatedContent): animated deletion preserves remaining items' state AND deleted item gets exit animation

**HUMAN-ONLY: Do not orchestrate the emulator via adb. Manual UI verification is performed by the human between waves. Do not waste tokens on adb commands, screenshot capture, or emulator automation.**
</verification>

<success_criteria>
**Mandatory (both outcomes):**
- AnimatedContent contentKey uses normalizeKey() instead of raw SwiftHashable from idMap (Step 1)
- explicitResetKey checkpoint evaluated and documented — either implemented or deferred with rationale (Step 4)
- iOS and Android builds succeed with no regressions
- Android tests pass

**Outcome A (key() wrapping works — preferred):**
- Behavioral tests prove: animated deletion preserves remaining items' state AND deleted item gets exit animation
- Behavioral tests prove (or manual verification documents): key() inside AnimatedContent content lambda does not break AnimatedVisibilityScope chain
- All animated render loops in VStack/HStack/ZStack use key(identityKey ?? i) with seenKeys duplicate-key guard

**Outcome B (key() breaks animateEnterExit — fallback):**
- Inner key() changes reverted; only contentKey normalization retained
- Limitation documented in SUMMARY: animated container loops rely on positional identity for inner rendering
- contentKey-only alternative documented as the working approach
</success_criteria>

<pfw_alignment>
**Point-Free skill alignment (MANDATORY):** This plan writes behavioral tests for animated containers that may exercise TCA-managed state (e.g., ForEach over TCA IdentifiedArray with animated deletion). Before writing test views or TestStore assertions, consult:
- `/pfw-composable-architecture` — for correct TestStore usage, `withAnimation` + `store.send()` patterns, IdentifiedArray mutation patterns
- `/pfw-swift-navigation` — if any test involves NavigationStack animation

Ensure tests use `@Dependency(\.withAnimation)` or the correct TCA animation pattern rather than raw `withAnimation` closures where TCA state is involved.
</pfw_alignment>

<diagnostic_logging>
**Diagnostic logging:** If any diagnostic logging is required during implementation, use the existing `FUSE_IDENTITY_DEBUG` compiler flag pattern. Log via `android.util.Log.d("ComposeIdentity", ...)` guarded by `#if FUSE_IDENTITY_DEBUG`. Do NOT introduce new debug flag names or logging mechanisms.
</diagnostic_logging>

<output>
After completion, create `.planning/phases/18.1-implement-canonical-view-identity-system/18.1-06-SUMMARY.md`

**Traceability note:** VIEWID-05 and VIEWID-09 are referenced in this plan's frontmatter but are not yet defined in REQUIREMENTS.md. During phase closure, add these requirement IDs to `.planning/REQUIREMENTS.md` with descriptions matching the must_haves truths they trace to.

<checkpoint>
**HUMAN INTERVENTION REQUIRED — Wave 3 (Plan 06 — AnimatedContent) complete.**

Before proceeding to Wave 4 (Plan 07 — Lazy Containers):
1. **Run `/clear`** to compact context — minimises token usage for Wave 4 execution.
2. **Verify Identity tab on Android emulator** — confirm that Section 3 (AnimatedContent) now shows CORRECT behaviour:
   - Animated deletion preserves remaining items' counter state
   - Deleted items get visible exit animation (fade/slide out)
   - key() inside AnimatedContent does not break animateEnterExit
3. **Confirm TCA tests for Section 3 now PASS** — `just ios-test fuse-app`

Once confirmed, proceed to Wave 4: Plan 07 (Lazy Containers).
</checkpoint>
</output>
</content>
</invoke></output>
