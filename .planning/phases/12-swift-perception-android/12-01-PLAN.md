---
phase: 12-swift-perception-android
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - forks/swift-perception/Sources/PerceptionCore/SwiftUI/WithPerceptionTracking.swift
  - forks/swift-perception/Package.swift
autonomous: true
requirements: [OBS-29, OBS-30]
must_haves:
  truths:
    - "PerceptionRegistrar compiles on Android and delegates to ObservationRegistrar"
    - "WithPerceptionTracking compiles on Android as a View passthrough"
    - "Perception checking (MachO/dyld introspection) remains Darwin-only"
    - "Bindable.swift remains gated out on Android (depends on SwiftUI ObservedObject)"
  artifacts:
    - path: "forks/swift-perception/Sources/PerceptionCore/Perception/PerceptionRegistrar.swift"
      provides: "PerceptionRegistrar Android compilation with ObservationRegistrar delegation"
      contains: "public struct PerceptionRegistrar"
    - path: "forks/swift-perception/Sources/PerceptionCore/SwiftUI/WithPerceptionTracking.swift"
      provides: "WithPerceptionTracking Android passthrough View implementation"
      contains: "os(Android)"
  key_links:
    - from: "PerceptionRegistrar.swift"
      to: "ObservationRegistrar"
      via: "canImport(Observation) runtime delegation"
      pattern: "rawValue = ObservationRegistrar\\(\\)"
    - from: "WithPerceptionTracking.swift (Android)"
      to: "SkipFuseUI.View"
      via: "import SkipFuseUI, struct conformance"
      pattern: "import SkipFuseUI"
---

<objective>
Enable swift-perception's PerceptionRegistrar and WithPerceptionTracking on Android by removing overly-broad `!os(Android)` guards and providing a minimal Android-specific WithPerceptionTracking passthrough.

Purpose: TCA depends on `Perceptible` conformances and `PerceptionRegistrar` API surface. These types must compile and function on Android for TCA's binding/scoping infrastructure to work.

Output: Modified PerceptionRegistrar.swift and WithPerceptionTracking.swift in the swift-perception fork.
</objective>

<execution_context>
@/Users/jacob/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jacob/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-swift-perception-android/12-RESEARCH.md
@forks/swift-perception/Sources/PerceptionCore/Perception/PerceptionRegistrar.swift
@forks/swift-perception/Sources/PerceptionCore/SwiftUI/WithPerceptionTracking.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable PerceptionRegistrar on Android</name>
  <files>forks/swift-perception/Sources/PerceptionCore/Perception/PerceptionRegistrar.swift</files>
  <action>
VERIFY ONLY — no edits expected. Confirm PerceptionRegistrar already compiles on Android without changes.

Original analysis considered modifying PerceptionRegistrar.swift guards, but determined no changes are needed:

1. **Line 3 — SwiftUI import guard**: Change from `#if canImport(SwiftUI) && !os(Android)` to `#if canImport(SwiftUI) && !os(Android)` (keep as-is — SwiftUI is not available on Android, but the rest of the struct must compile). The fix is that the struct itself is NOT inside this guard — only the import is. Verify the struct `PerceptionRegistrar` is already outside the guard (it is — the guard only covers `import SwiftUI`).

2. **Line 7 — MachO import**: Keep `#if DEBUG && canImport(SwiftUI) && !os(Android)` for `import MachO` (Darwin-only).

3. **Line 24 — perceptionChecks field**: Keep `#if DEBUG && canImport(SwiftUI) && !os(Android)` (perception checking cache is Darwin-only, uses MachO introspection).

4. **Line 57 — access() method `@_transparent` attribute**: Change `#if DEBUG && canImport(SwiftUI) && !os(Android)` to `#if DEBUG && canImport(SwiftUI) && !os(Android)` (keep — `@_transparent` paired with `check()` which is Darwin-only). BUT the `check()` call inside access() at line 64 must also stay guarded.

5. **Line 253 — perception checking extension** (`check()`, `isSwiftUI()`, `RangeSet`, etc.): Keep `#if DEBUG && canImport(SwiftUI) && !os(Android)` — this entire block is Darwin-only MachO/dyld introspection.

**ACTUAL CHANGES NEEDED:** After careful analysis, the PerceptionRegistrar struct and its core methods (access, willSet, didSet, withMutation) are already NOT gated by `!os(Android)` — they compile on all platforms. The `canImport(SwiftUI) && !os(Android)` guards only affect:
- The `import SwiftUI` statement (line 3)
- The `import MachO` statement (line 7)
- The `perceptionChecks` cache field (line 24)
- The `@_transparent` attribute on access() (line 57)
- The `check()` call inside access() (line 64)
- The perception checking extension block (line 253+)

All of these are correctly Darwin-only. **No changes are needed to PerceptionRegistrar.swift** — it already compiles on Android. The `#if canImport(Observation)` path at line 42-46 will be taken on Android (since libswiftObservation.so is available), creating a native `ObservationRegistrar` as the backing store.

**Verify this by confirming:** The struct `PerceptionRegistrar` at line 19 is NOT inside any `#if` guard. The init at line 38 is NOT guarded. The access/willSet/didSet/withMutation methods starting at lines 60, 90, 117, 146 are NOT guarded. Only the perception-checking additions (check(), isSwiftUI(), etc.) are guarded.

If the above analysis is correct (it is based on the file reading), this task becomes a **verification-only task**: Confirm PerceptionRegistrar already compiles on Android by running `make darwin-build EXAMPLE=fuse-library` (which validates Swift compilation including the perception import chain).

Actually wait — there IS one issue. On Android, `canImport(SwiftUI)` is false. The `#if canImport(SwiftUI) && !os(Android)` guard on line 3 means `import SwiftUI` is excluded. But the perception checking at line 57-66 uses `#if DEBUG && canImport(SwiftUI) && !os(Android)` which will be false on Android. This is correct — no SwiftUI, no perception checking.

The PerceptionRegistrar struct itself, its init, and core methods are all outside any platform guard. They compile on all platforms. On Android with `canImport(Observation)` true, the init creates `ObservationRegistrar()`. The Perceptible-constrained methods delegate to ObservationRegistrar when the subject conforms to Observable.

**Conclusion: No code changes needed for PerceptionRegistrar.swift.** Just verify it compiles.

Run: `cd /Users/jacob/Developer/src/github/jacobcxdev/swift-crossplatform/examples/fuse-library && swift build 2>&1 | tail -5` to confirm compilation.
  </action>
  <verify>
    <automated>cd /Users/jacob/Developer/src/github/jacobcxdev/swift-crossplatform/examples/fuse-library && swift build 2>&1 | tail -20</automated>
    <manual>Confirm PerceptionRegistrar struct is outside all #if guards and compiles on all platforms</manual>
  </verify>
  <done>PerceptionRegistrar compiles and delegates to ObservationRegistrar on Android via canImport(Observation) path. No code changes needed — verified existing guards are correctly scoped.</done>
</task>

<task type="auto">
  <name>Task 2: Add WithPerceptionTracking Android implementation</name>
  <files>forks/swift-perception/Sources/PerceptionCore/SwiftUI/WithPerceptionTracking.swift</files>
  <action>
**Pre-check: Verify Package.swift dependency exists.**
The swift-perception fork's Package.swift already declares SkipFuseUI as an Android-conditional dependency on the PerceptionCore target:
```swift
.product(name: "SkipFuseUI", package: "skip-fuse-ui", condition: .when(platforms: [.android])),
```
Confirm this line exists in `forks/swift-perception/Package.swift` (PerceptionCore target dependencies). If missing, add it following the pattern above. This is required for `import SkipFuseUI` to resolve on Android.

**Main change:**
The entire WithPerceptionTracking.swift is gated by `#if canImport(SwiftUI) && !os(Android)`. On Android, `canImport(SwiftUI)` is already false, so the entire file is excluded regardless of the `!os(Android)` check.

Add an Android-specific `WithPerceptionTracking` implementation at the END of the file (after the `#endif` that closes the main `#if canImport(SwiftUI) && !os(Android)` block at line 224).

Add this block:

```swift
#if os(Android)
import SkipFuseUI

/// On Android, `WithPerceptionTracking` is a transparent passthrough.
/// The observation bridge (skip-android-bridge) handles Compose recomposition
/// directly via JNI, so no SwiftUI `@State`-based change tracking is needed.
/// This type exists so that user code wrapping view bodies in
/// `WithPerceptionTracking { }` compiles on Android.
public struct WithPerceptionTracking<Content: View>: View {
    private let content: () -> Content

    public init(@ViewBuilder content: @escaping () -> Content) {
        self.content = content
    }

    public init(content: @escaping @autoclosure () -> Content) {
        self.content = content
    }

    public var body: some View {
        #if DEBUG
        _PerceptionLocals.$isInPerceptionTracking.withValue(true) {
            content()
        }
        #else
        content()
        #endif
    }
}
#endif
```

Key decisions:
- Use `import SkipFuseUI` (not SwiftUI — `canImport(SwiftUI)` is false on Android)
- Two init overloads: `@ViewBuilder` closure and `@autoclosure` (matching Darwin API surface used by TCA)
- In DEBUG: set `_PerceptionLocals.isInPerceptionTracking = true` so TCA's perception-checking debug paths know we're inside tracking
- In RELEASE: pure passthrough for zero overhead
- Only conform to `View` — other Darwin content types (Scene, Commands, ToolbarContent, etc.) are not relevant on Android
  </action>
  <verify>
    <automated>cd /Users/jacob/Developer/src/github/jacobcxdev/swift-crossplatform/examples/fuse-library && swift build 2>&1 | tail -20 && cd /Users/jacob/Developer/src/github/jacobcxdev/swift-crossplatform && make android-build EXAMPLE=fuse-library 2>&1 | tail -20</automated>
    <manual>Verify WithPerceptionTracking compiles on macOS (no regression) and the Android build validates the #if os(Android) code path including the SkipFuseUI import</manual>
  </verify>
  <done>WithPerceptionTracking has an Android-specific passthrough implementation using SkipFuseUI's View. DEBUG builds set isInPerceptionTracking for TCA debug paths. macOS build still passes (no regression). Android build passes (SkipFuseUI import resolves via Package.swift conditional dependency).</done>
</task>

</tasks>

<verification>
1. `make darwin-build EXAMPLE=fuse-library` passes — no Swift compilation regressions
2. `make android-build EXAMPLE=fuse-library` passes — Android code path compiles (validates SkipFuseUI import and WithPerceptionTracking Android impl)
3. `make darwin-test EXAMPLE=fuse-library` passes — existing 227 tests still pass
4. WithPerceptionTracking.swift has both Darwin (`#if canImport(SwiftUI) && !os(Android)`) and Android (`#if os(Android)`) implementations
5. PerceptionRegistrar.swift is unchanged — already compiles on all platforms
6. forks/swift-perception/Package.swift has SkipFuseUI as Android-conditional dependency on PerceptionCore target
</verification>

<success_criteria>
- PerceptionRegistrar confirmed to compile on Android (delegates to ObservationRegistrar)
- WithPerceptionTracking has Android passthrough that compiles
- No macOS/iOS regression in existing tests
- OBS-29 partially addressed (PerceptionRegistrar compiles)
- OBS-30 addressed (withPerceptionTracking free function already works; WithPerceptionTracking view now available)
</success_criteria>

<output>
After completion, create `.planning/phases/12-swift-perception-android/12-01-SUMMARY.md`
</output>
