---
phase: 13-api-parity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/fuse-library/Tests/NavigationTests/PresentationParityTests.swift
  - examples/fuse-library/Tests/NavigationTests/TextStateButtonStateTests.swift
autonomous: true
requirements:
  - NAV-05
  - NAV-07
  - NAV-08
must_haves:
  truths:
    - "TCA sheet presentation with $store.scope binding dispatches actions correctly"
    - "TCA fullScreenCover presentation with $store.scope binding dispatches actions correctly"
    - "Popover fallback to sheet is documented and tested at data layer"
    - "TextState preserves plain text content on Android"
    - "ButtonState with role renders correct action and role"
  artifacts:
    - path: "examples/fuse-library/Tests/NavigationTests/PresentationParityTests.swift"
      provides: "Sheet, fullScreenCover, popover presentation data-layer tests"
      contains: "fullScreenCover"
    - path: "examples/fuse-library/Tests/NavigationTests/TextStateButtonStateTests.swift"
      provides: "TextState and ButtonState Android parity tests"
      contains: "TextState"
  key_links:
    - from: "PresentationParityTests.swift"
      to: "PresentationReducer"
      via: "TestStore send/receive for presentation lifecycle"
      pattern: "store\\.send.*present|store\\.receive.*dismiss"
---

<objective>
Verify TCA presentation modifiers (sheet, fullScreenCover, popover) and TextState/ButtonState work at the data layer on Android, closing NAV-05, NAV-07, NAV-08 parity gaps.

Purpose: The modern TCA presentation patterns (`$store.scope` + `.sheet(item:)` / `.fullScreenCover(item:)`) are unguarded in Store+Observation.swift. This plan verifies the data-layer lifecycle (present -> child action -> dismiss) works correctly via TestStore, confirming these APIs are Android-ready without UI-level testing.

Output: Two new test files proving presentation lifecycle and TextState/ButtonState parity.
</objective>

<execution_context>
@/Users/jacob/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jacob/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-api-parity/13-RESEARCH.md
@examples/fuse-library/Tests/NavigationTests/PresentationTests.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add presentation parity tests for sheet, fullScreenCover, and popover</name>
  <files>examples/fuse-library/Tests/NavigationTests/PresentationParityTests.swift</files>
  <action>
Create PresentationParityTests.swift with Swift Testing `@Suite` / `@Test` pattern. Gate with `#if !SKIP`.

Test the data-layer lifecycle for each presentation type using `@Presents` + `PresentationAction`:

**Sheet test (NAV-05):**
- Define a parent `@Reducer struct SheetFeature` with `@Presents var child: ChildFeature.State?` and action `case child(PresentationAction<ChildFeature.Action>)`.
- Define a simple `ChildFeature` reducer with an incrementable counter.
- TestStore: send action to present (sets child to non-nil), verify state.
- TestStore: send child action through `.child(.presented(.increment))`, verify child state updates.
- TestStore: send `.child(.dismiss)`, verify child state becomes nil.

**FullScreenCover test (NAV-08):**
- Same pattern as sheet but with a `FullScreenFeature` parent. The data-layer lifecycle is identical (both use `@Presents` + `PresentationAction`). The difference is only in the SwiftUI modifier used (`.fullScreenCover(item:)` vs `.sheet(item:)`), which is a view-layer concern.
- TestStore: present, send child action, dismiss. Verify state transitions.

**Popover test (NAV-07):**
- Same data-layer pattern. Popover falls back to sheet on Android (already implemented in Popover.swift). The reducer lifecycle is identical.
- TestStore: present, interact, dismiss.

All three tests prove the `@Presents` + `PresentationAction.dismiss` lifecycle works correctly. Since the modern `$store.scope` binding path (Store+Observation.swift) has no Android guards, and skip-fuse-ui provides the view-layer modifiers, this data-layer validation is sufficient for Android parity.

Use file-scope types for all `@Reducer` structs (macro expansion constraint). Reference existing PresentationTests.swift patterns for TestStore usage.
  </action>
  <verify>
    <automated>cd /Users/jacob/Developer/src/github/jacobcxdev/swift-crossplatform/examples/fuse-library && swift test --filter PresentationParityTests 2>&1 | tail -10</automated>
  </verify>
  <done>Sheet, fullScreenCover, and popover presentation lifecycles pass TestStore validation. Present -> child action -> dismiss cycle works correctly for all three.</done>
</task>

<task type="auto">
  <name>Task 2: Add TextState and ButtonState parity tests</name>
  <files>examples/fuse-library/Tests/NavigationTests/TextStateButtonStateTests.swift</files>
  <action>
Create TextStateButtonStateTests.swift with Swift Testing `@Suite` / `@Test` pattern. Gate with `#if !SKIP`.

**TextState tests (SC-4):**
- Verify `TextState("Hello")` creates a text state with correct plain text content.
- Verify `TextState` concatenation (`+` operator) produces correct combined plain text.
- Verify `TextState` with formatting (`.bold()`, `.italic()`) still contains the original text (formatting is cosmetic, plain text accessible via internal `_plainText`).
- Verify `AlertState` with `TextState` title and message preserves text content.

**ButtonState tests (SC-4):**
- Verify `ButtonState(role: .destructive, action: .delete) { TextState("Delete") }` creates button with correct role and action.
- Verify `ButtonState(role: .cancel) { TextState("Cancel") }` creates cancel button.
- Verify `ButtonState(action: .confirm) { TextState("OK") }` creates default-role button.
- Verify `AlertState` with multiple `ButtonState` entries has correct button count and actions.

These tests validate the data structures that drive alert/dialog rendering. On Android, `TextState` renders as plain text (bold/italic/color silently dropped) and `ButtonState` correctly maps roles to button styling. The data layer is platform-independent.

Import SwiftUINavigation (re-exported by ComposableArchitecture) for TextState/ButtonState types.
  </action>
  <verify>
    <automated>cd /Users/jacob/Developer/src/github/jacobcxdev/swift-crossplatform/examples/fuse-library && swift test --filter TextStateButtonStateTests 2>&1 | tail -10</automated>
  </verify>
  <done>TextState preserves plain text through formatting operations. ButtonState correctly stores role and action. AlertState composes TextState + ButtonState correctly.</done>
</task>

</tasks>

<verification>
1. `swift test --filter PresentationParityTests` passes
2. `swift test --filter TextStateButtonStateTests` passes
3. Full test suite: `cd examples/fuse-library && swift test` passes with no regressions
</verification>

<success_criteria>
- Sheet presentation lifecycle (present/interact/dismiss) verified via TestStore
- FullScreenCover presentation lifecycle verified via TestStore
- Popover presentation lifecycle verified via TestStore
- TextState plain text preservation verified
- ButtonState role and action storage verified
- No iOS regressions
</success_criteria>

<output>
After completion, create `.planning/phases/13-api-parity/13-02-SUMMARY.md`
</output>
