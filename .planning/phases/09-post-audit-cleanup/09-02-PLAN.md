# Plan 09-02: Documentation Sync

**Phase:** 09 — Post-Audit Cleanup
**Wave:** 2 of 3
**Goal:** Update REQUIREMENTS.md traceability table to reflect actual implementation status and document the Perception bypass limitation.
**Depends on:** 09-01 (test fixes confirm implementation completeness)

---

## Canonical Pattern References

- **`/pfw-perception`** — Perception registrar behaviour, `@Perceptible` vs native `@Observable`, platform differences
- **`/pfw-composable-architecture`** — TCA requirement categories (TCA-*, DEP-*) and their test evidence locations

---

## Tasks

### Task 1: Update REQUIREMENTS.md checkboxes (127 stale items)

**Files:**
- `.planning/REQUIREMENTS.md`

**Changes:**
Bulk-update the traceability table: mark all 127 requirements that are implemented and tested from `[ ] Pending` to `[x] Complete`. Cross-reference against:

| Category | Stale Count | Evidence Source |
|----------|-------------|-----------------|
| OBS (Observation) | 30 | Phase 1-2 verification reports + ObservationTests |
| TCA (Core + State) | ~19 | Phase 3-4 verification reports + TCATests, SharingTests |
| SHR (Shared State) | 14 | Phase 4 verification + SharingTests |
| NAV (Navigation) | 16 | Phase 5 verification + NavigationTests |
| CP (CasePaths) | 8 | Phase 2 verification + FoundationTests |
| SD (SQLiteData) | 12 | Phase 6 verification + DatabaseTests |
| CD (CustomDump) | 5 | Phase 2 verification + FoundationTests |
| UI (SwiftUI) | 8 | Phase 5 verification + SwiftUIPatternTests |
| SPM (Build) | 6 | Phase 1 verification + Makefile targets |
| TEST (Testing) | partial | Phase 7 verification + TestStoreTests |

For each requirement, verify the test file and line reference before marking complete. Update the "Test File" and "Notes" columns where blank.

**Why:** 127 of 184 requirements show `[ ]` despite being fully implemented — misleading progress tracking identified in MILESTONE-AUDIT.md.

**Verification:** Count of `[x]` entries equals 184. No `[ ]` entries remain except any genuinely incomplete items (expect 0).

### Task 2: Document Perception bypass limitation

**Files:**
- `.planning/STATE.md` (Pending Todos section)
- `examples/fuse-app/README.md` (Known Limitations section)

**Changes:**
Add a clear note documenting the Perception bypass per `/pfw-perception` patterns:

> **Perception bypass on Android:** `PerceptionRegistrar` delegates to native `ObservationRegistrar`, bypassing the bridge's `recordAccess` hooks. Raw `@Perceptible` views (without TCA) will not trigger Compose recomposition. This is safe for all TCA usage (TCA uses `ObservationStateRegistrar` which routes through the bridge directly). Only affects code using `swift-perception`'s `@Perceptible` macro directly for view driving outside TCA.

Update STATE.md pending todo to mark this as "Documented" rather than "Open".

**Why:** Identified by Gemini verifier in Phase 3. Currently listed as open todo but only needs documentation, not a code fix.

**Verification:** README.md Known Limitations section contains the Perception bypass note. STATE.md todo updated.

---

## Verification

1. `grep -c '\[x\]' .planning/REQUIREMENTS.md` returns 184
2. README.md Known Limitations section documents Perception bypass
3. STATE.md Perception bypass todo marked Documented
