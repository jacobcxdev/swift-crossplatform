# Plan 09-03: Android Verification

**Phase:** 09 — Post-Audit Cleanup
**Wave:** 3 of 3
**Goal:** Verify Android test execution works after the xctest-dynamic-overlay fork fix from 09-01, capture results, and close all deferred Android TODOs.
**Depends on:** 09-01 (xctest-dynamic-overlay fix must land first)

---

## Canonical Pattern References

- **`/pfw-issue-reporting`** — `isTesting` detection on Android (dlsym, process args, env vars), platform import guards
- **`/pfw-composable-architecture`** — TestStore Android execution expectations, effect scheduling without MainSerialExecutor
- **`/pfw-testing`** — Cross-platform test conventions, `withKnownIssue` for platform-specific gaps

---

## Tasks

### Task 1: Run `skip android test` for fuse-library

**Commands:**
```bash
cd examples/fuse-library && skip android test 2>&1 | tee .planning/phases/09-post-audit-cleanup/android-test-results.log
```

**Expected:** All 6 test targets execute on Android via Robolectric:
- ObservationTests (bridge semantics, stress tests)
- FoundationTests (CasePaths, CustomDump, IssueReporting, IdentifiedCollections)
- TCATests (TestStore, edge cases)
- SharingTests (shared state, observation)
- NavigationTests (stack, presentation, SwiftUI patterns)
- DatabaseTests (StructuredQueries, SQLiteData)

**Success criteria:** Test output shows pass/fail counts per target. Any failures are investigated and either fixed or documented with `withKnownIssue` per `/pfw-testing` patterns.

**Why:** TEST-10 Tier 2 (Android emulator validation) was blocked by xctest-dynamic-overlay. After 09-01 fix, this should unblock.

### Task 2: Run `skip android test` for fuse-app

**Commands:**
```bash
cd examples/fuse-app && skip android test 2>&1 | tee .planning/phases/09-post-audit-cleanup/android-app-test-results.log
```

**Expected:** FuseAppIntegrationTests execute on Android. All 30 tests pass (including the 2 DatabaseFeature tests fixed in 09-01).

### Task 3: Update STATE.md with results

**Files:**
- `.planning/STATE.md`

**Changes:**
- Update "xctest-dynamic-overlay Android test build" todo: mark Resolved with test results
- Update "Android runtime verification" todo: mark Resolved or document remaining gaps
- Update "Android UI rendering validation" todo: document Robolectric coverage vs emulator gaps
- Update phase/plan counters to reflect Phase 9 progress

### Task 4: Capture test evidence

**Files:**
- `.planning/phases/09-post-audit-cleanup/09-03-SUMMARY.md` (created during execution)

**Changes:**
Record:
- Exact pass/fail counts for each test target on Android
- Any platform-specific failures with root cause analysis
- Comparison of macOS vs Android test results
- Resolution status for each deferred Android TODO from STATE.md

---

## Verification

1. `skip android test` exits successfully for both fuse-library and fuse-app
2. Test result logs captured in `.planning/phases/09-post-audit-cleanup/`
3. STATE.md Android-related TODOs updated with resolution status
4. Any new platform-specific failures documented with `withKnownIssue` per `/pfw-testing`
