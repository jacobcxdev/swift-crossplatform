---
phase: 07-integration
plan: 04
type: execute
wave: 4
depends_on: ["07-02", "07-03"]
files_modified:
  - docs/FORKS.md
  - examples/fuse-library/Package.swift
  - examples/fuse-library/Tests/ObservationTests/ObservationTests.swift
  - examples/fuse-library/Tests/FoundationTests/CasePathsTests.swift
  - examples/fuse-library/Tests/FoundationTests/IdentifiedCollectionsTests.swift
  - examples/fuse-library/Tests/FoundationTests/CustomDumpTests.swift
  - examples/fuse-library/Tests/FoundationTests/IssueReportingTests.swift
  - examples/fuse-library/Tests/TCATests/StoreReducerTests.swift
  - examples/fuse-library/Tests/TCATests/EffectTests.swift
  - examples/fuse-library/Tests/TCATests/ObservableStateTests.swift
  - examples/fuse-library/Tests/TCATests/BindingTests.swift
  - examples/fuse-library/Tests/TCATests/DependencyTests.swift
  - examples/fuse-library/Tests/SharingTests/SharedPersistenceTests.swift
  - examples/fuse-library/Tests/SharingTests/SharedBindingTests.swift
  - examples/fuse-library/Tests/SharingTests/SharedObservationTests.swift
  - examples/fuse-library/Tests/NavigationTests/NavigationTests.swift
  - examples/fuse-library/Tests/NavigationTests/NavigationStackTests.swift
  - examples/fuse-library/Tests/NavigationTests/PresentationTests.swift
  - examples/fuse-library/Tests/NavigationTests/UIPatternTests.swift
  - examples/fuse-library/Tests/DatabaseTests/StructuredQueriesTests.swift
  - examples/fuse-library/Tests/DatabaseTests/SQLiteDataTests.swift
autonomous: true
requirements: [DOC-01]

must_haves:
  truths:
    - "FORKS.md documents all 17 forks with upstream version, commits ahead, key changes, and rationale"
    - "Each fork change is classified as upstreamable, fork-only, or conditional"
    - "Upstreamable changes include draft PR descriptions for upstream maintainers"
    - "A Mermaid dependency graph shows fork-to-fork relationships"
    - "All existing tests are reorganised into feature-aligned targets with zero test loss"
    - "Test count before reorganisation equals test count after reorganisation"
  artifacts:
    - path: "docs/FORKS.md"
      provides: "Complete fork documentation with dependency graph and PR drafts"
      min_lines: 300
    - path: "examples/fuse-library/Package.swift"
      provides: "Feature-aligned test targets replacing per-module test targets"
      contains: "ObservationTests"
  key_links:
    - from: "docs/FORKS.md"
      to: "forks/"
      via: "git metadata extracted per-fork"
      pattern: "## "
    - from: "examples/fuse-library/Package.swift"
      to: "examples/fuse-library/Tests/"
      via: "test target declarations referencing feature-aligned directories"
      pattern: "\\.testTarget"
---

<objective>
Document all 17 forks with upstream version, commits ahead, key changes, rationale, upstream PR candidates with draft descriptions, and a Mermaid dependency graph. Reorganise the existing fuse-library test suite into feature-aligned targets (D3).

Purpose: Makes the fork strategy transparent for evaluators and developers (D11). Aligns test structure with modular feature architecture (D3).
Output: `docs/FORKS.md` with structured per-fork sections and dependency visualization. 6 feature-aligned test targets replacing 20 per-module targets.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-integration/07-RESEARCH.md
@.planning/phases/07-integration/07-CONTEXT.md
@.planning/phases/07-integration/research/R5b-forks-deep.md
@.planning/phases/07-integration/research/R6-test-reorganisation.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate comprehensive fork documentation (DOC-01)</name>
  <files>
    docs/FORKS.md
  </files>
  <action>
**Use R5b data — do NOT re-run git commands.** The research has complete per-fork metadata (157 total commits across 17 forks, all classified).

**FORKS.md structure (per D11):**

```markdown
# Fork Documentation

## Overview
- 17 forks tracking `dev/swift-crossplatform` branch
- 157 total fork-specific commits
- 3 remote configurations (Skip-ecosystem, Point-Free with flote-works, single-remote)

## Dependency Graph
[Mermaid diagram showing fork → fork relationships]

## Fork Details
### [fork-name]
- **Upstream:** [org/repo] @ [version/commit]
- **Branch:** dev/swift-crossplatform
- **Commits ahead:** N
- **Rebase risk:** LOW/MEDIUM/HIGH/VERY HIGH

#### Key Changes
| File | Change | Classification |
|------|--------|---------------|
| ... | ... | upstreamable / fork-only / conditional |

#### Rationale
[Why these changes are needed]

#### Upstream PR Candidates
[For upstreamable changes: draft PR title and description]
```

**17 forks to document (from R5b):**

1. **skip-android-bridge** — ObservationRecording record-replay, JNI exports, swiftThreadingFatal stub (163 lines). Classification: fork-only (Android-specific bridge).

2. **skip-ui** — ViewObservation hooks around Evaluate(), nativeEnable/startRecording/stopAndObserve JNI calls (27 lines). Classification: fork-only (bridge integration).

3. **swift-composable-architecture** — 39 commits, 55 source files. ObservationStateRegistrar Android path, TestStore effectDidSubscribe fallback, extensive #if os(Android) guards. Classification: mixed (some upstreamable, most conditional). VERY HIGH rebase risk.

4. **swift-navigation** — 27 commits, 13 source files. TextState/ButtonState bridge extensions, presentation guards. Classification: conditional. HIGH rebase risk.

5. **swift-sharing** — 10 source files. FileStorageKey Android enablement, AppStorageKey Android path, SharedSubscription no-ops. Classification: conditional. HIGH rebase risk.

6. **swift-dependencies** — OpenURL/AppEntryPoint guards, NavigationID Android path. Classification: mostly upstreamable.

7. **swift-perception** — PerceptionRegistrar passthrough to native ObservationRegistrar on Android. Classification: conditional.

8. **swift-clocks** — SwiftUI EnvironmentKey guard. Classification: upstreamable.

9. **combine-schedulers** — SwiftUI guard + OpenCombine support. Classification: upstreamable.

10. **xctest-dynamic-overlay** — isTesting Android dlsym detection. Classification: upstreamable.

11. **swift-custom-dump** — SwiftUI guard. Classification: upstreamable.

12. **swift-snapshot-testing** — SwiftUI/UIKit guards for Android. Classification: conditional.

13. **swift-case-paths** — Zero fork-specific commits (stale snapshot). Classification: none needed.

14. **swift-identified-collections** — Zero fork-specific commits (stale snapshot). Classification: none needed.

15. **swift-structured-queries** — SQLite header bundling with __ANDROID__ guard. Classification: upstreamable.

16. **GRDB.swift** — SQLite header bundling with __ANDROID__ guard. Classification: upstreamable.

17. **sqlite-data** — Re-exports StructuredQueriesSQLite + GRDB, dependency injection. Classification: conditional.

**Upstream PR candidates (Tier 1 — likely accepted, from R5b):**
For each, include a draft PR description explaining the change to upstream maintainers:
- `xctest-dynamic-overlay`: "Add Android `isTesting` detection via dlsym"
- `swift-custom-dump`: "Guard SwiftUI import for non-Apple platforms"
- `combine-schedulers`: "Guard SwiftUI import + add OpenCombine support"
- `swift-dependencies`: "Guard OpenURL/AppEntryPoint for non-Apple platforms"
- `swift-clocks`: "Guard SwiftUI EnvironmentKey for non-Apple platforms"

**Mermaid dependency graph (from R5b fork relationships):**
Show which forks depend on which. Key chain: skip-android-bridge → skip-ui → swift-composable-architecture. Point-Free dependency tree: xctest-dynamic-overlay → swift-case-paths → swift-identified-collections → ... → swift-composable-architecture.

**3-fork observation bridge chain** highlighted with distinct color/style in the graph.
  </action>
  <verify>
Verify `docs/FORKS.md` exists and contains:
- All 17 fork sections
- Mermaid dependency graph
- At least 5 upstream PR candidate sections with draft descriptions
- Change classifications for every fork
- Rebase risk assessment for high-change forks
  </verify>
  <done>docs/FORKS.md documents all 17 forks with upstream versions, commits ahead, key changes with classifications, rationale, upstream PR drafts, and a rendered Mermaid dependency graph.</done>
</task>

<task type="auto">
  <name>Task 2: Reorganise tests into feature-aligned targets (D3, multi-pass)</name>
  <files>
    examples/fuse-library/Package.swift
    examples/fuse-library/Tests/ (multiple directories)
  </files>
  <action>
**Implement D3: Test reorganisation into feature-aligned targets.** This is a risk-minimised, multi-pass operation. Zero tests may be lost.

**Current state (from R6):** 20 test targets, 22 files, 226 test methods. ObservationTrackingTests (7 tests) are exact duplicates — net 219 unique tests across 19 meaningful targets.

**Target feature-aligned structure (6 groups):**

| Feature Group | New Target Name | Source Targets (to merge) | Test Files |
|---|---|---|---|
| Observation | `ObservationTests` | FuseLibraryTests (ObservationTests.swift only), ObservationTrackingTests (DELETE — redundant) | ObservationTests.swift, FuseLibraryTests.swift, XCSkipTests.swift |
| Foundation | `FoundationTests` | CasePathsTests, IdentifiedCollectionsTests, CustomDumpTests, IssueReportingTests | CasePathsTests.swift, IdentifiedCollectionsTests.swift, CustomDumpTests.swift, IssueReportingTests.swift |
| TCA Core | `TCATests` | StoreReducerTests, EffectTests, ObservableStateTests, BindingTests, DependencyTests | StoreReducerTests.swift, EffectTests.swift, ObservableStateTests.swift, BindingTests.swift, DependencyTests.swift |
| Sharing | `SharingTests` | SharedPersistenceTests, SharedBindingTests, SharedObservationTests | SharedPersistenceTests.swift, SharedBindingTests.swift, SharedObservationTests.swift |
| Navigation | `NavigationTests` | NavigationTests, NavigationStackTests, PresentationTests, UIPatternTests | NavigationTests.swift, NavigationStackTests.swift, PresentationTests.swift, UIPatternTests.swift |
| Database | `DatabaseTests` | StructuredQueriesTests, SQLiteDataTests | StructuredQueriesTests.swift, SQLiteDataTests.swift |

**Pass 1 — Inventory (no changes, record baseline):**
1. Run `cd examples/fuse-library && swift test 2>&1 | tail -5` to capture the baseline test count.
2. Record the exact number (expected: 226 methods, 219 unique after redundant removal).
3. List every test file path and its target. Cross-reference against the table above. Confirm the mapping is complete — every file appears exactly once in the new structure.

**Pass 2 — Create new target directories and move files:**
1. Create new directories: `Tests/ObservationTests/`, `Tests/FoundationTests/`, `Tests/TCATests/`, `Tests/SharingTests/`, `Tests/DatabaseTests/`. (NavigationTests directory already exists — it will absorb NavigationStackTests, PresentationTests, UIPatternTests files.)
2. Move test files from old directories to new feature-aligned directories using `mv`. Preserve file names exactly (no renames).
3. Move FuseLibraryTests/ObservationTests.swift → ObservationTests/ObservationTests.swift
4. Move FuseLibraryTests/FuseLibraryTests.swift → ObservationTests/FuseLibraryTests.swift (scaffold tests stay with observation — they depend on FuseLibrary)
5. Move FuseLibraryTests/XCSkipTests.swift → ObservationTests/XCSkipTests.swift (Skip harness)
6. Do NOT move ObservationTrackingTests files — delete the entire directory (redundant per R6).
7. Delete now-empty old directories: CasePathsTests/, IdentifiedCollectionsTests/, CustomDumpTests/, IssueReportingTests/, StoreReducerTests/, EffectTests/, ObservableStateTests/, BindingTests/, DependencyTests/, SharedPersistenceTests/, SharedBindingTests/, SharedObservationTests/, NavigationStackTests/, PresentationTests/, UIPatternTests/, StructuredQueriesTests/, SQLiteDataTests/, FuseLibraryTests/, ObservationTrackingTests/.

**Pass 3 — Update Package.swift:**
Replace all 20 old `.testTarget` declarations with 6 new feature-aligned targets:

```swift
// Observation tests (Phase 1 bridge + tracking)
.testTarget(name: "ObservationTests", dependencies: [
    "FuseLibrary",
    .product(name: "SkipTest", package: "skip"),
], resources: [.process("Resources")], plugins: [.plugin(name: "skipstone", package: "skip")]),

// Foundation library tests (Phase 2)
.testTarget(name: "FoundationTests", dependencies: [
    .product(name: "CasePaths", package: "swift-case-paths"),
    .product(name: "IdentifiedCollections", package: "swift-identified-collections"),
    .product(name: "CustomDump", package: "swift-custom-dump"),
    .product(name: "IssueReporting", package: "xctest-dynamic-overlay"),
    .product(name: "IssueReportingTestSupport", package: "xctest-dynamic-overlay"),
]),

// TCA core tests (Phase 3 + 4 state/bindings)
.testTarget(name: "TCATests", dependencies: [
    .product(name: "ComposableArchitecture", package: "swift-composable-architecture"),
    .product(name: "Dependencies", package: "swift-dependencies"),
    .product(name: "DependenciesMacros", package: "swift-dependencies"),
    .product(name: "DependenciesTestSupport", package: "swift-dependencies"),
]),

// Sharing tests (Phase 4)
.testTarget(name: "SharingTests", dependencies: [
    .product(name: "Sharing", package: "swift-sharing"),
]),

// Navigation tests (Phase 5)
.testTarget(name: "NavigationTests", dependencies: [
    .product(name: "ComposableArchitecture", package: "swift-composable-architecture"),
]),

// Database tests (Phase 6)
.testTarget(name: "DatabaseTests", dependencies: [
    .product(name: "SQLiteData", package: "sqlite-data"),
    .product(name: "DependenciesTestSupport", package: "swift-dependencies"),
]),
```

**IMPORTANT dependency notes:**
- `TCATests` needs the union of dependencies from StoreReducerTests, EffectTests, ObservableStateTests, BindingTests, AND DependencyTests. DependencyTests needs `Dependencies`, `DependenciesMacros`, `DependenciesTestSupport` in addition to `ComposableArchitecture`.
- `ObservationTests` inherits FuseLibraryTests' SkipTest/skipstone setup (resources + plugin) because it contains the Skip harness files.
- `NavigationTests` target name conflicts with the old NavigationTests target — this is fine because we're replacing, not adding alongside.
- `DatabaseTests` needs `DependenciesTestSupport` because SQLiteDataTests uses it.

**Pass 4 — Verify (compilation + test count):**
1. Run `cd examples/fuse-library && swift build --build-tests` — must compile cleanly.
2. Run `cd examples/fuse-library && swift test` — all tests must pass.
3. Count test methods in output. Must equal 219 (226 original - 7 redundant ObservationTrackingTests).
4. If count differs, STOP and investigate. Do NOT proceed to Pass 5.

**Pass 5 — Cleanup verification:**
1. Confirm no old test directories remain (all should have been deleted in Pass 2).
2. Run `swift test` one final time to confirm stability.
3. Verify `swift test --filter ObservationTests` runs the observation group, `swift test --filter TCATests` runs the TCA group, etc.

**Key constraint:** If ANY pass fails compilation or changes the test count, revert that pass and investigate before continuing.
  </action>
  <verify>
1. `cd examples/fuse-library && swift build --build-tests` — compiles with 6 feature-aligned test targets.
2. `cd examples/fuse-library && swift test` — all 219 tests pass (226 - 7 redundant).
3. `swift test --filter ObservationTests` — runs observation group only.
4. `swift test --filter TCATests` — runs TCA core group only.
5. No old test directories remain in `Tests/`.
6. Package.swift has exactly 6 `.testTarget` declarations (down from 20).
  </verify>
  <done>All 219 unique tests reorganised into 6 feature-aligned targets (ObservationTests, FoundationTests, TCATests, SharingTests, NavigationTests, DatabaseTests). Zero tests lost. Old per-module targets removed. Test filtering works by feature domain.</done>
</task>

</tasks>

<verification>
1. `docs/FORKS.md` exists with all 17 fork sections
2. Mermaid diagram renders correctly (valid Mermaid syntax)
3. At least 5 forks have upstream PR candidate sections
4. Every fork has upstream version, commits ahead, classification
5. `cd examples/fuse-library && swift build --build-tests` — compiles with 6 feature-aligned test targets
6. `cd examples/fuse-library && swift test` — all 219 tests pass (226 - 7 redundant)
7. Package.swift has exactly 6 `.testTarget` declarations
8. No old per-module test directories remain (CasePathsTests/, StoreReducerTests/, etc.)
</verification>

<success_criteria>
- DOC-01 fully covered: every fork documented with upstream version, commits ahead, key changes, rationale, and upstream PR candidates
- Dependency graph visualized via Mermaid
- D3 implemented: all 219 unique tests reorganised into 6 feature-aligned targets (Task 2) with zero test loss
- Test count verified: 219 tests after reorganisation (226 original - 7 redundant ObservationTrackingTests)
- Documentation is actionable for evaluators and developers (per D5, D11)
</success_criteria>

<output>
After completion, create `.planning/phases/07-integration/07-04-SUMMARY.md`
</output>
