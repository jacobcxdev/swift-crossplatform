---
phase: 16-api-parity-completion
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - forks/swift-composable-architecture/Sources/ComposableArchitecture/Internal/Core.swift
  - forks/swift-composable-architecture/Sources/ComposableArchitecture/SwiftUI/ViewStore.swift
  - forks/swift-composable-architecture/Sources/ComposableArchitecture/SwiftUI/IfLetStore.swift
  - forks/swift-composable-architecture/Sources/ComposableArchitecture/SwiftUI/NavigationStackStore.swift
  - forks/swift-composable-architecture/Sources/ComposableArchitecture/SwiftUI/Binding.swift
  - forks/swift-composable-architecture/Sources/ComposableArchitecture/SwiftUI/Popover.swift
  - forks/swift-composable-architecture/Sources/ComposableArchitecture/SwiftUI/Alert+Observation.swift
  - forks/swift-composable-architecture/Sources/ComposableArchitecture/SwiftUI/ConfirmationDialog.swift
  - forks/swift-composable-architecture/Sources/ComposableArchitecture/SwiftUI/Binding+Observation.swift
  - forks/swift-composable-architecture/Sources/ComposableArchitecture/SwiftUI/NavigationStack+Observation.swift
  - forks/swift-composable-architecture/Sources/ComposableArchitecture/SwiftUI/Store+Observation.swift
  - forks/swift-composable-architecture/Sources/ComposableArchitecture/Effects/Animation.swift
  - forks/swift-composable-architecture/Sources/ComposableArchitecture/SwiftUI/ViewAction.swift
  - examples/fuse-library/Tests/TCATests/BindingTests.swift
  - examples/fuse-library/Tests/TCATests/StoreTests.swift
  - examples/fuse-library/Tests/NavigationTests/TextStateTests.swift
autonomous: true
requirements:
  - TCA-19
  - TCA-20
  - NAV-05
  - NAV-07

must_haves:
  truths:
    - "Binding+Observation extensions compile on Android without #if !os(Android) guard"
    - "Alert+Observation and ConfirmationDialog observation extensions compile on Android"
    - "IfLetStore compiles on Android (guards removed) or is documented as intentionally excluded"
    - "TextState modifiers (.bold(), .italic(), .kerning()) execute without crash on both platforms"
    - "BindingLocal is defined once in ViewStore.swift (no duplicate in Core.swift)"
    - "@Observable alternative to IfLetStore is tested and working on both platforms"
    - "Store.send(_:animation:) compiles on Android (withTransaction chain functional)"
  artifacts:
    - path: "examples/fuse-library/Tests/TCATests/BindingTests.swift"
      provides: "IfLetStore alternative pattern test"
      contains: "testIfLetStoreAlternativePattern"
    - path: "examples/fuse-library/Tests/NavigationTests/TextStateTests.swift"
      provides: "TextState modifier compilation and execution test"
      contains: "testTextStateModifiersCompileAndExecute"
  key_links:
    - from: "forks/swift-composable-architecture/Sources/ComposableArchitecture/SwiftUI/Alert+Observation.swift"
      to: "forks/swift-navigation/Sources/SwiftNavigation/ButtonState.swift"
      via: "animatedSend case in switch statement"
      pattern: "\\.animatedSend"
    - from: "forks/swift-composable-architecture/Sources/ComposableArchitecture/Effects/Animation.swift"
      to: "forks/skip-fuse-ui/Sources/SkipSwiftUI/Animation/Transaction.swift"
      via: "Effect.animation calls Transaction(animation:) which chains to withTransaction"
      pattern: "Transaction\\(animation:"
---

<objective>
Remove all Android guards from the TCA fork (comprehensive audit), clean up BindingLocal duplication, and write focused tests for each enablement — completing API parity on Android.

Purpose: With withTransaction and swift-navigation guards resolved in Plan 01, TCA's animation-related guards can now be removed. The comprehensive audit covers all ~24 guards in TCA, deduplicates BindingLocal, and validates each enablement with a dedicated test.

Output: Fully unguarded TCA fork (except UIKitNavigation export and ObservedObject.Wrapper deprecated paths), passing tests on Darwin.
</objective>

<execution_context>
@/Users/jacob/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jacob/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-api-parity-completion/16-CONTEXT.md
@.planning/phases/16-api-parity-completion/16-RESEARCH.md
@.planning/phases/16-api-parity-completion/16-01-SUMMARY.md
@forks/swift-composable-architecture/Sources/ComposableArchitecture/Internal/Core.swift
@forks/swift-composable-architecture/Sources/ComposableArchitecture/SwiftUI/ViewStore.swift
@forks/swift-composable-architecture/Sources/ComposableArchitecture/SwiftUI/IfLetStore.swift
@forks/swift-composable-architecture/Sources/ComposableArchitecture/SwiftUI/Binding.swift
@forks/swift-composable-architecture/Sources/ComposableArchitecture/SwiftUI/Alert+Observation.swift
@forks/swift-composable-architecture/Sources/ComposableArchitecture/SwiftUI/Binding+Observation.swift
@forks/swift-composable-architecture/Sources/ComposableArchitecture/SwiftUI/NavigationStack+Observation.swift
@forks/swift-composable-architecture/Sources/ComposableArchitecture/SwiftUI/Store+Observation.swift
@forks/swift-composable-architecture/Sources/ComposableArchitecture/Effects/Animation.swift
@forks/swift-composable-architecture/Sources/ComposableArchitecture/SwiftUI/ViewAction.swift
@forks/swift-composable-architecture/Sources/ComposableArchitecture/SwiftUI/Popover.swift
@forks/swift-composable-architecture/Sources/ComposableArchitecture/SwiftUI/NavigationStackStore.swift
@forks/swift-composable-architecture/Sources/ComposableArchitecture/SwiftUI/ConfirmationDialog.swift
@forks/swift-composable-architecture/Sources/ComposableArchitecture/Exports.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Comprehensive TCA guard removal and BindingLocal cleanup</name>
  <files>
    forks/swift-composable-architecture/Sources/ComposableArchitecture/Internal/Core.swift
    forks/swift-composable-architecture/Sources/ComposableArchitecture/SwiftUI/ViewStore.swift
    forks/swift-composable-architecture/Sources/ComposableArchitecture/SwiftUI/IfLetStore.swift
    forks/swift-composable-architecture/Sources/ComposableArchitecture/SwiftUI/NavigationStackStore.swift
    forks/swift-composable-architecture/Sources/ComposableArchitecture/SwiftUI/Binding.swift
    forks/swift-composable-architecture/Sources/ComposableArchitecture/SwiftUI/Popover.swift
    forks/swift-composable-architecture/Sources/ComposableArchitecture/SwiftUI/Alert+Observation.swift
    forks/swift-composable-architecture/Sources/ComposableArchitecture/SwiftUI/ConfirmationDialog.swift
    forks/swift-composable-architecture/Sources/ComposableArchitecture/SwiftUI/Binding+Observation.swift
    forks/swift-composable-architecture/Sources/ComposableArchitecture/SwiftUI/NavigationStack+Observation.swift
    forks/swift-composable-architecture/Sources/ComposableArchitecture/SwiftUI/Store+Observation.swift
    forks/swift-composable-architecture/Sources/ComposableArchitecture/Effects/Animation.swift
    forks/swift-composable-architecture/Sources/ComposableArchitecture/SwiftUI/ViewAction.swift
  </files>
  <action>
**Comprehensive audit and removal of all `#if !os(Android)` and `#if canImport(SwiftUI) && !os(Android)` guards in TCA fork.**

**BindingLocal cleanup (do atomically):**
1. In `ViewStore.swift` (~line 632): Remove the `#if !os(Android)` guard wrapping the `BindingLocal` `DependencyKey` extension so it compiles on both platforms.
2. In `Core.swift` (~line 14-18): Delete the `#if !canImport(SwiftUI) || os(Android)` block that defines the duplicate `BindingLocal`. Also check if there's a `_isInPerceptionTracking` block in Core.swift with a similar guard — if so, evaluate whether it can also be consolidated.

**Non-animation guard removal (these don't depend on withTransaction):**
- `IfLetStore.swift` x3 guards: Remove `#if !os(Android)`. IfLetStore is deprecated but per user decision, remove guards and let it compile. Only re-gate if it's the sole cause of a build failure.
- `NavigationStackStore.swift` x1 guard: Remove `#if !os(Android)`. Same deprecated-but-unguard strategy.
- `Binding.swift` x2 guards: Remove `#if !os(Android)` or `&& !os(Android)`.
- `Popover.swift` x1 guard: Remove guard.
- `ViewStore.swift` lines ~251, ~365: Remove `#if !os(Android)` guards (2 guards besides the BindingLocal one).

**Animation guard removal (depends on Plan 01 withTransaction):**
- `Animation.swift`: Change `#if canImport(SwiftUI) && !os(Android)` to `#if canImport(SwiftUI)`.
- `Alert+Observation.swift` x2 guards: Change `&& !os(Android)` to just `canImport(SwiftUI)` or remove the `!os(Android)` part. These reference ButtonState.animatedSend (now unguarded from Plan 01).
- `ConfirmationDialog.swift` (TCA): Same treatment as Alert+Observation.
- `ViewAction.swift` line ~31: Remove guard from `send(_:animation:)` overload.

**ObservedObject.Wrapper guards — LEAVE AS-IS (per research):**
- `Binding+Observation.swift` x4 (lines 14, 47, 290, 373) — deprecated, ObservedObject not in SkipFuseUI
- `NavigationStack+Observation.swift` x2 (lines 74, 111) — deprecated, same reason
- `Store+Observation.swift` x2 (lines 197, 317) — deprecated, same reason

**NavigationStack+Observation.swift line 150:**
- Remove the `#if !os(Android)` guard. skip-fuse-ui provides `NavigationStack<Data, Root>`. If it fails to compile due to `ModifiedContent` constraint, re-gate with `#if !os(Android)`.

**MUST STAY (do NOT change):**
- `Exports.swift` line 14: `#if !os(Android)` wrapping `@_exported import UIKitNavigation` — UIKitNavigation doesn't exist on Android.

After all changes, build verify: `swift build` from both `examples/fuse-library/` and `examples/fuse-app/`. If any guard removal causes a build failure that's NOT due to a missing SkipFuseUI type (i.e. it's a real code incompatibility), re-gate that specific block with `#if !os(Android)` and document with a comment.
  </action>
  <verify>
    <automated>cd /Users/jacob/Developer/src/github/jacobcxdev/swift-crossplatform/examples/fuse-library && swift build 2>&1 | tail -5</automated>
    <manual>Verify BindingLocal only defined once (grep for "enum BindingLocal" across TCA sources — should appear only in ViewStore.swift)</manual>
  </verify>
  <done>All removable guards removed from TCA fork. BindingLocal deduplicated. ObservedObject.Wrapper guards preserved (deprecated). UIKitNavigation export guard preserved. Both example projects build on Darwin.</done>
</task>

<task type="auto">
  <name>Task 2: Enablement tests for all guard removals</name>
  <files>
    examples/fuse-library/Tests/TCATests/BindingTests.swift
    examples/fuse-library/Tests/TCATests/StoreTests.swift
    examples/fuse-library/Tests/NavigationTests/TextStateTests.swift
  </files>
  <action>
Write one focused test per enablement. Tests run on both Darwin and Android (not platform-gated). Add to existing test files where a natural fit exists; create new test file only if needed.

**Test 1: IfLetStore alternative pattern** (in `BindingTests.swift` or a new file in TCATests/)
```swift
/// Tests the modern @Observable alternative to the deprecated IfLetStore pattern.
/// IfLetStore is deprecated and excluded from test coverage — this test proves
/// the recommended replacement works on both platforms.
@Test func testIfLetStoreAlternativePattern() async {
    // Define a parent reducer with @Presents optional child state
    // Send action to set child non-nil → verify state set
    // Send action to set child nil → verify state cleared
    // This proves the if-let-scope pattern works without IfLetStore
}
```
Use the pattern from 16-RESEARCH.md code example — parent with @Presents child, TestStore send/receive cycle.

**Test 2: Store.send with animation** (in `StoreTests.swift`)
```swift
/// Tests that Store.send(_:animation:) compiles and routes correctly on Android.
/// This exercises the withTransaction chain: send(animation:) → Transaction(animation:) → withTransaction → withAnimation.
@Test func testSendWithAnimation() async {
    // Simple reducer, TestStore
    // store.send(.increment, animation: .default) — verify state changes
    // The animation parameter is a no-op in test context but must compile and not crash
}
```
Note: TestStore's `send` doesn't take `animation:` directly. Instead, test that `Effect.animation()` compiles by creating an effect with `.animation(.default)` modifier and verifying it executes. Or test `ViewStore.send(_:animation:)` if accessible. Use whatever path actually exercises the withTransaction chain. The key is that the animation-related API compiles and doesn't crash.

**Test 3: TextState modifiers** (in `TextStateTests.swift` — create if doesn't exist, or add to existing NavigationTests file)
```swift
/// Tests that TextState rich text modifiers compile and execute on Android
/// without crash. Does NOT assert on rendered output — that's UI-level testing.
@Test func testTextStateModifiersCompileAndExecute() {
    let bold = TextState("Hello").bold()
    let italic = TextState("Hello").italic()
    let kerning = TextState("Hello").kerning(1.5)
    let foreground = TextState("Hello").foregroundColor(.red)
    let font = TextState("Hello").font(.body)
    let combined = TextState("Hello").bold().italic().font(.headline)

    // Verify equality still works with modifiers
    #expect(bold == TextState("Hello").bold())
    #expect(bold != italic)

    // Verify concatenation with modifiers
    let concat = TextState("Hello ") + TextState("World").bold()
    #expect(concat == TextState("Hello ") + TextState("World").bold())
}
```

**Test 4: Alert/ConfirmationDialog with animated action** (in existing NavigationTests or TCATests)
```swift
/// Tests that ButtonState with animatedSend action type works on Android.
/// Exercises the newly-unguarded animatedSend enum case.
@Test func testButtonStateAnimatedAction() {
    let button = ButtonState(action: .send(.confirm, animation: .default)) {
        TextState("OK")
    }
    // Verify button properties accessible
    #expect(button.label == TextState("OK"))
}
```

Run `make darwin-test EXAMPLE=fuse-library` to verify all tests pass.
  </action>
  <verify>
    <automated>cd /Users/jacob/Developer/src/github/jacobcxdev/swift-crossplatform && make darwin-test EXAMPLE=fuse-library 2>&1 | tail -20</automated>
    <manual>Verify test names include: testIfLetStoreAlternativePattern, testTextStateModifiersCompileAndExecute, testButtonStateAnimatedAction</manual>
  </verify>
  <done>All enablement tests pass on Darwin. Tests cover: IfLetStore alternative (@Observable pattern), TextState rich text modifiers, animation-related APIs (ButtonState.animatedSend), Store.send with animation chain. Tests are not platform-gated (will run on Android too).</done>
</task>

</tasks>

<verification>
1. `make darwin-build` passes for both fuse-library and fuse-app
2. `make darwin-test EXAMPLE=fuse-library` passes (all existing + new tests)
3. Grep for `!os(Android)` in TCA sources shows ONLY: Exports.swift (UIKitNavigation), ObservedObject.Wrapper blocks (8 guards in 3 files), and any targeted inner guards with explanatory comments
4. BindingLocal defined exactly once (in ViewStore.swift)
5. Core.swift no longer contains BindingLocal
6. New tests: testIfLetStoreAlternativePattern, testTextStateModifiersCompileAndExecute, testButtonStateAnimatedAction all pass
</verification>

<success_criteria>
- All TCA `#if !os(Android)` guards removed except UIKitNavigation export and ObservedObject.Wrapper deprecated paths
- BindingLocal deduplicated to single definition in ViewStore.swift
- Binding+Observation, Alert+Observation, ConfirmationDialog observation extensions unguarded
- IfLetStore unguarded (deprecated, no test) with @Observable alternative test proving replacement works
- TextState modifiers tested (compile + no crash + equality)
- Animation chain (Store.send → withTransaction → withAnimation) functional
- All tests pass on Darwin
</success_criteria>

<output>
After completion, create `.planning/phases/16-api-parity-completion/16-02-SUMMARY.md`
</output>
