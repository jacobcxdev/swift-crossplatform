---
phase: 19-reimplement-skipapp-showcase-fuse-showcases-in-fuse-app-with-tca-and-skipui-api-validation
plan: 03
type: execute
wave: 2
depends_on: ["19-01", "19-02"]
files_modified:
  - examples/fuse-app/Sources/FuseApp/TestHarnessFeature.swift
  - examples/fuse-app/Sources/FuseApp/ControlPanelView.swift
autonomous: true
requirements:
  - SHOWCASE-03

must_haves:
  truths:
    - "App shows 2 tabs: Showcase and Control"
    - "Showcase tab displays ShowcaseView with searchable NavigationStack list"
    - "Control tab shows ControlPanelView with scenario controls"
    - "ScenarioEngine infrastructure is preserved and functional"
    - "Debug toolbar overlay still works during scenario execution"
  artifacts:
    - path: "examples/fuse-app/Sources/FuseApp/TestHarnessFeature.swift"
      provides: "Root reducer with 2-tab structure (Showcase + Control)"
      contains: "case showcase, control"
    - path: "examples/fuse-app/Sources/FuseApp/ControlPanelView.swift"
      provides: "Updated ControlPanelView for 2-tab structure"
  key_links:
    - from: "TestHarnessFeature.swift"
      to: "ShowcaseFeature.swift"
      via: "Scope(state: \\.showcase, action: \\.showcase)"
      pattern: "Scope.*showcase"
---

<objective>
Restructure TestHarnessFeature from the stub left by Plan 01 into the final 2-tab structure (Showcase + Control), composing ShowcaseFeature from Plan 02.

Purpose: The app's root reducer and view now reflect the final architecture -- Showcase tab for playground browsing, Control tab for ScenarioEngine.
Output: Working 2-tab app with searchable playground list in Showcase tab.
</objective>

<execution_context>
@/Users/jacob/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jacob/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-reimplement-skipapp-showcase-fuse-showcases-in-fuse-app-with-tca-and-skipui-api-validation/19-CONTEXT.md
@.planning/phases/19-reimplement-skipapp-showcase-fuse-showcases-in-fuse-app-with-tca-and-skipui-api-validation/19-RESEARCH.md
@.planning/phases/19-reimplement-skipapp-showcase-fuse-showcases-in-fuse-app-with-tca-and-skipui-api-validation/19-01-SUMMARY.md
@.planning/phases/19-reimplement-skipapp-showcase-fuse-showcases-in-fuse-app-with-tca-and-skipui-api-validation/19-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure TestHarnessFeature to 2-tab architecture</name>
  <files>examples/fuse-app/Sources/FuseApp/TestHarnessFeature.swift</files>
  <action>
    Rewrite TestHarnessFeature (which was left as a stub by Plan 01) to the final 2-tab structure:

    **State:**
    - `var selectedTab: Tab = .showcase`
    - `var showcase = ShowcaseFeature.State()`
    - Keep all ScenarioEngine state: `pendingUICommand`, `selectedScenarioIDs`, `runningScenarioID`, `currentStepDescription`, `executionMode`, `currentStepIndex`, `totalStepCount`, `breakOnAllCheckpoints`, `eventLog`, `isScenarioRunning`
    - `enum Tab: String, Equatable, CaseIterable { case showcase, control }` with displayName computed property

    **Action:**
    - `case tabSelected(State.Tab)`
    - `case showcase(ShowcaseFeature.Action)`
    - Keep all ScenarioEngine actions: `resetAll`, `executeUICommand`, `uiCommandCompleted`, `cancelUICommand`, `scenarioToggled`, `scenarioSetSelected`, `scenarioStarted`, `scenarioStepChanged`, `scenarioEnded`, `debugPlay/Pause/Step/StepOver/Stop`, `toggleBreakOnAllCheckpoints`, `scenarioStepIndexChanged`, `eventLogAppend`, `clearEventLog`

    **Reducer body:**
    - `Scope(state: \.showcase, action: \.showcase) { ShowcaseFeature() }` at the top
    - `Reduce { state, action in ... }` handling tab switching, scenario management, debug controls
    - For `resetAll`: just reset showcase state (`state.showcase = .init()`) and clear pendingUICommand
    - For `executeUICommand`: with only 2 tabs and no child forwarding on showcase, just set pendingUICommand (scenario commands go through the engine, not tab children)
    - Remove all references to forEachNamespace, peerSurvival, engineTest

    **TestHarnessView:**
    - `TabView(selection: $store.selectedTab.sending(\.tabSelected))` with 2 tabs:
      1. ShowcaseView(store: store.scope(state: \.showcase, action: \.showcase)) with tabItem Label("Showcase", systemImage: "list.bullet.rectangle") and tag .showcase
      2. NavigationStack { ControlPanelView(store: store) } with tabItem Label("Control", systemImage: "gearshape") and tag .control
    - Keep the debug toolbar overlay (the ZStack with transport controls) exactly as-is -- it shows when `store.isScenarioRunning`
    - Keep DebugButton helper view as-is
  </action>
  <verify>
    <automated>cd /Users/jacob/Developer/src/github/jacobcxdev/swift-crossplatform/examples/fuse-app && swift build 2>&1 | tail -5</automated>
  </verify>
  <done>TestHarnessFeature restructured with Showcase + Control tabs. ShowcaseFeature composed via Scope. ScenarioEngine infrastructure preserved. Debug toolbar intact.</done>
</task>

<task type="auto">
  <name>Task 2: Update ControlPanelView for 2-tab structure</name>
  <files>examples/fuse-app/Sources/FuseApp/ControlPanelView.swift</files>
  <action>
    Update ControlPanelView to work with the 2-tab TestHarnessFeature:
    - Remove any references to ForEachNamespace, PeerSurvival, or EngineTest tabs/actions
    - Update scenario selection UI to reflect available scenarios (which will be empty since Plan 01 deletes existing scenarios -- the infrastructure supports adding new ones on-demand)
    - Keep the store binding pattern (`StoreOf<TestHarnessFeature>`)
    - Keep all scenario management UI: scenario list, run/stop buttons, event log, debug controls
    - If ControlPanelView references child tab actions for scenario forwarding, simplify to just engine controls

    The ControlPanelView should compile cleanly with the new TestHarnessFeature state shape.
  </action>
  <verify>
    <automated>cd /Users/jacob/Developer/src/github/jacobcxdev/swift-crossplatform/examples/fuse-app && swift build 2>&1 | tail -5</automated>
  </verify>
  <done>ControlPanelView updated for 2-tab structure. No references to deleted Phase 18.1 types. Compiles cleanly.</done>
</task>

</tasks>

<verification>
- `swift build` succeeds
- App has exactly 2 tabs: Showcase and Control
- Showcase tab shows searchable list of 84 playgrounds (all rendering placeholder text)
- Control tab shows ScenarioEngine controls
</verification>

<success_criteria>
Working 2-tab app structure. Users can browse all 84 playground entries (placeholder content) and use the Control tab for scenario management.
</success_criteria>

<output>
After completion, create `.planning/phases/19-reimplement-skipapp-showcase-fuse-showcases-in-fuse-app-with-tca-and-skipui-api-validation/19-03-SUMMARY.md`
</output>
