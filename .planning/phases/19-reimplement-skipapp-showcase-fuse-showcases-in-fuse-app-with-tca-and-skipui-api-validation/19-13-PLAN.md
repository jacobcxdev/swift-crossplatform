---
phase: 19-reimplement-skipapp-showcase-fuse-showcases-in-fuse-app-with-tca-and-skipui-api-validation
plan: 13
type: execute
wave: 4
depends_on: ["19-12"]
files_modified:
  - examples/fuse-app/Sources/FuseApp/FuseApp.swift
  - examples/fuse-app/Sources/FuseApp/PlaygroundTypes.swift
  - examples/fuse-app/Sources/FuseApp/PlaygroundDestinationView.swift
  - examples/fuse-app/Sources/FuseApp/ShowcaseFeature.swift
  - examples/fuse-app/Sources/FuseApp/TestHarnessFeature.swift
  - examples/fuse-app/Sources/FuseApp/ControlPanelView.swift
  - examples/fuse-app/Sources/FuseApp/ScenarioEngine.swift
  - examples/fuse-app/Sources/FuseApp/PlatformHelper.swift
  - examples/fuse-app/Sources/FuseApp/StatePlaygroundModel.swift
  - examples/fuse-app/Sources/FuseApp/AccessibilityPlayground.swift
  - examples/fuse-app/Sources/FuseApp/AlertPlayground.swift
  - examples/fuse-app/Sources/FuseApp/AnimationPlayground.swift
  - examples/fuse-app/Sources/FuseApp/BackgroundPlayground.swift
  - examples/fuse-app/Sources/FuseApp/BlendModePlayground.swift
  - examples/fuse-app/Sources/FuseApp/BlurPlayground.swift
  - examples/fuse-app/Sources/FuseApp/BorderPlayground.swift
  - examples/fuse-app/Sources/FuseApp/ButtonPlayground.swift
  - examples/fuse-app/Sources/FuseApp/ColorPlayground.swift
  - examples/fuse-app/Sources/FuseApp/ColorEffectsPlayground.swift
autonomous: true
requirements: ["SHOWCASE-02", "SHOWCASE-03", "SHOWCASE-10"]

must_haves:
  truths:
    - "All 19 files in this group conform to PFW skill rules with zero violations"
    - "TCA infrastructure files (ShowcaseFeature, TestHarnessFeature) follow canonical @Reducer patterns"
    - "No legacy TCA APIs (ViewStore, WithViewStore, @PresentationState, @BindableState) present"
    - "No anti-patterns from pfw-modern-swiftui (Binding.init(get:set:), logic in view bodies)"
    - "Cross-platform guards correct per CLAUDE.md (#if os(Android), #if SKIP)"
  artifacts:
    - path: "examples/fuse-app/Sources/FuseApp/ShowcaseFeature.swift"
      provides: "TCA NavigationStack composition"
    - path: "examples/fuse-app/Sources/FuseApp/TestHarnessFeature.swift"
      provides: "Root TCA reducer with tab management"
  key_links:
    - from: "ShowcaseFeature.swift"
      to: "PlaygroundDestinationView.swift"
      via: "NavigationStack destination routing"
      pattern: "PlaygroundDestinationView"
---

<objective>
Validate infrastructure files and A-B playgrounds against all relevant PFW skills.

Purpose: Ensure the core TCA infrastructure (ShowcaseFeature, TestHarnessFeature, ShowcasePath) and the first batch of playground files conform to Point-Free canonical patterns. This group includes the most critical files — the TCA reducers that compose the entire app.

Output: All 19 files validated and fixed. Zero PFW skill violations remaining.
</objective>

<execution_context>
@/Users/jacob/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jacob/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-reimplement-skipapp-showcase-fuse-showcases-in-fuse-app-with-tca-and-skipui-api-validation/19-CONTEXT.md
</context>

## Critical Constraint: Minimal Deviation from Upstream

**The upstream source of truth is `examples/skipapp-showcase-fuse/Sources/ShowcaseFuse/`.**

For every playground file, the executor MUST compare against the corresponding upstream file. Changes should ONLY be made when:
1. A `/pfw-*` or Axiom skill rule **genuinely requires** it (cite the specific rule)
2. Cross-platform compatibility demands it (cite the CLAUDE.md gotcha)
3. TCA wrapping was added by design (infrastructure files only)

**DO NOT change code that:**
- Is merely stylistic (e.g. renaming variables, reordering modifiers)
- Could be "improved" but isn't violating any skill rule
- Differs from upstream but works correctly as-is

**For each change made:** Document (a) which skill rule mandates it, (b) the upstream code, (c) the replacement. If zero violations are found in a file, report "No changes needed — upstream-faithful" and move on.

<tasks>

<task type="auto">
  <name>Task 1: Validate infrastructure files against PFW skills</name>
  <files>
    examples/fuse-app/Sources/FuseApp/FuseApp.swift
    examples/fuse-app/Sources/FuseApp/PlaygroundTypes.swift
    examples/fuse-app/Sources/FuseApp/PlaygroundDestinationView.swift
    examples/fuse-app/Sources/FuseApp/ShowcaseFeature.swift
    examples/fuse-app/Sources/FuseApp/TestHarnessFeature.swift
    examples/fuse-app/Sources/FuseApp/ControlPanelView.swift
    examples/fuse-app/Sources/FuseApp/ScenarioEngine.swift
    examples/fuse-app/Sources/FuseApp/PlatformHelper.swift
    examples/fuse-app/Sources/FuseApp/StatePlaygroundModel.swift
  </files>
  <action>
Read the following PFW skill files for validation rules (invoke the Skill tool or read the SKILL.md directly):
- `/pfw-composable-architecture` — @Reducer patterns, @ObservableState, action naming, Store usage, no legacy APIs
- `/pfw-case-paths` — @CasePathable enum conformance, case path key paths
- `/pfw-swift-navigation` — NavigationStack path binding, NavigationLink(state:)
- `/pfw-modern-swiftui` — action closure naming, no Binding.init(get:set:), multiline logic extraction
- `/pfw-perception` — WithPerceptionTracking usage, @Perception.Bindable
- `/pfw-identified-collections` — IdentifiedArrayOf usage patterns
- `/pfw-observable-models` — @Observable class patterns, method naming
- `/pfw-sharing` — @Shared patterns

Then read each of the 9 infrastructure files and check against ALL of these rules:

**TCA checks (ShowcaseFeature.swift, TestHarnessFeature.swift):**
- @Reducer struct (not class), no `Reducer` suffix on type name
- @ObservableState on State struct
- Action enum is @CasePathable, NOT Equatable
- Action cases named after user actions (e.g. `buttonTapped`), not logic (e.g. `increment`)
- Uses `body` with `Reduce {}`, not legacy `reduce(into:)`
- No legacy ViewStore/WithViewStore
- No legacy @PresentationState (use @Presents)
- No legacy @BindableState (Action conforms to BindableAction with BindingReducer() if bindings needed)
- @Dependency declared inside @Reducer (not outside)
- CombineReducers only used when modifier (.ifLet, .forEach) is applied
- Scope(state:action:) for child composition
- store.scope(state:action:) in views for child stores
- NavigationStack uses $store.scope(state:\.path, action:\.path) pattern
- @Reducer enum Path not nested inside parent (use ParentPath naming)
- .forEach(\.path, action:\.path) { Path.body } for stack navigation

**SwiftUI checks (all files):**
- No Binding.init(get:set:) — use dynamic member lookup helpers instead
- Multiline action closures extracted to private methods
- Methods named after user actions, not logic
- No heavy work in initializers
- Views use `let store: StoreOf<X>` or `@Bindable var store: StoreOf<X>` (not @State for stores)

**Cross-platform checks (all files per CLAUDE.md):**
- #if os(Android) or #if SKIP guards where needed
- No #if canImport(SwiftUI) && !os(Android) unless justified
- Internal access (not private) for View structs (Skip transpiler rejects private Views)

Fix any violations found. Document what was changed and why.
  </action>
  <verify>
    <automated>cd /Users/jacob/Developer/src/github/jacobcxdev/swift-crossplatform/examples/fuse-app && swift build 2>&1 | tail -5</automated>
  </verify>
  <done>All 9 infrastructure files pass PFW skill validation with zero violations. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Validate A-B playground files against PFW skills</name>
  <files>
    examples/fuse-app/Sources/FuseApp/AccessibilityPlayground.swift
    examples/fuse-app/Sources/FuseApp/AlertPlayground.swift
    examples/fuse-app/Sources/FuseApp/AnimationPlayground.swift
    examples/fuse-app/Sources/FuseApp/BackgroundPlayground.swift
    examples/fuse-app/Sources/FuseApp/BlendModePlayground.swift
    examples/fuse-app/Sources/FuseApp/BlurPlayground.swift
    examples/fuse-app/Sources/FuseApp/BorderPlayground.swift
    examples/fuse-app/Sources/FuseApp/ButtonPlayground.swift
    examples/fuse-app/Sources/FuseApp/ColorPlayground.swift
    examples/fuse-app/Sources/FuseApp/ColorEffectsPlayground.swift
  </files>
  <action>
Read the same PFW skill files as Task 1. Then read each of the 10 playground files and check:

**For plain View playgrounds (no TCA):**
- No Binding.init(get:set:) — derive bindings via dynamic member lookup
- Multiline action closures (>1 line) extracted to private methods on the view
- Private methods named after user actions (e.g. `submitButtonTapped`), not logic
- No heavy work in initializers — use .task modifier for async
- @State used correctly (value types, not reference types unless @Observable class)
- Internal access for View structs (not private — Skip transpiler requirement)
- No unnecessary self references
- No comments in generated code (per pfw-observable-models)

**For TCA-wrapped playgrounds (if any use @Reducer):**
- All TCA checks from Task 1 apply
- store.send(.actionName) not store.send(.view(.actionName)) unless @ViewAction is used
- @ViewAction macro used correctly if present

**Cross-platform checks:**
- No platform-specific APIs without #if guards
- No force unwraps that could crash on Android (check data! patterns)
- Foundation types used correctly (Date, UUID, etc. — available on both platforms)

Fix any violations found. Document what was changed and why.
  </action>
  <verify>
    <automated>cd /Users/jacob/Developer/src/github/jacobcxdev/swift-crossplatform/examples/fuse-app && swift build 2>&1 | tail -5</automated>
  </verify>
  <done>All 10 A-B playground files pass PFW skill validation with zero violations. Build succeeds.</done>
</task>

</tasks>

<verification>
All 19 files in this group validated against PFW skills. `swift build` succeeds. `swift test` passes.
</verification>

<success_criteria>
- Zero PFW skill violations in any of the 19 files
- `swift build` succeeds after all fixes
- `swift test` passes after all fixes
- Each fix documented with rule reference (e.g. "pfw-composable-architecture: DO NOT conform Action to Equatable")
</success_criteria>

<output>
After completion, create `.planning/phases/19-reimplement-skipapp-showcase-fuse-showcases-in-fuse-app-with-tca-and-skipui-api-validation/19-13-SUMMARY.md`
</output>
