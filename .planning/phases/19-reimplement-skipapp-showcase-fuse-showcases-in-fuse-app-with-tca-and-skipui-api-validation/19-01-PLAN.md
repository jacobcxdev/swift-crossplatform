---
phase: 19-reimplement-skipapp-showcase-fuse-showcases-in-fuse-app-with-tca-and-skipui-api-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/fuse-app/Sources/FuseApp/ForEachNamespaceSetting.swift
  - examples/fuse-app/Sources/FuseApp/PeerSurvivalSetting.swift
  - examples/fuse-app/Sources/FuseApp/IdentityComponents.swift
  - examples/fuse-app/Sources/FuseApp/ScenarioEngineSetting.swift
  - examples/fuse-app/Tests/FuseAppIntegrationTests/IdentityFeatureTests.swift
  - examples/fuse-app/Tests/FuseAppIntegrationTests/FuseAppIntegrationTests.swift
autonomous: true
requirements:
  - SHOWCASE-01

must_haves:
  truths:
    - "Phase 18.1 test harness files are deleted from the codebase"
    - "Old integration tests referencing deleted types are removed"
    - "Remaining source files still compile (no dangling references)"
  artifacts:
    - path: "examples/fuse-app/Sources/FuseApp/"
      provides: "Source directory without Phase 18.1 files"
  key_links: []
---

<objective>
Delete all Phase 18.1 test harness files and their associated tests. Phase 18.1 validation is complete -- these files served their purpose and must be removed before the showcase restructuring begins.

Purpose: Clean slate for showcase port -- removes ForEachNamespaceSetting, PeerSurvivalSetting, IdentityComponents, ScenarioEngineSetting and their test files.
Output: Source tree with only ScenarioEngine.swift, ControlPanelView.swift, TestHarnessFeature.swift, FuseApp.swift remaining.
</objective>

<execution_context>
@/Users/jacob/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jacob/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-reimplement-skipapp-showcase-fuse-showcases-in-fuse-app-with-tca-and-skipui-api-validation/19-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Phase 18.1 source files</name>
  <files>
    examples/fuse-app/Sources/FuseApp/ForEachNamespaceSetting.swift
    examples/fuse-app/Sources/FuseApp/PeerSurvivalSetting.swift
    examples/fuse-app/Sources/FuseApp/IdentityComponents.swift
    examples/fuse-app/Sources/FuseApp/ScenarioEngineSetting.swift
  </files>
  <action>
    Delete these 4 files:
    - ForEachNamespaceSetting.swift (ForEachNamespaceSetting reducer + view)
    - PeerSurvivalSetting.swift (PeerSurvivalSetting reducer + view)
    - IdentityComponents.swift (LocalCounterFeature, card components)
    - ScenarioEngineSetting.swift (ScenarioEngineSetting reducer + view)

    These are Phase 18.1 identity validation files. Per user decision: "Remove completely: ForEachNamespaceSetting, PeerSurvivalSetting, IdentityComponents, ScenarioEngineSetting".

    After deletion, update TestHarnessFeature.swift to comment out or stub references to the deleted types so the project can still compile. Specifically:
    - Remove the `forEachNamespace`, `peerSurvival`, `engineTest` state properties
    - Remove the corresponding Scope reducers in `body`
    - Remove the corresponding Action cases
    - Remove the Tab enum cases (keep only `control` for now -- Plan 03 will add `showcase`)
    - Simplify the view to only show the Control tab
    - Keep ScenarioEngine.swift (infrastructure stays) and ControlPanelView.swift

    Note: TestHarnessFeature will be minimal/stub after this -- Plan 03 will fully restructure it.
  </action>
  <verify>
    <automated>cd /Users/jacob/Developer/src/github/jacobcxdev/swift-crossplatform/examples/fuse-app && swift build 2>&1 | tail -5</automated>
  </verify>
  <done>4 Phase 18.1 source files deleted. TestHarnessFeature compiles with only Control tab. No dangling references.</done>
</task>

<task type="auto">
  <name>Task 2: Delete Phase 18.1 test files and update integration tests</name>
  <files>
    examples/fuse-app/Tests/FuseAppIntegrationTests/IdentityFeatureTests.swift
    examples/fuse-app/Tests/FuseAppIntegrationTests/FuseAppIntegrationTests.swift
  </files>
  <action>
    1. Delete IdentityFeatureTests.swift entirely (tests ForEachNamespaceSetting and identity features that no longer exist).

    2. In FuseAppIntegrationTests.swift:
       - Remove the entire `ForEachNamespaceSettingTests` suite (references deleted ForEachNamespaceSetting type)
       - Remove the entire `ForEachNamespaceExtendedTests` suite (same)
       - Update `TestHarnessFeatureTests` to match the simplified TestHarnessFeature:
         - Update `initialState` test: remove references to forEachNamespace/peerSurvival/engineTest tabs
         - Update `tabSwitching` test: only test available tabs (just `control` for now)
         - Remove `resetAllClearsState` test (references deleted child reducers)
         - Remove `executeUICommandAndAcknowledge` test (references forEachNamespace child)
         - Remove `cancelUICommandClearsState` test (references forEachNamespace child)
         - Remove `executeUICommandOnNonForwardingTab` test (references old tab structure)
       - Keep the file with a minimal TestHarnessFeatureTests suite that validates the stub state

    The tests will be properly rebuilt in Plan 12 after the full showcase is wired.
  </action>
  <verify>
    <automated>cd /Users/jacob/Developer/src/github/jacobcxdev/swift-crossplatform/examples/fuse-app && swift test 2>&1 | tail -10</automated>
  </verify>
  <done>IdentityFeatureTests.swift deleted. FuseAppIntegrationTests.swift cleaned of all Phase 18.1 references. All remaining tests pass.</done>
</task>

</tasks>

<verification>
- `swift build` succeeds in examples/fuse-app/
- `swift test` passes with no failures
- No files named ForEachNamespaceSetting, PeerSurvivalSetting, IdentityComponents, or ScenarioEngineSetting exist in Sources/
- No test files reference deleted types
</verification>

<success_criteria>
Phase 18.1 files fully removed. Project compiles and tests pass. Clean foundation for showcase port.
</success_criteria>

<output>
After completion, create `.planning/phases/19-reimplement-skipapp-showcase-fuse-showcases-in-fuse-app-with-tca-and-skipui-api-validation/19-01-SUMMARY.md`
</output>
