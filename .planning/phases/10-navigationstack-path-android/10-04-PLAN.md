---
phase: 10-navigationstack-path-android
plan: 04
type: execute
wave: 3
depends_on: [10-03]
files_modified:
  - forks/skip-fuse-ui/Sources/SkipSwiftUI/**
  - forks/swift-composable-architecture/Sources/ComposableArchitecture/**
  - forks/swift-navigation/Sources/**
  - examples/fuse-library/Tests/**
  - examples/fuse-app/Sources/**
autonomous: true
requirements: [NAV-01, NAV-02, NAV-03, TCA-32, TCA-33]

must_haves:
  truths:
    - "All fix-required gaps from 10-GAP-REPORT.md are addressed with code changes"
    - "Dismiss mechanism verified working on Android (withKnownIssue wrappers removed if dismiss works)"
    - "Each significant gap fix has at least one test proving the bridging works"
    - "macOS swift build and swift test pass for both fuse-library and fuse-app"
    - "skip android build succeeds for both fuse-library and fuse-app"
  artifacts:
    - path: "forks/skip-fuse-ui/Sources/SkipSwiftUI/"
      provides: "Missing skip-fuse-ui counterparts for skip-ui modifications"
    - path: "examples/fuse-library/Tests/"
      provides: "Tests validating gap fixes"
  key_links:
    - from: "forks/skip-fuse-ui"
      to: "forks/skip-ui"
      via: "SkipUIBridging protocol Java_view delegates"
      pattern: "Java_view"
    - from: "examples/fuse-library/Tests"
      to: "forks/skip-fuse-ui"
      via: "Import and exercise gap-fixed APIs"
      pattern: "import.*SkipFuseUI\\|import SwiftUI"
---

<objective>
Fix all gaps identified as fix-required in 10-GAP-REPORT.md — create missing skip-fuse-ui counterparts, correct guards, verify dismiss, and add tests for each fix.

Purpose: Close all audit-identified gaps so that TCA apps work correctly on Android with skip-fuse-ui's bridging layer. This is the main code change plan.

Output: Fixed gaps with tests, passing builds on macOS and Android.
</objective>

<execution_context>
@/Users/jacob/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jacob/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-navigationstack-path-android/10-CONTEXT.md
@.planning/phases/10-navigationstack-path-android/10-RESEARCH.md
@.planning/phases/10-navigationstack-path-android/10-GAP-REPORT.md
@.planning/phases/10-navigationstack-path-android/10-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix skip-fuse-ui counterpart gaps and correct cross-fork guards</name>
  <files>
    forks/skip-fuse-ui/Sources/SkipSwiftUI/**
    forks/swift-composable-architecture/Sources/ComposableArchitecture/**
    forks/swift-navigation/Sources/**
  </files>
  <action>
Read `10-GAP-REPORT.md` and execute ALL fix-required items. The exact files and changes depend on the gap report findings, but the expected categories are:

**Category 1: Missing skip-fuse-ui counterparts.**
For each missing counterpart identified in the gap report:
1. Create the SkipSwiftUI wrapper following the established pattern: `struct` conforming to `View` + `SkipUIBridging` extension providing `Java_view`.
2. Match SwiftUI API exactly — same generic signatures, parameter names, behavior.
3. Type-erase at bridge boundary (`any View` when crossing to skip-ui).
4. Follow existing file organization in `forks/skip-fuse-ui/Sources/SkipSwiftUI/`.

**Category 2: Incorrect TCA guards.**
For each guard the gap report flags as incorrect:
1. If the guarded code uses APIs that skip-fuse-ui now provides → remove the guard (make code available on all platforms).
2. If the guard has an incorrect `#else` fallback → fix the fallback.
3. Preserve guards that are correct (UIKit code, deprecated APIs, withTransaction usage).

**Special investigation: NavigationStack extension (line 150).**
If the gap report confirms skip-fuse-ui's generic NavigationStack allows TCA's `extension NavigationStack where Data == StackState<State>.PathView` to compile:
1. Remove the `#if !os(Android)` guard from the extension.
2. Evaluate whether the free-function adapter from prior 10-01 can be simplified or removed.
3. If the extension does NOT compile (type erasure at bridge boundary), keep the adapter approach.

**Category 3: Dismiss fix.**
If the gap report confirms dismiss works on Android:
1. Find all `withKnownIssue` wrappers in integration tests related to dismiss (research identified 3 in Phase 09-04).
2. Remove the wrappers and verify tests pass without them.
3. If dismiss only partially works, fix the broken parts per the gap report's recommendations.
If the gap report says dismiss is broken:
1. Implement the fix per the report's recommendations.
2. The research traced the full chain — the issue is likely in the Dismiss.swift guard or the EnvironmentValues bridging.

**Category 4: swift-navigation / swift-sharing guard corrections.**
Apply any guard fixes identified in the gap report for these forks.

**Commit strategy:** One commit per logical fix within fork submodules. Parent repo updates all submodule pointers in a single commit at the end.
  </action>
  <verify>
    <automated>cd /Users/jacob/Developer/src/github/jacobcxdev/swift-crossplatform/examples/fuse-library && swift build 2>&1 | tail -5</automated>
    <manual>Check that all fix-required gaps from the report have corresponding code changes</manual>
  </verify>
  <done>All fix-required gaps have code changes. macOS build passes for both examples.</done>
</task>

<task type="auto">
  <name>Task 2: Add tests for gap fixes and verify Android build</name>
  <files>
    examples/fuse-library/Tests/**
    examples/fuse-app/Sources/**
  </files>
  <action>
**Step 1: Add tests for each significant gap fix.**
Per CONTEXT.md decision: "Tests per gap — each significant gap fix gets at least one test proving the bridging works."

For each fix from Task 1:
1. Add a test in the appropriate test target (NavigationTests, ObservationTests, FuseAppIntegrationTests, etc.).
2. Tests should exercise the bridged API path, not just compilation.
3. Follow established test patterns: Swift Testing macros for new tests (except XCTest for Skip-transpiled targets per Phase 08 decision).

**Step 2: Dismiss test verification.**
If dismiss was confirmed working:
1. Verify the integration tests that previously had `withKnownIssue` now pass clean.
2. Run the full test suite: `swift test` in fuse-library and fuse-app.

**Step 3: Full test suite on macOS.**
```bash
cd examples/fuse-library && swift test
cd examples/fuse-app && swift test
```
All tests must pass. Fix any regressions.

**Step 4: Android build verification.**
```bash
cd examples/fuse-library && skip android build
cd examples/fuse-app && skip android build
```
Both must succeed. This validates the skip-fuse-ui changes compile through the Skip transpiler.

**Step 5: Android test verification.**
```bash
cd examples/fuse-library && skip android test
cd examples/fuse-app && skip android test
```
Run if Android emulator is available. Report results — total tests, passing, known issues.
  </action>
  <verify>
    <automated>cd /Users/jacob/Developer/src/github/jacobcxdev/swift-crossplatform/examples/fuse-library && swift test 2>&1 | tail -5</automated>
    <manual>Verify Android build succeeds, check test count hasn't regressed</manual>
  </verify>
  <done>All gap fix tests pass on macOS. Android build succeeds. Test count has not regressed. Dismiss withKnownIssue wrappers removed if dismiss confirmed working.</done>
</task>

</tasks>

<verification>
- `swift test` passes for both fuse-library and fuse-app on macOS
- `skip android build` succeeds for both examples
- All fix-required gaps from 10-GAP-REPORT.md have corresponding code changes
- Each significant fix has at least one test
- Dismiss status resolved (withKnownIssue removed or issue documented with fix)
</verification>

<success_criteria>
- Zero fix-required gaps remaining from the audit
- Test suite passes on macOS with no regressions
- Android builds succeed for both examples
- Dismiss works on Android OR documented with specific remaining blocker
- One commit per logical fix in fork submodules
</success_criteria>

<output>
After completion, create `.planning/phases/10-navigationstack-path-android/10-04-SUMMARY.md`
</output>
