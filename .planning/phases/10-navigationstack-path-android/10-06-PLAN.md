---
phase: 10-navigationstack-path-android
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - Makefile
  - .planning/STATE.md
autonomous: true
gap_closure: true
requirements:
  - NAV-01
  - NAV-02
  - NAV-03
  - TCA-32
  - TCA-33

must_haves:
  truths:
    - "CLAUDE.md documents all 19 forks, not 17"
    - "CLAUDE.md has an Environment Variables section explaining TARGET_OS_ANDROID, SKIP_BRIDGE, and #if os(Android)"
    - "CLAUDE.md lists 4 new gotchas: withTransaction fatalError, Android builds vs tests, clean builds after dep changes, skip-fuse-ui generic NavigationStack"
    - "CLAUDE.md Build & Test section describes smart default Makefile targets covering both examples"
    - "Makefile iterates over fuse-library and fuse-app by default for build, test, android-build, android-test, skip-test, skip-verify, clean"
    - "Makefile EXAMPLE= override still works for single-example targeting"
    - "STATE.md entries for CLAUDE.md and Makefile accurately reflect actual file state"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Updated developer guidance with 19 forks, env vars, new gotchas, smart defaults"
      contains: "19 fork submodules"
    - path: "Makefile"
      provides: "Multi-example smart default build targets"
      contains: "EXAMPLES ?= fuse-library fuse-app"
    - path: ".planning/STATE.md"
      provides: "Corrected phase 10 decision entries matching actual file state"
  key_links:
    - from: "CLAUDE.md"
      to: "Makefile"
      via: "Build & Test section references Makefile targets"
      pattern: "make build.*both examples"
---

<objective>
Close 2 verification gaps from Phase 10: apply the CLAUDE.md updates (environment variables, gotchas, fork count, Build & Test section) and Makefile smart defaults (multi-example iteration) that were planned in 10-01 but never written to disk. Correct STATE.md entries that incorrectly claim these were already done.

Purpose: SC4 and SC5 from the Phase 10 verification report both FAILED because the files were never modified despite STATE.md claiming completion. This plan applies the actual changes.
Output: Updated CLAUDE.md, Makefile, and corrected STATE.md.
</objective>

<execution_context>
@/Users/jacob/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jacob/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-navigationstack-path-android/10-CONTEXT.md
@.planning/phases/10-navigationstack-path-android/10-VERIFICATION-CLAUDE.md
@CLAUDE.md
@Makefile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLAUDE.md with environment variables, gotchas, fork count, and Build & Test section</name>
  <files>CLAUDE.md</files>
  <action>
Apply the following changes to CLAUDE.md:

1. **Update fork count** (2 locations):
   - Line 9: Change "17 fork submodules" to "19 fork submodules"
   - Line 51: Change "All 17 forks" to "All 19 forks"

2. **Update Build & Test section** (lines 1-32):
   - Change the intro paragraph (line 9) to explain that `make build`, `make test`, etc. now iterate over both `fuse-library` and `fuse-app` by default. `EXAMPLE=fuse-app` overrides to target a single example.
   - Update the comment block to show the new behavior:
     ```
     make build                         # swift build both examples (macOS)
     make test                          # swift test both examples (macOS)
     make android-build                 # skip android build both examples
     make android-test                  # skip android test both examples
     make skip-test                     # skip test both examples
     make skip-verify                   # skip verify --fix both examples
     make clean                         # clean both examples
     make test-filter FILTER=ObservationTests  # Run a single test suite (uses EXAMPLE, default: fuse-library)
     ```

3. **Add Environment Variables section** after the "Platform Conditionals" section (after line 77) and before "Project Planning":

   ```markdown
   ## Environment Variables

   | Variable / Guard | Scope | Effect |
   |------------------|-------|--------|
   | `TARGET_OS_ANDROID` | SPM `Context.environment` | Enables Android-specific dependencies in Package.swift at resolution time |
   | `SKIP_BRIDGE` | Swift compiler define | Gates bridge-level observation code in skip-android-bridge (`ObservationRecording`, JNI exports) |
   | `#if os(Android)` | Swift conditional compilation | Standard platform check for Android-specific runtime code paths |
   | `#if canImport(SwiftUI)` | Swift conditional compilation | False on Android — SkipFuseUI re-exports SkipSwiftUI, not Apple's SwiftUI module |
   | `#if SKIP` | Swift conditional compilation | True only in Skip-transpiled Kotlin context (e.g., `loadPeerLibrary`) |
   ```

4. **Add 4 new gotchas** to the Gotchas section (after line 96):

   - **`withTransaction` unavailable on Android**: `withTransaction` is `@available(*, unavailable)` with `fatalError()` in skip-fuse-ui. Use plain `store.send()` for state mutations — never use animated navigation or dismiss paths on Android.
   - **Android builds can pass when running fails**: `skip android build` succeeding does NOT mean the app works. Always verify with `skip android test` or emulator testing — runtime JNI/bridge failures only surface at execution time.
   - **Clean builds after dependency changes**: After modifying any fork's `Package.swift` or changing submodule pointers, run `swift package clean` or `make clean` before rebuilding. Incremental builds can use stale dependency artifacts.
   - **skip-fuse-ui NavigationStack is generic**: Unlike skip-ui's non-generic `NavigationStack`, skip-fuse-ui provides `NavigationStack<Data, Root>` matching SwiftUI's generic signature. TCA extensions constrain `Data` to `StackState<State>.PathView` — this compiles on Android via skip-fuse-ui.

5. **Update Roadmap reference** in Project Planning section: Change "7 phases" to "10 phases" on line 83.
  </action>
  <verify>
    <automated>grep -c "19 fork submodules" CLAUDE.md && grep -c "Environment Variables" CLAUDE.md && grep -c "withTransaction" CLAUDE.md && grep -c "skip-fuse-ui NavigationStack is generic" CLAUDE.md && grep -c "10 phases" CLAUDE.md</automated>
    <manual>Read CLAUDE.md and verify all 5 change groups are present and correctly formatted</manual>
  </verify>
  <done>CLAUDE.md contains: "19 fork submodules" (2 locations), Environment Variables table with 5 entries, 8 total gotchas (original 4 + 4 new), Build & Test section referencing smart defaults covering both examples, "10 phases" in Project Planning</done>
</task>

<task type="auto">
  <name>Task 2: Update Makefile with smart defaults and fix STATE.md inaccuracies</name>
  <files>Makefile, .planning/STATE.md</files>
  <action>
**Makefile changes:**

Replace the entire Makefile with a version that:

1. Adds `EXAMPLES ?= fuse-library fuse-app` as the default multi-example variable
2. Adds `ifdef EXAMPLE` / `else` conditional block:
   - If `EXAMPLE=X` is set explicitly, operate on that single example (backward compatible)
   - If not set, iterate over all `EXAMPLES` using `$(foreach)`
3. Targets to update: `build`, `test`, `android-build`, `android-test`, `skip-test`, `skip-verify`, `clean`
4. `test-filter` stays single-example (uses first example from EXAMPLES or explicit EXAMPLE override) since filters are typically target-specific
5. Keep all submodule management targets unchanged (status, push-all, pull-all, diff-all, branch-all)

Pattern for multi-example targets:
```makefile
EXAMPLES ?= fuse-library fuse-app

ifdef EXAMPLE
# Single example override: EXAMPLE=fuse-app make build
TARGETS := $(EXAMPLE)
else
TARGETS := $(EXAMPLES)
endif

.PHONY: build test android-build android-test skip-test skip-verify clean

build:
	@for ex in $(TARGETS); do \
		echo "=== Building $$ex ===" && \
		cd examples/$$ex && swift build && cd ../.. || exit 1; \
	done

test:
	@for ex in $(TARGETS); do \
		echo "=== Testing $$ex ===" && \
		cd examples/$$ex && swift test && cd ../.. || exit 1; \
	done
```

Follow this pattern for android-build, android-test, skip-test, skip-verify, clean. Use `$(MAKE) -C` or shell `cd` with `|| exit 1` for error propagation.

For `test-filter`, use `$(firstword $(TARGETS))` as the example directory since filters are suite-specific.

**STATE.md corrections:**

In STATE.md under `### Decisions`, find and update these two entries:
- `[Phase 10]: CLAUDE.md expanded with environment variable docs, 4 new gotchas, updated Makefile reference` — append " (applied in 10-06 gap closure)" to clarify it was not done in the original plan
- `[Phase 10]: Makefile smart defaults iterate both examples; EXAMPLE= override for single targeting` — append " (applied in 10-06 gap closure)" to clarify it was not done in the original plan
  </action>
  <verify>
    <automated>grep -c "EXAMPLES ?=" Makefile && grep "ifdef EXAMPLE" Makefile && grep "fuse-library fuse-app" Makefile && grep "10-06 gap closure" .planning/STATE.md | wc -l</automated>
    <manual>Run `make build` from repo root and verify it builds both fuse-library and fuse-app sequentially. Run `EXAMPLE=fuse-app make build` and verify it only builds fuse-app.</manual>
  </verify>
  <done>Makefile defaults to iterating both fuse-library and fuse-app for all build/test targets. EXAMPLE= override targets a single example. STATE.md entries corrected to reflect that changes were applied in 10-06, not in the original claimed plan.</done>
</task>

</tasks>

<verification>
1. `grep "19 fork submodules" CLAUDE.md` returns matches (2 occurrences)
2. `grep "Environment Variables" CLAUDE.md` returns a match
3. `grep "EXAMPLES ?= fuse-library fuse-app" Makefile` returns a match
4. `grep "ifdef EXAMPLE" Makefile` returns a match
5. `grep "10-06 gap closure" .planning/STATE.md` returns 2 matches
6. `make build` from repo root builds both examples without error
7. `EXAMPLE=fuse-app make build` builds only fuse-app
</verification>

<success_criteria>
- CLAUDE.md has 19 fork count, Environment Variables section, 8 gotchas, updated Build & Test, 10 phases reference
- Makefile iterates both examples by default; EXAMPLE= override works
- STATE.md decision entries corrected with "(applied in 10-06 gap closure)" annotation
- All verification commands pass
</success_criteria>

<output>
After completion, create `.planning/phases/10-navigationstack-path-android/10-06-SUMMARY.md`
</output>
