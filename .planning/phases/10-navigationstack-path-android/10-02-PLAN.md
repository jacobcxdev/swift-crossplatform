---
phase: 10-navigationstack-path-android
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - forks/sqlite-data/Package.swift
  - forks/swift-composable-architecture/Package.swift
  - examples/fuse-library/Package.swift
  - examples/fuse-app/Package.swift
autonomous: true
requirements: [NAV-01, NAV-02, NAV-03, TCA-32, TCA-33]

must_haves:
  truths:
    - "swift package resolve in fuse-library produces zero identity conflict warnings"
    - "swift package resolve in fuse-app produces zero identity conflict warnings"
    - "swift build succeeds for both fuse-library and fuse-app on macOS"
  artifacts:
    - path: "forks/sqlite-data/Package.swift"
      provides: "skip-android-bridge as local path instead of remote URL"
      contains: '../skip-android-bridge'
    - path: "forks/swift-composable-architecture/Package.swift"
      provides: "skip-android-bridge as local path instead of remote URL"
      contains: '../skip-android-bridge'
    - path: "examples/fuse-library/Package.swift"
      provides: "skip-fuse as local path instead of remote URL"
      contains: '../../forks/skip-fuse'
    - path: "examples/fuse-app/Package.swift"
      provides: "Clean dependency declarations with no unused deps"
  key_links:
    - from: "forks/sqlite-data/Package.swift"
      to: "forks/skip-android-bridge"
      via: "local path dependency"
      pattern: '\.\./skip-android-bridge'
    - from: "forks/swift-composable-architecture/Package.swift"
      to: "forks/skip-android-bridge"
      via: "local path dependency"
      pattern: '\.\./skip-android-bridge'
---

<objective>
Resolve all SPM dependency identity conflicts by converting remote URL references to local sibling paths for forked packages, and clean up unused dependency declarations in example Package.swift files.

Purpose: SPM identity conflicts cause warnings and unpredictable dependency resolution. All references to forked packages must use local paths so SwiftPM resolves them by directory name identity, not URL identity.

Output: Updated Package.swift files across 4 packages with zero identity conflict warnings.
</objective>

<execution_context>
@/Users/jacob/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jacob/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-navigationstack-path-android/10-CONTEXT.md
@.planning/phases/10-navigationstack-path-android/10-RESEARCH.md
@forks/sqlite-data/Package.swift
@forks/swift-composable-architecture/Package.swift
@examples/fuse-library/Package.swift
@examples/fuse-app/Package.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert remote URL references to local paths in fork Package.swift files</name>
  <files>
    forks/sqlite-data/Package.swift
    forks/swift-composable-architecture/Package.swift
  </files>
  <action>
**Step 1: Grep ALL Package.swift files for remote URL references to forked packages.**

Search for `skip-android-bridge.git` (remote URL form) across all Package.swift files. The research identified two files:
- `forks/sqlite-data/Package.swift` (~line 51)
- `forks/swift-composable-architecture/Package.swift` (~line 41)

Also grep for any other forked packages referenced by remote URL that should be local paths. Check for:
- `skip-fuse-ui.git` — should be `../skip-fuse-ui`
- Any `pointfreeco` or `skiptools` URLs pointing to packages we have as local forks

**Step 2: In `forks/sqlite-data/Package.swift`:**

Change the skip-android-bridge line from:
```swift
.package(url: "https://source.skip.tools/skip-android-bridge.git", "0.6.1"..<"2.0.0"),
```
To:
```swift
.package(path: "../skip-android-bridge"),
```

Leave `skip-bridge` and `swift-jni` as remote URLs (they are NOT forked, no identity conflict).

**Step 3: In `forks/swift-composable-architecture/Package.swift`:**

Change the skip-android-bridge line from:
```swift
.package(url: "https://source.skip.tools/skip-android-bridge.git", "0.6.1"..<"2.0.0"),
```
To:
```swift
.package(path: "../skip-android-bridge"),
```

Leave `skip-bridge` and `swift-jni` as remote URLs. Verify `skip-fuse-ui` is already a local path (`../skip-fuse-ui`).

**Step 4: Grep all remaining fork Package.swift files** for any other remote URLs referencing forked packages. Check swift-navigation, swift-sharing, swift-perception specifically. Fix any found.

**Step 5: Commit each fork submodule separately** (one commit per fork with its Package.swift change).
  </action>
  <verify>
    <automated>cd /Users/jacob/Developer/src/github/jacobcxdev/swift-crossplatform && grep -r "skip-android-bridge.git" forks/*/Package.swift examples/*/Package.swift; echo "EXIT:$?"</automated>
    <manual>Zero matches means all remote URL references to skip-android-bridge have been converted to local paths</manual>
  </verify>
  <done>All forked packages reference skip-android-bridge via local path `../skip-android-bridge`, not remote URL. No SPM identity conflicts for this package.</done>
</task>

<task type="auto">
  <name>Task 2: Fix example Package.swift files and verify zero-warning resolution</name>
  <files>
    examples/fuse-library/Package.swift
    examples/fuse-app/Package.swift
  </files>
  <action>
**Step 1: In `examples/fuse-library/Package.swift`:**

The research identified that `skip-fuse` is referenced via remote URL but a local fork exists at `forks/skip-fuse`. Change:
```swift
.package(url: "https://source.skip.tools/skip-fuse.git", from: "...")
```
To:
```swift
.package(path: "../../forks/skip-fuse")
```

Also verify all other fork references use local paths (`../../forks/<name>`).

**Step 2: In `examples/fuse-app/Package.swift`:**

Per CONTEXT.md decision: "Remove unused dependency declarations from both fuse-app and fuse-library Package.swift — they cause warnings and will become errors in future SwiftPM."

Review the dependency list against actual target dependencies. Remove any `.package(...)` declarations that no target uses. Common candidates per research: `swift-dependencies`, `skip-android-bridge`, `skip-ui` if only used transitively.

Also verify all remaining fork references use local paths.

**Step 3: Verify zero warnings.**

Run in both example directories:
```bash
cd examples/fuse-library && swift package resolve 2>&1 | grep -i "warning\|identity\|conflict"
cd examples/fuse-app && swift package resolve 2>&1 | grep -i "warning\|identity\|conflict"
```

Both must produce zero matches.

**Step 4: Build verification.**

Run `swift build` in both example directories to confirm resolution didn't break anything.

**Step 5: Commit.** One commit per example directory, then update parent repo submodule pointers in a single parent commit.
  </action>
  <verify>
    <automated>cd /Users/jacob/Developer/src/github/jacobcxdev/swift-crossplatform/examples/fuse-library && swift package resolve 2>&1 | grep -ci "warning\|identity\|conflict" | xargs test 0 -eq && echo "PASS" || echo "FAIL"</automated>
    <manual>Run same check for fuse-app. Run swift build in both directories.</manual>
  </verify>
  <done>Both fuse-library and fuse-app resolve with zero identity conflict warnings. swift build succeeds in both. No unused dependency declarations remain.</done>
</task>

</tasks>

<verification>
- `grep -r "skip-android-bridge.git" forks/*/Package.swift examples/*/Package.swift` returns zero matches
- `swift package resolve` in fuse-library produces zero warnings
- `swift package resolve` in fuse-app produces zero warnings
- `swift build` succeeds in both fuse-library and fuse-app
</verification>

<success_criteria>
- skip-android-bridge referenced via local path in all Package.swift files
- skip-fuse referenced via local path in fuse-library
- No unused dependency declarations in fuse-app or fuse-library
- Zero SPM identity conflict warnings across both example projects
- Both example projects build successfully on macOS
</success_criteria>

<output>
After completion, create `.planning/phases/10-navigationstack-path-android/10-02-SUMMARY.md`
</output>
