---
phase: 10-navigationstack-path-android
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - examples/fuse-app/Sources/FuseApp/ContactsFeature.swift
  - examples/fuse-library/Tests/NavigationTests/NavigationStackTests.swift
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - NAV-03
  - TCA-32
  - TCA-33

must_haves:
  truths:
    - "ContactsFeature.swift has a single NavigationStack(path:root:destination:) code path for both platforms"
    - "No #if os(Android) workaround remains in ContactsView.body"
    - "TODO comment about Android NavigationStack gap is removed"
    - "NavigationStack adapter tests validate push/pop dispatch through binding"
    - "skip android build succeeds with the adapter"
  artifacts:
    - path: "examples/fuse-app/Sources/FuseApp/ContactsFeature.swift"
      provides: "Unified ContactsView with single NavigationStack code path"
    - path: "examples/fuse-library/Tests/NavigationTests/NavigationStackTests.swift"
      provides: "Android adapter binding bridge tests"
  key_links:
    - from: "ContactsView.body"
      to: "NavigationStack(path:root:destination:)"
      via: "$store.scope(state: \\.path, action: \\.path)"
      pattern: "NavigationStack\\(path:"
    - from: "NavigationStackTests"
      to: "Store.subscript[fileID:filePath:line:column:]"
      via: "PathView set dispatching push/pop actions"
      pattern: "testPathViewBinding"
---

<objective>
Unify ContactsFeature.swift to use a single NavigationStack(path:root:destination:) code path (removing the `#if os(Android)` workaround), add adapter binding tests to NavigationStackTests, and verify the Android build.

Purpose: The `#if os(Android)` branch in ContactsView renders a plain NavigationStack without path binding, making contact detail navigation non-functional on Android. With the adapter from Plan 01, both platforms can use the same code. Tests validate the PathView-to-[Any] binding bridge dispatches correct TCA actions.

Output: Unified ContactsFeature.swift + extended NavigationStackTests + Android build verification.
</objective>

<execution_context>
@/Users/jacob/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jacob/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-navigationstack-path-android/10-RESEARCH.md
@.planning/phases/10-navigationstack-path-android/10-01-SUMMARY.md
@examples/fuse-app/Sources/FuseApp/ContactsFeature.swift
@examples/fuse-library/Tests/NavigationTests/NavigationStackTests.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unify ContactsFeature and remove Android workaround</name>
  <files>examples/fuse-app/Sources/FuseApp/ContactsFeature.swift</files>
  <action>
1. **Remove the TODO comment** at lines 23-27 of ContactsFeature.swift:
   ```
   // TODO: ANDROID - StackState path binding is silently unused on Android.
   // NavigationStack does not bind the path on this platform (see ContactsView
   // #if os(Android) branch which uses plain NavigationStack without path:).
   // Implement path binding in skip-ui or use a platform-appropriate navigation pattern.
   ```

2. **Replace the entire `#if os(Android)` / `#else` / `#endif` block** in `ContactsView.body` (lines 276-303) with the unified code:
   ```swift
   NavigationStack(path: $store.scope(state: \.path, action: \.path)) {
       contactsList
   } destination: { store in
       switch store.case {
       case let .detail(detailStore):
           ContactDetailView(store: detailStore)
       }
   }
   .sheet(
       item: $store.scope(state: \.destination?.addContact, action: \.destination.addContact)
   ) { addStore in
       NavigationStack {
           AddContactView(store: addStore)
       }
   }
   ```

   This is the exact same code that was previously in the `#else` (iOS) branch. On iOS it resolves to the `extension NavigationStack.init(path:root:destination:)`. On Android it resolves to the free function `NavigationStack(path:root:destination:)` from Plan 01's adapter.

3. **Verify no other `#if os(Android)` blocks remain** in ContactsFeature.swift related to navigation. The only Android conditionals should be those genuinely needed for other purposes (there should be none in this file after this change).
  </action>
  <verify>
    <automated>cd /Users/jacob/Developer/src/github/jacobcxdev/swift-crossplatform/examples/fuse-app && swift build 2>&1 | tail -5</automated>
    <manual>Verify ContactsFeature.swift has no #if os(Android) blocks and no TODO: ANDROID comment</manual>
  </verify>
  <done>
    - ContactsView.body uses single `NavigationStack(path:root:destination:)` code path
    - No `#if os(Android)` workaround in ContactsFeature.swift
    - No TODO: ANDROID comment about NavigationStack gap
    - `swift build` passes for fuse-app on macOS
  </done>
</task>

<task type="auto">
  <name>Task 2: Add NavigationStack adapter tests and verify Android build</name>
  <files>examples/fuse-library/Tests/NavigationTests/NavigationStackTests.swift</files>
  <action>
1. **Add new tests** to `NavigationStackTests.swift` that validate the PathView-to-action dispatch logic that the Android adapter relies on. These tests exercise the `Store` subscript setter (lines 440-489 of NavigationStack+Observation.swift) which is the same push/pop dispatch logic the Android adapter replicates:

   a. `testPathViewBindingPush()` — Create a Store with AppFeature, get a `@Bindable` reference, obtain the stack store via `$bindableStore.scope(state: \.path, action: \.path)`, then verify that after `store.send(.pushDetail("Test"))`, `store.state.path.count == 1` and the pushed element has the correct title. This validates that the data flow TCA-32 (StackState append) works end-to-end through the binding.

   b. `testPathViewBindingPop()` — Push 2 items, then pop the last via `.path(.popFrom(id:))`, verify count is 1 and the remaining item is correct. This validates NAV-03 (path removeLast pops top destination) through the store action dispatch path that the Android adapter's binding setter uses.

   c. `testPathViewBindingMultiplePushPop()` — Push 3 items, pop 1, push 1, verify count is 3 and the final element is the newly pushed one. This validates that interleaved push/pop sequences (NAV-02 + NAV-03) maintain correct stack state through the same action dispatch path.

   All tests should use the existing `AppFeature` / `AppFeaturePath` / `DetailFeature` reducers already defined in the test file. Use `@Suite(.serialized) @MainActor` pattern per project convention. Use Swift Testing `@Test` macros and `#expect`.

2. **Run Android build verification:**
   ```bash
   cd examples/fuse-library && skip android build 2>&1 | tail -20
   ```
   This validates that the adapter compiles on Android through Skip's transpiler. If the build fails, investigate and fix the adapter code.

3. **Run the full macOS test suite** to ensure no regressions:
   ```bash
   cd examples/fuse-library && swift test 2>&1 | tail -20
   ```
  </action>
  <verify>
    <automated>cd /Users/jacob/Developer/src/github/jacobcxdev/swift-crossplatform && make test 2>&1 | tail -10</automated>
    <manual>Verify NavigationStackTests has new adapter-related tests and all pass</manual>
  </verify>
  <done>
    - 3 new tests (testPathViewBindingPush, testPathViewBindingPop, testPathViewBindingMultiplePushPop) pass
    - All existing NavigationStackTests still pass (no regressions)
    - Full macOS test suite passes
    - `skip android build` succeeds (adapter compiles on Android)
  </done>
</task>

</tasks>

<verification>
- `swift build` passes from `examples/fuse-app/` (unified ContactsView compiles)
- `make test` passes (all existing + new navigation tests)
- `skip android build` succeeds from `examples/fuse-library/`
- ContactsFeature.swift has zero `#if os(Android)` blocks
- ContactsFeature.swift has no TODO: ANDROID comment
- NavigationStackTests has 3 new binding adapter tests
</verification>

<success_criteria>
- Single code path for ContactsView navigation on both platforms
- Android build compiles the NavigationStack adapter through Skip transpiler
- All macOS tests pass including new adapter binding tests
- Zero platform-conditional workarounds in ContactsFeature.swift
</success_criteria>

<output>
After completion, create `.planning/phases/10-navigationstack-path-android/10-02-SUMMARY.md`
</output>
